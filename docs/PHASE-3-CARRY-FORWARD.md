# Phase 3 Carry-Forward Catalogue

**Created:** 2026-05-07 (Epic 13.5 retro Action Item A1)
**Status:** Living document — update as items close or new debt is named

This document consolidates **all open carry-forward items** from Phase-2 retros (Epics 9–13.5) plus residual `docs/WISHLIST.md` ideas, into a single prioritised list. It supersedes scattered `⏳ Phase-3 carry-forward` notes in individual retros. The sources are listed per-item for traceability.

**Project lead direction (2026-05-07 retro):** focus Phase 3 on stabilisation + standards close-out (categories A + B) before any new feature work (category E). Tooling and ROM-pressure-only items (C + D) are deferred indefinitely — surface them only if a specific trigger fires.

---

## Priority Legend

- **P1** — Active. Should land in the first Phase-3 epic (or epics) before any new feature work
- **P2** — Later. Post-stabilisation; revisit when category A + B is mostly closed
- **P3** — Deferred indefinitely. Re-evaluate only if a specific trigger fires (ROM pressure, perf-claim dispute, etc.)

---

## A. Standards / Compliance — **P1**

| # | Item | Source | Estimated effort | Notes |
|---|------|--------|------------------|-------|
| A.1 | **§-by-§ ANS Forth Core re-audit.** Walk DPANS94 + Forth 2014 chapter-by-chapter, verify each mandatory rule against the implementation. Likely 0–2 §-level structural gaps to surface (Stories 13.0 / 13.0.1 / 13.5 are the back-fill template — one story per gap, paired with a compliance-doc row addition). | Epic 13 retro #6; `docs/WISHLIST.md` § "Phase-3 systematic §-by-§ ANS Forth Core re-audit" | 1 audit story + 0–2 back-fill stories per gap | The strategic Phase-3 piece. Closes the "word-counted survey blindness" category that surfaced §3.4.1.3 / §3.1.4.1 mid-Epic-13. |
| A.2 | **Caught-form coverage gap for asm-error THROW codes −258..−269.** Harness extension to `tests/throw_migration_tests.fth` exercising the caught-form path for each asm-error code. Unblocked since Story 11.5.3 closed the EVALUATE source-frame fix. | Epic 9 retro carry → Epic 11.5 unblocked → Epic 12 carry → Epic 13 carry | ~½ story (test-only) | Standalone; can hitch-hike onto any sprint touching the THROW-test file. |
| A.3 | **Unprefixed `NUMBER?` base-specialization.** Base-aware parsing for unprefixed numerals when an explicit BASE is set. | Epic 9 retro carry → Epic 12 → Epic 13 | Small | Standalone. |

## B. Stabilisation / Process Debt — **P1**

| # | Item | Source | Estimated effort | Notes |
|---|------|--------|------------------|-------|
| B.1 | **A2 — Document `PAD` as canonical transient-buffer word for test authors.** Closes the long-overdue Epic-12 retro Action Item A1 (TIB-128 doc note) belatedly, now that the Story-13.5.4 real `PAD` exists. Add a short `tests/README.md` or inline header comment block to `Makefile`'s test section. | Epic 13.5 retro A2 (and Epic-12 retro A1) | Small (doc-only) | Should land before the next REPL probe is authored for any subsequent epic. |
| B.2 | **A3 — Capstone-aware drafting refresher: "mirrors prior arm" is a HALT signal.** Story-template / drafting-checklist edit. When a story's byte-budget rationale rests on "this mirrors arm X from Story Y", the drafter must instead count the parts of the new arm independently. Extends Lesson 13-C. | Epic 13.5 retro A3 | Small (template edit) | Apply to story-template before any future "mirror"-shaped story is drafted. |
| B.3 | **A4 — Story-to-story binary handoff: re-`wc -c` at next story's start.** Story-template edit promoting the discipline that caught the 6-byte 13.5.5-close-out-doc drift to standing practice. Every dev-pass's "Pre-edit baseline" task captures `wc -c` itself, not the prior story's reported number. | Epic 13.5 retro A4 | Small (template edit) | Per-story; template-level. Should land alongside B.2. |
| B.4 | **PD-2 — Story-drafter figure drift** (story-template note, 2.x scope). | Epic 13 retro #1 | Medium (template + retro audit) | Should land before Phase-3 story-drafting starts. Sibling of B.2 and B.3 — these three together are the story-template-discipline cluster. |
| B.5 | **PD-3 — PRD-vs-architecture transcription drift** (doc-build sync step). | Epic 13 retro #2 | Medium (process / Makefile target) | Should land before next PRD/Architecture refresh — i.e. before the Phase-3 PRD is drafted. |
| B.6 | **PD-6 — iz-cpm version stability** (`make check-tools` target). | Epic 13 retro #3 | Small | Standalone; opportunistic. |
| B.7 | **F-7 — NFR8 stress-matrix coverage gaps** (directory-full, zero-byte READ-FILE). | Epic 13 retro #5 | ~1 story (test-only + maybe small fix) | If hardware reveals issues; also a 2.x maintenance candidate. |
| B.8 | **Test-numbering hygiene** — Makefile duplicate test numbers from Story 11.3. | Epic 13 retro #11 | Small (cosmetic) | Cosmetic; renumber on next Makefile-touching story. |
| B.9 | **Disk-full hardware re-verification** on real CP/M 2.2 / MicroBeast. | `project_phase2_scope.md` | Small (hardware test) | Standalone; opportunistic. |

---

## C. Tooling / Measurement — **P3 (deferred indefinitely)**

| # | Item | Source | Trigger to revisit |
|---|------|--------|--------------------|
| C.1 | **`make bench` infrastructure** for measurement-backed NFR2 verdicts. | Epic 12 retro #3 → Epic 13 retro #7 | If ROM pressure or perf-claim disputes surface; or pre-emptively before any perf-sensitive epic |

## D. ROM-pressure-triggered — **P3 (deferred indefinitely)**

| # | Item | Source | Estimated saving |
|---|------|--------|------------------|
| D.1 | **TD-9 — DMA pool size-reduction** (4 FCBs sharing 2 buffers) | Epic 13 retro #4 | ~512 B saved |
| D.2 | **`.S` migration to pictured output** | Epic 13 retro #10 | ~90 B saved |
| D.3 | **MARKER full-graph snapshot** (current behaviour is documented scope-pick) | Epic 12 retro TD #1 | Semantics expansion — non-urgent |
| D.4 | **WORDS scope-pick (a)** | Epic 12 retro TD #2 | Semantics expansion — non-urgent |

**Trigger to revisit C / D:** ROM headroom shrinks below working margin OR a specific perf-claim dispute / use-case requirement surfaces.

---

## E. Phase-3+ Features — **P2 (later, post-stabilisation)**

These are net-new feature ideas (not debt). Defer until A + B is substantially closed, then revisit which to scope into a Phase-3 feature epic.

| # | Item | Source | Estimated effort | Notes |
|---|------|--------|------------------|-------|
| E.1 | **MicroBeast hardware vocabulary** (system timer ISR, GPIO, board-specific) | `project_phase2_scope.md` | Medium-large (1 epic) | Strong fit with E.2 via timer-driven `PAUSE` |
| E.2 | **Multitasker** (polyForth/fig-Forth style; tasks yield with `PAUSE`; `TASK` / `ACTIVATE`) | `docs/WISHLIST.md` | Large (1 epic) | Could pair with E.1 for ISR-driven preemption-feel |
| E.3 | **Semaphores** (`SIGNAL` / `WAIT`) | `docs/WISHLIST.md` | Small (post-multitasker) | Depends on E.2 |
| E.4 | **`SEE` decompiler** | `docs/WISHLIST.md` | Medium | Likely depends on E.5 |
| E.5 | **`TRAVERSE-WORDLIST`** (ANS extension) | `docs/WISHLIST.md` | Small | Enables E.4 (SEE) + xref tools |
| E.6 | **ANS Forth Locals** (`{: a b -- c :}` or `VALUE` / `TO`) | `docs/WISHLIST.md` | Medium-large | Compiler surgery |
| E.7 | **Z80 IN / OUT primitives** | `docs/WISHLIST.md` | Small | Easy as code words; could hitch-hike onto E.1 |
| E.8 | **Compilation to .com binary** (Forth without outer interpreter; tree-shaking) | `docs/WISHLIST.md` | Large (separate tool / mode) | Strategic — shifts antforth's product positioning |
| E.9 | **OO** (need Pountain's book first) | `docs/WISHLIST.md` | Large; research-first | Strategic; uncertain |
| E.10 | **Beginner's guide** | `project_phase2_scope.md` | Medium (doc) | User-facing; pairs naturally with v2.0 launch publicity |
| E.11 | **Per-wordset reference doc** | `project_phase2_scope.md` | Medium-large (doc) | Reference doc for the now-shipped wordsets |

---

## Suggested Phase-3 First-Epic Shape

The 12 P1 items split naturally into a process-foundation lead-in followed by the standards re-audit work. Mirroring the Epic 13.5 PD-1-first sequencing pattern:

- **Lead-in (must land first):** B.1 + B.2 + B.3 + B.4 + B.5. Story-template / process-discipline edits (mostly small, doc-only). These shape every subsequent dev-pass in the epic.
- **Standards re-audit (the strategic body):** A.1 §-by-§ Core re-audit. One audit story; 0–2 back-fill stories per gap surfaced.
- **Hitch-hikers (can land in any story touching the relevant area):** A.2 caught-form THROW −258..−269 (any THROW-test sprint), A.3 unprefixed `NUMBER?` (any number-parsing sprint), B.6 `make check-tools` (any tooling sprint), B.8 test-numbering hygiene (next Makefile-touching story), B.9 disk-full hardware re-verification (any hardware-touching story).
- **Conditional:** B.7 NFR8 stress-matrix gaps if hardware reveals issues (otherwise 2.x maintenance).

This shape would frame the first Phase-3 epic as **"Phase-3 process-foundation + ANS Core §-by-§ re-audit + standalone debt close-out"** — comparable in shape to Epic 13.5 (process-recovery vehicle) and could close the residual carry-forward catalogue cleanly before any net-new feature epic starts.

---

## Status Tracking

Mark items as `✅ Done`, `🔄 In progress`, `❌ Dropped`, or leave default (open) as work proceeds. When an item closes, append a closure note (story reference + date) and leave the row in place as historical record.

| # | Status | Closure note |
|---|--------|--------------|
| B.1 | ✅ Done | Story 14.1 / 2026-05-08 — `tests/README.md` authored at project root; Makefile test-section pointer comment added; PAD-as-canonical guidance landed (ANS §6.2.2000 + §3.3.3.6); HERE-collision class documented (Story-13.5.1 / 13.6 F-9 / 13.5.4 references); S12 reference inline; verdict criteria 5/5 PASS. |
| B.2 | ✅ Done | Story 14.2 / 2026-05-08 — second `<critical>` block landed in `_bmad/bmm/workflows/4-implementation/create-story/instructions.xml` adjacent (after) the PD-1 block; trigger surface enumerates literal "mirrors" + "same shape as Story Y" + "this is the X arm of Story Y" + Story-Y reference pattern + comparison-to-prior-work shorthand; required action: itemise new arm parts independently before byte-budget rationale is accepted; cites Lesson 13.5-C + B.2 closure + TD-7 / Story 13.5.5 +40-byte overshoot. Verdict criteria 4/4 PASS (synthesised mirror phrase surfaces HALT; grep "mirrors" / canonical phrase / lesson cite / `<critical>` count delta +1). Zero binary delta. |
| B.3 | ✅ Done | Story 14.3 / 2026-05-08 — new `### Pre-edit baseline` subsection (Form A) landed at top of `## Tasks / Subtasks` in `_bmad/bmm/workflows/4-implementation/create-story/template.md`; first bullet captures `wc -c build/antforth.com` with sub-bullet "Do not inherit the prior story's reported number — re-`wc -c` from the actual current build artifact (B.3 / Lesson 13.5-F; cf. Story 13.5.5 close-out 6-byte doc-drift)"; second bullet captures `make test-repl` baseline pass count; closing `### Story tasks` subsection header added before the `Task 1` placeholder so the Pre-edit baseline section doesn't structurally swallow downstream tasks. Verdict criteria 5/5 PASS — (a) `grep 'wc -c' template.md` → 2 matches (lines 21, 22); (b) `grep -E 'Do not inherit'` → 1 match (line 22); (c) `grep -E 'Lesson 13\.5-F'` → 1 match (line 22); (d) `grep -E 'Pre-edit'` → 1 match (line 19); (e) recursive self-validation — Task 1.1 / 1.2 obtained 24,995 / 26,460 by direct `wc -c` invocation, not transcribed from Story 14.2 File List. Zero binary delta. CR fix-pass corrections (post-/CR adversarial review): closure note's AC5(a) count corrected (was "1 match", actual 2); Task 10.1 sprint-status transition claim corrected (was "ready-for-dev → in-progress → review", actual `backlog → review` per git diff — pre-dev-pass row was at `backlog`, not `ready-for-dev`); template structural fixes per M1, M2 above; AC4(b) cite extended to inline Story-13.5.5 reference per M4; temporal signal added to Pre-edit baseline section header per L1. |
| B.3-followup | 🔄 In progress | Architecture-doc one-line correction — `architecture.md:430` canonical Pre-edit baseline example uses `**Do not**` markdown bold, which cannot satisfy the AC5(b)-style regex `Do not inherit|Don.?t inherit`. Story 14.3 dropped the bold from the template literal and flagged this for the next architecture refresh. Tracked as a separate row to prevent the deferral from rotting in story prose only (per S10 / NFR-P3-31 — workflow > memory > prompt). Closure: edit `architecture.md:430` to drop `**` from `**Do not**` so the canonical example matches the regex and any future copy-paste preserves the discipline. |
| B.4 | ✅ Done | Story 14.4 / 2026-05-08 — third `<critical>` block landed in `_bmad/bmm/workflows/4-implementation/create-story/instructions.xml` at lines 62..86, adjacent-after the B.2 block (chronological PD-1 → B.2 → B.4 reading order preserved). Trigger surface enumerates figures (binary-size byte counts, percentages, line:column citations) + tables (verdict-table rows, per-component byte-budget rows) + code blocks (quoted snippets, regex literals, grep patterns); names the trigger as "quote or copy an artefact from a prior story or retrospective". Required action: validate every quoted figure/table/code block against its source-of-truth at draft time before relying on it — re-read the cited file:line directly, re-run the cited command, or re-extract the cited table from the cited document. Cites PD-2 — Story-drafter figure drift; Epic 13 retro #1, `epic-13-retro-2026-05-05.md:230`; related disciplines Lesson 13.5-F (binary-handoff variant) + B.3 enforcer; recursive precedents Story 14.2 AC6(d) + Story 14.3 AC5(b). Verdict criteria 5/5 PASS — (a) `grep -nE '(figure[- ]drift\|PD-2)'` → 2 matches (lines 62, 80); (b) `grep -nE 'figure\|table\|code block'` → 9 matches in block range covering all 3 categories; (c) `grep -nE 'source-of-truth\|draft time'` → 2 matches (lines 71, 79); (d) `grep -nE 'PD-2\|Epic 13 retro #1'` → 2 matches (lines 62, 80); (e) `grep -cE '<critical'` → 19 post-edit (pre-edit 17; **Δ = +2**, not the drafter's expected +1 — same recursive count-metric moment Story 14.2 AC6(d) surfaced; the new block contributes the opening `<critical>` tag at :62 plus the recursive-precedents sub-clause's literal `<critical>` reference at line 85, both matched by the no-closing-bracket-anchor metric — amended in-pass per Story 14.2/14.3 precedent, not HALTed). Recursive self-validation (AC5(f)): all four cited file:line references re-validated by direct re-read at Pre-edit Task 1.8 (B.2 block at :33..60; architecture.md:262/410..422/605/935; epic-13-retro-2026-05-05.md:230; PHASE-3-CARRY-FORWARD.md:104) — all VERIFIED unchanged. Two task-spec divergences caught and resolved in-pass: (1) AC1/Task 2.3 claimed 4-space `<critical>` open indent, actual is 2-space — in-tree precedent wins; (2) AC1/Task 2.5 claimed `</critical>` on own line, actual B.2 close is inline with last content character — in-tree precedent wins. Zero binary delta. S9 exempt + documented. **/CR fix-pass corrections (post-/CR adversarial review)**: (H1+H2) sprint-status transition claim corrected from false "canonical three-state `ready-for-dev → in-progress → review` executed; differs from Story 14.3" to actual `backlog → review` single-step (verified by `git show 64e91bf:sprint-status.yaml`) — exactly the figure-drift class B.4 codifies, surfaced in B.4's own dev-pass; sprint-status.yaml filed as candidate enrichment to AC5(f) pre-flight re-read list for future B-cluster stories. (M1) B.4-followup row added below tracking architecture canonical example block-close-style drift. (L1) AC5(e) Completion Notes mis-attribution fixed (line 85 reference is in the recursive-precedents sub-clause within the AC4 closure parenthetical at :84..86, not the AC4 cite proper at :80..81). |
| B.4-followup | 🔄 In progress | Architecture-doc canonical example block-close-style correction — `architecture.md:421` canonical `<critical>` block format example closes `</critical>` on its own line, but the in-tree precedents (B.2 block at `instructions.xml:60`, PD-1 block at `:31`, B.4 block at `:86`) all close `</critical>` inline with the last content character. Story 14.4's recursive self-validation log §(ii.b) flagged this internal architecture-vs-precedent inconsistency at dev-pass time. Tracked as a separate row to prevent the deferral from rotting in story prose only (per S10 / NFR-P3-31 — workflow > memory > prompt; same shape as B.3-followup row at :105). Closure: edit `architecture.md:421` to inline `</critical>` with the last content character so the canonical example matches the in-tree precedents and any future copy-paste preserves the discipline. |
| B.5 | ✅ Done | Story 14.5 / 2026-05-08 — new tool subtree at `tools/check-doc-sync/` (`check-doc-sync.sh` Bash drift-checker + `README.md` documentation) following the `tools/bdos_probe/` precedent; new `check-doc-sync` PHONY target in `Makefile` (`.PHONY:` extension at line 44 + advisory-only target body at lines 64..69, grouped after the `firmware-repro-test` tool-invoking target precedent). Four drift checks per architecture §"B.5-D1" — (a) FR-P3-N / NFR-P3-N label parity (strict, bidirectional, with inclusive-range expansion for citations like `FR-P3-22..25`), (b) Story X.Y[.Z[.W…]] citation resolution (strict, against the three epics files; one-or-more trailing dot-separated components), (c) §N.M[.K[…]] reference parity vs `docs/ans-forth-core-compliance.md` (advisory pre-A.1 / Story 15.1; `STRICT_SECTIONS` flag flips post-A.1), (d) section-name parity (advisory; empty allowlist by design). Final post-/CR verdict: **(c) advisory-only** — `[advisory] doc-sync: 18 advisory item(s); 0 drift`, exit 0; 3 `[advisory-§]` items (§6.1.0090 / §6.1.1561 / §6.2.1342 cited in architecture but missing from compliance doc — expected pre-A.1, will close with Story 15.1) + 15 `[advisory-section]` items (PRD has 9 level-2 sections + architecture has 6 disjoint level-2 sections — expected by design per AC3(d), the docs serve different roles). Two-run determinism check: stdout + stderr identical across consecutive invocations. Verdict criteria 9/9 PASS. Zero binary delta. S9 exempt + documented. In-pass script refinements (B.4 figure-drift discipline applied to AC3(a) implementation detail): (1) PRD-side label regex extended from architecture's pinned `\*\*(FR\|NFR)-P3-[0-9]+\*\*` to a bullet-anchored `^- \*\*(FR\|NFR)-P3-[0-9]+` form to match PRD's actual definition shapes (`- **FR-P3-N:**` and `- **NFR-P3-N (carries ...):**`); (2) range-expansion added to architecture-side label extraction so range citations like `FR-P3-22..25` and `NFR-P3-1..33` count each label as referenced (without expansion the first run produced 35 false-positive drift items, all of which resolved cleanly once ranges were honoured). **/CR fix-pass corrections (post-/CR adversarial review):** (H1) Story-X.Y citation regex was `[0-9]+\.[0-9]+(\.[0-9]+)?` — capped at 3 components, silently truncating `Story 11.5.1.2` cited at `architecture.md:598` to `Story 11.5.1` (which does have an epics header) and producing a false-clean verdict; widened to `[0-9]+(\.[0-9]+)+`, the genuine `[story-cite]` strict drift surfaced, reconciled in-pass per AC6(b) by editing `architecture.md:598` `Story 11.5.1.2` → `Story 11.5.1` (the parent crash-audit story whose deliverable the bdos_probe tool subtree was). (M1) §-ref regex same 3-component cap silently truncated `§3.1.4.1` / `§3.4.1.3` — widened to `§[0-9]+(\.[0-9]+)+` and README updated. (M2) Sprint-status transition claim corrected from false `ready-for-dev → review` to actual `backlog → review` (verified by `git show 7172754:sprint-status.yaml`) — exactly the figure-drift class B.4 codifies, surfaced in B.5's own dev-pass, same shape as Story 14.4 H1+H2. (M3) AC3(c) "compliance-related sections" qualifier honestly documented as vacuous on current corpus (whole-document scan; no non-compliance §-refs exist). (M4) Script refactored to read each input file exactly once into `*_LINES` cache variables (token extraction + per-orphan loc lookups now operate on cached content, not reopening files). (L1) Makefile target body line range corrected from `:65..69` to `:64..69` (4-line comment block + rule + recipe). |
| A.1 | ✅ Done | Story 15.1 / 2026-05-09 — §-by-§ A.1 audit walk completed against DPANS94 §6.1 (133 rules) + §6.2 (46 rules) + 7 structural §-rules (§3.1.4.1 / §3.2.6 / §3.3.3.1 / §3.3.3.4 / §3.3.3.6 / §3.4.1.3 / §9). Every row in `docs/ans-forth-core-compliance.md` carries DPANS94 §-number, rule text, closed-set verdict, source `file:line`, closure story, notes per CCD-P3-1's 6-column schema (architecture `:175..188`). **Verdict summary:** §6.1 — 133/133 implemented (3 implemented-with-caveat: §6.1.0030 # asm sigil dispatch / §6.1.0190 ." TD-5 closed / §6.1.0570 >NUMBER 8-bit u1 truncation; all accepted-with-rationale, none new defects). §6.2 — 13/46 implemented from DPANS94 1994 baseline + 1 Forth-2014 bonus (HOLDS) = 14 §6.2-shaped entries (2 of which are implemented-with-caveat: §6.2.2148 RESTORE-INPUT / §6.2.2182 SAVE-INPUT, both cross-REFILL impl-defined per §6.2.2148 ambiguous condition, accepted-with-rationale per Story 13.5.5 closure); 33 deliberately-omitted rows added per CCD-P3-1's "no silent gaps" rule with one-sentence rationale each (audit count rationalisation: v2.0 doc claimed "14 of 46" but counted Forth-2014 HOLDS inside the DPANS94 1994 46-word baseline — corrected to 13 of 46 + 1 Forth-2014 bonus). **Total implemented-with-caveat: 5** (3 §6.1 + 2 §6.2; all accepted-with-rationale). Structural §-rules — all 7 rowed; F1 worked example at §3.3.3.1 (alignment satisfied behaviourally — Z80 byte-addressable, no alignment assumption); F7 dual-row pattern at §3.1.4.1 + §6.1.0350 / §6.1.0310 (word + structural rule both rowed, linked via Notes). **Back-fills spawned: 0** (architecture `:227` envelope was 0..2 expected; clean walk surfaced no §-level structural gaps requiring kernel surgery). **Citation cross-check (F6, ≤20 cap):** 4 in-pass CCD-3 fixes — `src/strings.asm:339` §6.1.0567 → §6.1.0570; `src/strings.asm:812` §6.2.0190 → §6.1.0190; `src/outer_interpreter.asm:605` §6.1.2275 → §9.6.1.2275; `src/system.asm:467` ENVIRONMENT? CORE-EXT count `12/46` → `13/46 + 1 Forth-2014 bonus HOLDS`. **Pre-known [advisory-§] items closed:** §6.1.0090 / §6.1.1561 / §6.2.1342 all rowed (post-edit `tools/check-doc-sync/check-doc-sync.sh` should clear all three [advisory-§] items; the 15 [advisory-section] items remain advisory-by-design per AC3(d) of Story 14.5). Zero binary delta — doc-only audit. S9 exempt + documented. |
| A.2 | Direct-commit work item (no story file) | Per Epic 14 retro 2026-05-09 Action Item A1 + A6 — caught-form coverage gap for asm-error THROW codes -258..-272 (15 codes; the block extended from -258..-269 to -258..-272 post-Story-11.5.6) is small enough to land as a direct-commit test extension to `tests/throw_migration_tests.fth` rather than as a story-shaped work item. Closes when the next CR cadence touches the throw-test harness; no formal sprint-status flip. |
| A.3 | Direct-commit work item (no story file) | Per Epic 14 retro 2026-05-09 Action Item A1 + A6 — unprefixed `NUMBER?` base-specialization is small (+0..+30 bytes per architecture `:355`) and constrained to `w_NUMBER_Q_cf` in `src/strings.asm`. Lands as a direct-commit when the next number-parsing-touching change occurs; no formal sprint-status flip. |
| B.6 | ❌ Deferred indefinitely (Epic 14 retro 2026-05-09) | Per Epic 14 retro Lesson 14-F (ceremony has diminishing returns) — `make check-tools` was solo-dev tooling-on-tooling cargo. The actual contributor base is one (the project lead). Mechanical-sweep contributing dev-checks the toolchain manually beats codifying-discipline as a Make target with a sidecar `.tool-versions` file. Trigger to revisit: a second sustained contributor materialises with a different toolchain. |
| B.8 | ❌ Deferred indefinitely (Epic 14 retro 2026-05-09) | Per Epic 14 retro Lesson 14-F — Makefile test-numbering hygiene (cosmetic renumber to fix Story-11.3 duplicate IDs) was solo-dev tooling-on-tooling cargo. The duplicates are harmless (`make test-repl` reports each test independently). Trigger to revisit: a sustained reader expresses confusion about a duplicate test number, OR the hygiene becomes mechanically forced by a future epic's test-add story. |
| (all other P1 + P2 items default to open) | | |
| (P3 items deferred until trigger fires — see C/D trigger note above) | | |
