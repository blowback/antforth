# Story 13.5.6: Epic 13.5 close-out gate — verdict table for PD-1 + TD-1..TD-7; on-device round-trip re-run; **antforth 2.0 tag applied**

Status: done

<!--
This is the seventh and final story in Epic 13.5 (Phase-2 Cleanup
Slate). It mirrors Story 13.6's close-out gate shape (per `epics.md:1834`:
"Mirror of Story 13.6 (Epic-13 close-out gate)"), but the close-out
walk runs over the Tag-Blocking Slate from the Epic 13 retro
(`epic-13-retro-2026-05-05.md:201..211`) — PD-1 + TD-1..TD-7 — and the
v2.0.0 tag is **applied at this story's close** rather than deferred
to a later gate. Story 13.6 already produced the Epic-13 + Phase-2
release-gate verdict (24,694 bytes / 952 PASS / hardware run-3 PASS
2026-05-04) and named the v2.0.0 tag deferral; the Epic 13 retrospective
2026-05-05 then reclassified TD-5 + TD-6 upward to tag-blocking and
spawned Epic 13.5 to discharge the full slate before the tag lands.

Origin lineage:
  Project-lead direction at Epic 13 retro 2026-05-05: "v2.0.0 tag
  DEFERRED post-retro pending Epic 13.5 cleanup slate (project-lead
  direction 2026-05-05)" (`epic-13-retro-2026-05-05.md:265,276`).
  Mirror-of-13.6 framing per `epics.md:1834`.
  Tag-Blocking Slate canonical map per `epic-13-retro-2026-05-05.md:201..211`:
    13.5.0 → PD-1 (workflow + create-story-AC alignment)
    13.5.1 → TD-1 + TD-2 + TD-4 (R/W per-FCB dirty-flag)
    13.5.2 → TD-3 (READ-FILE EOF/error disambiguation)
    13.5.3 → TD-5 (`."` BC-clobber)
    13.5.4 → TD-6 (PAD / HERE-cross-line correctness)
    13.5.5 → TD-7 (user-facing SAVE-INPUT / RESTORE-INPUT for EVALUATE)
    13.5.6 → close-out gate + v2.0.0 tag (this story)

Severity: AUDIT-ONLY in code shape. Zero new mechanism, zero new
EQUs, zero new dictionary words. Expected binary delta = 0 for
`src/*.asm`. Like Stories 9.6 / 10.10 / 11.8 / 11.5.7 / 12.6 / 13.6,
the deliverable is *measurement* artefacts (verdict table, cumulative
ROM accounting, hardware re-run transcript) plus the v2.0.0 tag
proposal recorded for project-lead application.

Standing commitments (S1..S10) from Epic 13 retro apply to this story
as they did to 13.5.0..13.5.5:
  S1 — adversarial review runs separately via `CR` command in fresh
       context; this story's ACs do NOT enumerate "trigger an
       adversarial review pass" (PD-1 closed the in-pass anti-pattern
       structurally 2026-05-05).
  S5 — HALT on PARTIAL ship attempts. No "ship 4/16 ACs + spawn
       13.5.6.1" pattern.
  S8 — "pre-existing" / "out-of-scope" cannot discharge correctness
       defects.
  S9 — mid-epic hardware-smoke cadence is mandatory (this story's
       binary delta is expected zero, but the on-device round-trip
       and 12-line FS smoke ARE the binding hardware evidence for the
       v2.0.0 tag — S9 applies in full).
  S10 — workflow > memory > prompt; structural fixes live in workflow
        files + templates.

Hardware smoke is MANDATORY per S9 + per the Phase-2 release-gate bar
inherited from Story 13.6: the 12-line FS smoke + Journey 1 on-device
round-trip are the binding hardware evidence for the v2.0.0 tag. The
hardware run is the *new* evidence dimension for this story versus a
plain audit-only close-out — the cleanup slate added +308 production
bytes and 21 new REPL tests (944..964), so the post-cleanup binary
must be re-validated on real CP/M 2.2 / MicroBeast hardware before
the tag can be applied.

v2.0.0 tag application: at THIS STORY'S CLOSE, AND ONLY AT THIS
STORY'S CLOSE, per `epics.md:1713,1836` and project-lead direction
2026-05-05. The dev agent records the tag-proposal command line; the
project lead applies the tag.

Validation is optional. Run validate-create-story for quality check
before dev-story.
-->

## Story

As an antforth maintainer,
I want Epic 13.5 (the Phase-2 Cleanup Slate) to close with a verdict-table walk over PD-1 + TD-1..TD-7 confirming each tag-blocker is structurally discharged, an Epic-13.5 cumulative ROM-delta accounting against the post-Story-13.6 baseline (24,694 bytes), a `make test-repl` regression pass on the post-Story-13.5.5 baseline (973 PASS / 0 FAIL with the 21 new tests 944..964 included), a real-MicroBeast-hardware re-run of the on-device round-trip from PRD Journey 1 plus the 12-line FS smoke batch on the post-cleanup binary, an Epic-13.5 milestone marker, the standing-commitment audit (S1, S5, S8, S9, S10) confirming Epic 13.5 closed without backsliding, and a `git tag -a v2.0.0` proposal command line recorded for project-lead application,
so that every tag-blocker named in the Epic 13 retro's Tag-Blocking Slate is verified discharged by direct measurement, the on-device edit/test/persist loop is re-demonstrated working on real hardware after the cleanup slate, and **antforth 2.0 can be tagged** — completing the Phase-2 process-recovery vehicle and delivering the public 2.0 release that was deferred from Story 13.6.

This is the **Epic 13.5 close-out gate** AND the **v2.0.0 tag-application gate**. It mirrors Story 13.6's close-out gate shape (per `epics.md:1834`) but runs the verdict-table walk over the Tag-Blocking Slate (PD-1 + TD-1..TD-7) instead of Epic 13's NFR envelope, and applies the v2.0.0 tag at story close rather than queuing it as deferred. It is **audit-only** in code shape — same style as Stories 9.6 / 10.10 / 11.8 / 11.5.7 / 12.6 / 13.6: no new mechanism, no new EQUs, no new dictionary words. Modulo: the tag proposal is recorded; the project lead applies the actual tag.

---

## Severity / Phase Re-Statement (BINDING — context for every dev-pass decision)

`antforth 2.0` is the **public release tag** at the end of Phase 2. Story 13.6 produced the Phase-2 release-gate verdict 2026-05-04 with 24,694 bytes / 952 PASS / hardware run-3 PASS but explicitly deferred the tag pending the Epic 13.5 cleanup slate (per `13-6-…md` Task 13.2 + `epic-13-retro-2026-05-05.md:265,276`). Epic 13.5 has discharged all seven tag-blockers (PD-1 + TD-1..TD-7) across Stories 13.5.0..13.5.5. Story 13.5.6 is the verdict-table walk that confirms the slate closed cleanly and triggers the tag application.

| Dimension | Story 13.6 (Phase-2 release gate) | Story 13.5.6 (this story) |
|---|---|---|
| **Scope of audit** | Epic 13 NFR envelope (NFR4 / NFR8 / NFR9 / NFR13 / NFR17) | Tag-Blocking Slate (PD-1 + TD-1..TD-7) verdict walk |
| **Cumulative ROM** | Phase-2 cumulative (+10,664 from post-Epic-8) | Phase-2 + cleanup-slate cumulative (+10,972 expected) |
| **Test baseline** | 947 → 952 PASS (+5 closure tests 939..943) | 952 → 973 PASS (+21 cleanup-slate tests 944..964) |
| **Hardware smoke** | Journey 1 + 12-line FS smoke run-3 on 24,694-byte binary | **Re-run** Journey 1 + 12-line FS smoke on 25,002-byte binary |
| **Tag** | v2.0.0 deferred per project-lead direction | **v2.0.0 tag applied** (proposal recorded; project-lead action) |
| **Standing-commitment audit** | S1..S10 framework introduced at retro 2026-05-05 | S1..S10 audit closing the Epic 13.5 process-recovery vehicle |

The story is **audit-only in code shape**. The expected binary delta is 0 bytes for `src/*.asm` — confirm via `wc -c build/antforth.com` pre and post (both = 25,002 per Story 13.5.5 close). The new evidence dimensions versus Story 13.6 are: (i) the Tag-Blocking Slate verdict-table walk (one row per PD-1 + TD-1..TD-7 with grep/wc-verifiable evidence), (ii) the cleanup-slate cumulative ROM accounting (+308 bytes / +21 tests), (iii) the standing-commitment audit (S1..S10 codified at retro 2026-05-05; this story confirms each held across the slate), (iv) the v2.0.0 tag application gate.

If any verdict row fails (a TD didn't actually close cleanly; PD-1's structural fix has regressed; the post-cleanup binary fails hardware smoke; a regression surfaces in `make test-repl`), Story 13.5.6 **HALTs** per AC #15 and the project lead decides whether to spawn a fix-story (NOT a sibling-story-spawn anti-pattern — this is release-gate-discovered, mirroring Story 13.6's HALT framing).

---

## Acceptance Criteria

1. **Given** the Tag-Blocking Slate canonical map at `_bmad-output/implementation-artifacts/epic-13-retro-2026-05-05.md:201..211` (PD-1 + TD-1 + TD-2 + TD-3 + TD-4 + TD-5 + TD-6 + TD-7 mapped to Stories 13.5.0..13.5.5),
   **when** the dev-pass runs the verdict-table walk,
   **then** Completion Notes Task 1 contains an 8-row **Tag-Blocking Slate Verdict Table** with columns `Slate item | Story | Discharge mechanism | Evidence (grep / wc / file:line) | Verdict (PASS / FAIL / HALT)`. Each row carries direct mechanical evidence — not "the story closed clean" assertions — verifiable by re-running the cited command. The eight rows cover:

   - **(a) PD-1 (13.5.0)** — workflow + create-story-AC alignment. Evidence: `ls _bmad/bmm/workflows/bmad-quick-flow/quick-dev/steps/step-05-adversarial-review.md` returns "No such file" (file removed); `grep nextStepFile _bmad/bmm/workflows/bmad-quick-flow/quick-dev/steps/step-04-self-check.md` returns `nextStepFile: './step-06-resolve-findings.md'`; `grep "PD-1.*Story 13.5.0" ~/.claude/projects/-home-ant-src-microbeast-antforth/memory/feedback_adversarial_review.md` returns ≥1 match; **the binding PD-1 grep is** `grep -rn "trigger an adversarial review\|adversarial review pass" _bmad-output/implementation-artifacts/13.5-{1,2,3,4,5}-*.md | grep -vE "do NOT enumerate\|structurally cannot contain\|story's ACs structurally"` returns **zero matches** (the structural enforcer held across all five subsequent stories — this is the core PD-1 success criterion). The literal pattern alone returns 6 matches, ALL in negation-context boilerplate ("this story does NOT enumerate 'trigger an adversarial review pass'") that documents PD-1's enforcement; the refined grep filters these enforcer-citation sentences and is the binding measure of structural-enforcer integrity. `grep "after each story-close" _bmad/bmm/agents/dev.md` returns ≥1 match. Cross-reference Story 13.5.0 Completion Notes Task 8's verdict-criteria list (a..e); each criterion is mechanically re-verified. Verdict cell: PASS if all five sub-greps land; FAIL otherwise.
   - **(b) TD-1 (13.5.1)** — R/W mid-read FILE-POSITION accuracy via per-FCB dirty-flag. Evidence: `grep -nE "fcb_dirty\b" src/structures.asm src/file_access.asm` returns ≥1 match each (the parallel-array EQU + the set/clear/gate sites); REPL test 944 (or whatever 13.5.1 assigned for the FILE-POSITION accuracy reproducer) PASS in `make test-repl`. Cross-reference Story 13.5.1's File List + Change Log for the canonical citation. Verdict: PASS if grep matches + test passes.
   - **(c) TD-2 (13.5.1)** — R/W mixed-mode discipline (gating `file_flush` such that `REPOSITION-FILE` between READ and WRITE on the same R/W FID auto-flushes safely). Evidence: REPL test 945 (or 13.5.1's TD-2-specific row) PASS; the auto-flush path in `file_flush` checks `fcb_dirty` (cite the line). Verdict: PASS if test passes + code path verified.
   - **(d) TD-3 (13.5.2)** — READ-FILE EOF/error disambiguation (helper-layer rewrite). Evidence: `grep -nE "READ-FILE\|file_byte_read" src/file_access.asm` plus the disambiguation comment block landed by 13.5.2; REPL tests 949..951 PASS (per Story 13.5.2 close); ANS §11.6.1.2080 distinct EOF vs error semantics confirmed in code. Verdict: PASS if helper rewrite landed + tests pass.
   - **(e) TD-4 (13.5.1)** — W/O probe coverage (round out the FCB pool stress matrix). Evidence: REPL test 948 (or 13.5.1's W/O probe row) PASS; the W/O probe lives in `tests/file_access_tests.fth` Section 11 (or wherever 13.5.1 sited it). Verdict: PASS if probe test passes.
   - **(f) TD-5 (13.5.3)** — `."` BC-clobber fix. Evidence: REPL tests 952..955 (or whatever 13.5.3 assigned) PASS; `grep -nE "PUSH\s+BC\b\|EXX\b" src/strings.asm` (or wherever 13.5.3 landed the BC-preservation wrap) shows the structural fix. Verdict: PASS if BC-preservation in place + tests pass.
   - **(g) TD-6 (13.5.4)** — PAD / HERE-cross-line correctness. Evidence: a real `PAD` user-facing word lands at `HERE+84` per Story 13.5.4 Change Log (commit 6208a81); `grep -n '"PAD"' src/*.asm` returns the new DEFCODE site; REPL tests 956..959 PASS; `docs/ans-forth-core-compliance.md` Core Extension Bonus Coverage section reflects PAD added by 13.5.4. Verdict: PASS if PAD landed + tests pass + compliance doc updated.
   - **(h) TD-7 (13.5.5)** — user-facing `SAVE-INPUT` / `RESTORE-INPUT` for EVALUATE. Evidence: `grep -n '"SAVE-INPUT"\|"RESTORE-INPUT"' src/outer_interpreter.asm` returns ≥2 matches (one per user-facing word); REPL tests 960..964 PASS; `docs/ans-forth-core-compliance.md:354` shows "**14** of 46 DPANS94 §6.2 Core Extension words are implemented" (was "12 of 46" pre-13.5.4 / pre-13.5.5; PAD = 13.5.4, SAVE-INPUT + RESTORE-INPUT = 13.5.5). Verdict: PASS if both new words present + tests pass + compliance doc updated.

   **Verdict-table aggregation:** all 8 rows must verdict **PASS** for the Epic 13.5 slate to be considered discharged. Any row at **FAIL** triggers AC #15 HALT discipline; **HALT** verdict (a row that cannot be cleanly evaluated due to evidence-gathering surface area) likewise escalates to project lead. **No "accepted-with-rationale" verdict on this gate** — per S8 standing commitment, every slate item must close cleanly or HALT. Recorded in Completion Notes Task 1.

2. **Given** the Epic 13.5 per-story ROM trajectory measured against the post-Story-13.6 (= post-Phase-2-dev-close) baseline of **24,694 bytes** (production) / **26,010 bytes** (filesanity) per `_bmad-output/implementation-artifacts/13-6-…md` Task 1.1 / Task 1.2 + the per-story trajectory implicit in Stories 13.5.0..13.5.5 Change Logs,
   **when** `wc -c build/antforth.com` and `wc -c build/antforth_filesanity.com` are measured at story-drafting time AND at story-close,
   **then** the per-story and Epic-13.5-cumulative deltas are recorded in Completion Notes Task 2 with the per-story Completion-Notes line citation per `feedback_systematic_reference_check.md` (do not enumerate the trajectory from memory):

   - **13.5.0 (PD-1)**: pre 24,694 / post **24,694** (Δ = 0; doc + workflow + memory + agent-definition only) per `13.5-0-…md` Task 9.
   - **13.5.1 (TD-1+TD-2+TD-4)**: pre 24,694 / post **24,804** (Δ = +110; data +8 + code +102) per `13.5-1-…md` Change Log.
   - **13.5.2 (TD-3)**: pre 24,804 / post **24,834** (Δ = +30 net of CR -9 byte fix) per `13.5-2-…md` Change Log.
   - **13.5.3 (TD-5)**: pre 24,834 / post **24,836** (Δ = +2; data 0 + code +2) per `13.5-3-…md` Change Log.
   - **13.5.4 (TD-6)**: pre 24,836 / post **24,862** (Δ = +26) per `13.5-4-…md` Change Log.
   - **13.5.5 (TD-7)**: pre 24,862 / post **25,002** (Δ = +140; project-lead-accepted out-of-envelope by +40 per Z80 structural minimums; code-review compaction landed -6 bytes against the original +146 dev-pass figure) per `13.5-5-…md` Task 7.
   - **13.5.6 (this story)**: pre **25,002** / post **25,002** (Δ = **0**; audit-only). Any non-zero binary delta on this story HALTs per AC #15 — there is no `src/` implementation in scope.

   **Epic-13.5 cumulative (production binary):** 24,694 → 25,002 = **+308 bytes** (+1.25%). Per-story sum check: 0 + 110 + 30 + 2 + 26 + 140 + 0 = **308** — exact match, zero residual. Mirror Story 13.6 Task 5.2 reconciliation discipline. Filesanity cumulative: 26,010 → **26,467** = +457 bytes (per `13.5-5-…md:254`); reconciliation against per-story sums verified at story-close. The +457 filesanity vs +308 production gap is normal — filesanity-only scaffolding (FILE-IO-SANITY harness + extra probes) was already +1,316 bytes over production at the post-Story-13.6 baseline (26,010 − 24,694 = +1,316); per-story filesanity deltas may include sanity-probe additions that don't move production.

   **Phase-2 cumulative (post-Epic-8 → post-Story-13.5.6):** the pre-Phase-2 baseline is **14,030 bytes** per `9-6-…md:19,196` (commit `27c4cbd`, post-Epic-8 size, established by Story 9.6). Phase-2 + cleanup-slate cumulative: 14,030 → 25,002 = **+10,972 bytes (+78.2%)**. Per-epic sum: +757 (Epic 9) + +1,985 (Epic 10) + +653 (Epic 11) + +116 (Epic 11.5) + +689 (Epic 12) + +6,464 (Epic 13) + +308 (Epic 13.5) = **+10,972** — exact match against absolute, zero residual. Cite each per-epic delta against its CCD-4 close-out story per `feedback_systematic_reference_check.md`.

   **Justification framing recorded:** Epic 13.5 is **debt discharge, not feature work** (per `epics.md:1711` byte-budget gate). +308 bytes for: per-FCB dirty-flag (TD-1+2+4, +110), helper-layer rewrite for EOF/error disambiguation (TD-3, +30), `."` BC-preservation (TD-5, +2), real PAD word (TD-6, +26), user-facing SAVE-INPUT / RESTORE-INPUT (TD-7, +140). Every byte maps to a tag-blocker discharge per S8 standing commitment. Recorded in Completion Notes Task 2.

3. **Given** the post-Story-13.5.5 `make test-repl` baseline of **973 PASS / 0 FAIL** (per `13.5-5-…md:722`) and the 21 new cleanup-slate tests at IDs 944..964 (5 from 13.5.1 + 3 from 13.5.2 + 4 from 13.5.3 + 4 from 13.5.4 + 5 from 13.5.5),
   **when** `make test-repl` is run against the post-Story-13.5.5 binary,
   **then** the result is **973 PASS / 0 FAIL** — zero regression on the 1..952 baseline; all 21 new cleanup-slate tests still PASS. Pre-edit verification: `grep -c '^PASS:' <(make test-repl 2>&1)` returns 973; pre-edit FAIL count = 0. Post-edit (story-close): same — this story is audit-only, no test-count movement expected. Any movement HALTs per AC #15 (audit-only stories cannot move test counts; movement indicates an unintended side-effect). Recorded in Completion Notes Task 3 alongside `make test` (assembly thread) clean and `make test-file-sanity` PASS.

4. **Given** the **on-device "define / save-source / INCLUDE-back" round-trip** from PRD User Journey 1 (Mo's on-device session — `prd.md:172-180`) re-run on the post-cleanup binary (25,002 bytes), and the established hardware-transfer mechanism (post-9.6 / 10.10 / 11.8 / 11.5.7 / 12.6 / 13.6 precedent),
   **when** the user (project lead) operates on real MicroBeast hardware:
   - (i) defines a small word at the REPL — e.g., `: BLINK 0xF0 @ 1 XOR 0xF0 ! ;` (or the project lead's bench-equivalent variant)
   - (ii) saves the definition's source to a file on B: via `S" BLINK.FTH" R/W CREATE-FILE DROP` then writes the source bytes via `WRITE-FILE` and `CLOSE-FILE`
   - (iii) reboots the MicroBeast (or runs `BYE` and re-loads the .COM)
   - (iv) `INCLUDE B:BLINK.FTH` — the definition is restored
   - (v) `BLINK BLINK BLINK` — the LED blinks three times (or the bench-side equivalent),

   **then** the round-trip completes — PRD Journey 1 success criterion **re-verified** post-cleanup-slate. Capture verbatim console output of all five steps in Completion Notes Task 4 (mirror Story 13.6 Task 9 capture discipline; transcript to `~/Downloads/bestialitty-13-5-6-YYYYMMDD-HHMMSS.bin` or equivalent). This gate is **MANDATORY** for v2.0.0 tag application — without a re-verified hardware round-trip on the post-cleanup binary, the tag does not land. If the round-trip fails on hardware (e.g., a cleanup-slate change perturbs an intermediate step that was clean on the pre-cleanup 24,694-byte binary), HALT and flag for project lead per AC #15.

5. **Given** the **standard 12-line FS smoke batch** from Story 13.6 Task 10 (verified PASS run-3 2026-05-04 on the 24,694-byte binary at `~/Downloads/bestialitty-20260504-222930.bin`) and the post-cleanup binary (25,002 bytes),
   **when** the project lead exercises the smoke batch on real MicroBeast hardware (in addition to AC #4's round-trip),
   **then** every smoke line passes on the post-cleanup binary. The 12 lines are inherited verbatim from Story 13.6 Task 10 (which has the canonical text) — the dev pass cites that batch by reference rather than re-stating each line. Particular invariants to re-verify post-cleanup:
   - **R/O destructive-flush invariant** (Story 13.5 Task 10 line 11 — file size remains 2 bytes after the partial read, not extended by 128+ bytes; verifies the post-cleanup binary preserves the Story 13.5 mode-aware `file_flush` + `fcb_has_written` discipline).
   - **i*x preservation across Epic-13 + Epic-13.5 surface** (Story 13.6 Task 10 line 12 — `1 2 3 ' ABORT CATCH . . . .` returns `-1 3 2 1`; verifies the cleanup-slate words (PAD, SAVE-INPUT, RESTORE-INPUT, dirty-flag set/clear sites, BC-preservation in `."`) do not perturb the i*x register contract from Story 11.4.1).
   - **TD-5 BC-clobber fix on hardware** (post-13.5.3): `1 2 ." hello"` followed by `.S` shows the data stack still has `1 2` (no clobber). New explicit smoke line — recommend appending as line 13.
   - **TD-6 PAD on hardware** (post-13.5.4): `S" hi" PAD SWAP CMOVE PAD 2 TYPE` outputs `hi`. New explicit smoke line — recommend appending as line 14.
   - **TD-7 SAVE-INPUT / RESTORE-INPUT on hardware** (post-13.5.5): `: T1355 SAVE-INPUT RESTORE-INPUT . ; S" T1355" EVALUATE` outputs `0  ok`. Mirror Story 13.5.5 Task 9 (p2) probe.

   Recorded **verbatim** in Completion Notes Task 5. The 12-line batch becomes a 14- or 15-line batch with the new TD-5 + TD-6 + TD-7 hardware spot-checks appended (the dev pass picks the exact line count and order). This gate is **MANDATORY** for v2.0.0 tag application. Hardware transcript captured to `~/Downloads/bestialitty-13-5-6-YYYYMMDD-HHMMSS.bin` (per AC #4) covers both the round-trip and the smoke batch in one session.

6. **Given** the **per-TD post-fix REPL probe pinpoint re-verification** (independent of the aggregated `make test-repl` pass count — proves each cleanup-slate test actually exercises its claimed defect),
   **when** the dev-pass spot-checks each TD's anchor probe,
   **then** the 7 TD-anchor probes (one per TD-1..TD-7; PD-1 has no probe — verified via mechanical greps per AC #1(a)) are individually re-run via `printf … | iz-cpm …` and the outputs match the Story-13.5.x close-out figure verbatim. The probes:
   - **TD-1 anchor (Story 13.5.1)**: the FILE-POSITION mid-read accuracy reproducer. Cite `13.5-1-…md:705` (or wherever 13.5.1 sites the canonical reproducer line).
   - **TD-2 anchor (Story 13.5.1)**: the R/W mixed-mode REPOSITION-FILE auto-flush probe. Cite the canonical line.
   - **TD-3 anchor (Story 13.5.2)**: the EOF-vs-error disambiguation probe (per `13.5-2-…md` Task X).
   - **TD-4 anchor (Story 13.5.1)**: the W/O probe (per `13.5-1-…md` Task X).
   - **TD-5 anchor (Story 13.5.3)**: the `."` BC-preservation reproducer (per `13.5-3-…md` Task X).
   - **TD-6 anchor (Story 13.5.4)**: the PAD-cross-line reproducer (per `13.5-4-…md` Task X).
   - **TD-7 anchor (Story 13.5.5)**: the SAVE-INPUT/RESTORE-INPUT EVALUATE round-trip probe (`: T1355 SAVE-INPUT RESTORE-INPUT . ; S" T1355" EVALUATE` → `0 ok`; per `13.5-5-…md` test 961).

   Each spot-check confirms the cleanup-slate test ACTUALLY EXERCISES the defect rather than passing trivially (e.g., a probe that doesn't reach the fix path because of a typo would PASS the aggregated `make test-repl` count but FAIL this spot-check). Recorded in Completion Notes Task 6 with one-line evidence per probe.

7. **Given** the **standing-commitment audit** for Epic 13.5 (S1 + S5 + S8 + S9 + S10 from Epic 13 retro `_bmad-output/implementation-artifacts/epic-13-retro-2026-05-05.md:184..195`),
   **when** the dev-pass walks the standing commitments against the Epic 13.5 dev-pass record,
   **then** Completion Notes Task 7 records a 5-row table (one row per S1, S5, S8, S9, S10) confirming each held across Stories 13.5.0..13.5.5:
   - **S1 (in-pass adversarial review removed; CR command in fresh context)**: verified by AC #1(a) — `grep -rln "trigger an adversarial review\|adversarial review pass" _bmad-output/implementation-artifacts/13.5-{1,2,3,4,5}-*.md` returns **zero matches**. The structural enforcer (the `<critical>` block in `_bmad/bmm/workflows/4-implementation/create-story/instructions.xml` added by 13.5.0 Task 3) held across all five subsequent stories. **PASS** if zero matches; **FAIL** if any 13.5.x story file contains the pattern (would indicate PD-1 regressed silently).
   - **S5 (HALT on PARTIAL)**: verified by `grep -nE "HALT\b" _bmad-output/implementation-artifacts/13.5-{0,1,2,3,4,5}-*.md` — count the HALT references. Each HALT in the 13.5.x stories should be either an AC HALT condition (anticipated; documented as "no HALT triggered" in Completion Notes) or a project-lead-surface event (e.g., 13.5.5 byte-budget overshoot). **PASS** if no story shipped a PARTIAL fix + spawned a sibling story; **FAIL** if any 13.5.x.1-style sibling story exists (`grep -lE "13\.5-[0-5]-1\b" _bmad-output/implementation-artifacts/`).
   - **S8 (no "pre-existing" / "out-of-scope" discharge for correctness defects)**: verified by `grep -nE "accepted-with-rationale.*pre-existing\|pre-existing.*accepted-with-rationale" _bmad-output/implementation-artifacts/13.5-{1,2,3,4,5}-*.md` — should return zero matches for *correctness defects*. The 13.5.5 byte-budget overshoot uses "accepted-with-rationale" but for a *byte-budget* overshoot, not a correctness defect — that is permitted per the standing commitment. **PASS** if no correctness defect was discharged via "pre-existing"; **FAIL** otherwise.
   - **S9 (mid-epic hardware-smoke cadence mandatory)**: verified by `grep -lE "Hardware smoke.*PASS\|hardware run.*PASS" _bmad-output/implementation-artifacts/13.5-{1,2,3,4,5}-*.md` — each non-PD-1 story has a hardware-smoke task and a PASS verdict. PD-1 (13.5.0) is the documented S9 exemption (zero binary delta — no kernel surface to smoke). **PASS** if 5/5 binary-delta stories ran hardware smoke + PD-1 documented exemption; **FAIL** otherwise.
   - **S10 (workflow > memory > prompt; structural fixes in workflow files)**: verified by Story 13.5.0's structural-edit pattern (file deletion of `step-05-adversarial-review.md` + `<critical>` block in `instructions.xml`) holding through 13.5.1..13.5.5 dev-passes. The PASS criterion is "no 13.5.x story re-introduced an in-pass adversarial review pattern via memory or prompt" — which is the same evidence as S1. **PASS** if S1 PASSes; **FAIL** otherwise.

   Recorded in Completion Notes Task 7. The 5/5 PASS row set is the binding evidence that the Epic 13.5 process-recovery vehicle delivered its intended outcome. If any standing commitment FAILs, HALT per AC #15 — the v2.0.0 tag DOES NOT LAND on a process-recovery slate that itself regressed.

8. **Given** every Epic-13.5-introduced word and code change (per Story 13.5.0..13.5.5 File Lists) and the CCD-3 / NFR17 standards-citation discipline (`architecture.md:206-216`),
   **when** audited,
   **then** every standards-derived addition carries an inline `; ANS Forth 1994 §<x>` (or `; DPANS94 §<x>`) citation. The audit is **discovery, not regeneration** — if a citation is missing or wrong, fix in-place (comment-only edit; zero binary delta — confirm via Task 2 re-`wc -c` post-fix). Specifically:
   - **TD-6 PAD (13.5.4)** — `grep -nE "ANS Forth 1994 §6\.2\.2000\|DPANS94 §6\.2\.2000" src/*.asm` returns ≥1 match (per ANS §6.2.2000 PAD).
   - **TD-7 SAVE-INPUT / RESTORE-INPUT (13.5.5)** — `grep -nE "ANS Forth 1994 §6\.2\.2148\|DPANS94 §6\.2\.2148" src/outer_interpreter.asm` returns ≥1 match (RESTORE-INPUT per DPANS94 §6.2.2148 — note the 13.5.5 dev-pass corrected the spec's swapped citations); `grep -nE "ANS Forth 1994 §6\.2\.2182\|DPANS94 §6\.2\.2182" src/outer_interpreter.asm` returns ≥1 match (SAVE-INPUT per DPANS94 §6.2.2182).
   - **TD-3 helper-layer rewrite (13.5.2)** — the EOF-vs-error code path carries an inline citation to ANS §11.6.1.2080 READ-FILE EOF-vs-error semantics.
   - **All other TD fixes (TD-1, TD-2, TD-4, TD-5)** — the structural fixes are infrastructure (per-FCB dirty-flag / W/O probe / BC-preservation wrap), not new standards-derived words; carry structural-decision comments naming the architectural decision (E13-D1 / E13-D2 / E13-D3) where appropriate. Spot-check per Story 13.5.x File List.
   - **`docs/ans-forth-core-compliance.md`** — verify the leading sentence at line 354 reads "**14** of 46 DPANS94 §6.2 Core Extension words are implemented" (was "12 of 46" pre-13.5.4; PAD added by 13.5.4 took it to 13/46; SAVE-INPUT + RESTORE-INPUT added by 13.5.5 took it to 14/46). Cite the line; correct in-place if drifted.

   The audit yields a citation-coverage table in Completion Notes Task 8: one row per Epic-13.5 addition, columns `Story | Word/Site | Citation | Audit verdict (OK / MISSING / WRONG)`. Any MISSING / WRONG row is fixed in-pass (comment-only) per AC #15 in-pass-fix discipline. Total rows ≥ 8 (one per TD + the compliance doc rows).

9. **Given** the **register-convention preservation audit** (mirror Story 13.6 Task 10 i*x line) and the cleanup-slate words' interaction with the BC-as-TOS convention + i*x preservation contract from Story 11.4.1,
   **when** audited,
   **then** Completion Notes Task 9 records a 5-row table confirming each TD-3..TD-7 word body preserves the BC-as-TOS / i*x conventions documented in `docs/register-conventions.md`:
   - **TD-3 `file_byte_read` rewrite** — verify the helper layer continues to honour the EXX convention from Story 13.1's BDOS wrapper layer (`grep -nE "EXX\b" src/file_access.asm` count unchanged or +N for the helper-rewrite block).
   - **TD-5 `."` fix** — verify BC is preserved across the inner print path (the structural fix shape).
   - **TD-6 PAD** — verify the new word body uses the BC-as-TOS convention per the kernel's existing DEFCODE pattern (e.g., `PUSH BC` to spill old TOS before pushing the PAD address; `LD BC, …` for new TOS).
   - **TD-7 SAVE-INPUT / RESTORE-INPUT** — verify pick (a)'s body uses `PUSH BC` + four `LD L,(IY+UA.*); LD H,(IY+UA.*+1); PUSH HL` sequences for SAVE-INPUT, and the inverse for RESTORE-INPUT, per `13.5-5-…md` Task 3 sketch.
   - **High-on-TOS double-cell convention (Story 13.0.1)** — verify no Epic-13.5 word regressed the high-on-TOS convention. None of the cleanup-slate words push doubles in user-visible form (TD-7's SAVE-INPUT pushes 5 single cells, not a double); spot-check by inspection.

   No new register-convention guidance is added to `docs/register-conventions.md` by this story — Epic 13.5 is debt discharge, not convention introduction. Recorded in Completion Notes Task 9.

10. **Given** the cleanup-slate's documentation surface (`docs/ans-forth-core-compliance.md` updates from 13.5.4 + 13.5.5; any WISHLIST / project-context updates from any 13.5.x story),
    **when** audited,
    **then** Completion Notes Task 10 confirms:
    - `docs/ans-forth-core-compliance.md:354` reads "**14** of 46 DPANS94 §6.2 Core Extension words are implemented (PAD added by Story 13.5.4 — TD-6 closure 2026-05-06; SAVE-INPUT + RESTORE-INPUT added by Story 13.5.5 — TD-7 closure 2026-05-06)" or equivalent dev-pass-finalised wording.
    - Story 13.5.x caveats sub-bullets exist for 13.5.4 (PAD volatility / cross-line semantics) and 13.5.5 (EVALUATE-arm primary scope; cross-REFILL impl-defined deviation).
    - The §6.1 Core "133/133 = 100%" claim from Story 13.6 Task 8 is **unchanged** by Epic 13.5 (the cleanup-slate added CORE-EXT entries, not Core entries; Core compliance stays 100%).
    - `docs/WISHLIST.md` Phase-3 §-by-§ Core re-audit entry from Story 13.6 Task 8.5 is **unchanged** by Epic 13.5 (the cleanup-slate did not close that wishlist item; it stays carry-forward to Phase 3).
    - No documentation drift: any 13.5.x story's File List entry referencing a doc edit is grep-able post-close and consistent.

    Recorded in Completion Notes Task 10.

11. **Given** the byte-budget discipline (per Story 13.4 v2 retrospective: PD-13 envelope under-counted; calibrate from actuals + margin per `feedback_plain_qa_language.md`),
    **when** Story 13.5.6 closes,
    **then** the byte-count delta is reported as TWO numbers (data delta + code delta) per Story 13.4 v2 PD-13 idiom. **Expected envelope: data 0 / code 0 (zero binary delta — this story is audit-only).** Any binary delta (any change to `wc -c build/antforth.com`) HALTs and is investigated per AC #15 — audit-only stories have no `src/` implementation in scope; binary delta indicates a misroute or scope creep. Production binary expected: 25,002 bytes (matches Story 13.5.5 close per `13.5-5-…md:254`). Filesanity binary expected: 26,467 bytes (matches Story 13.5.5 close). Both numbers recorded in Completion Notes Task 11 as "pre / post / Δ" rows.

12. **Given** the **identifier gate** inherited from Stories 13.4 v2 / 13.5 / 13.5.1..13.5.5 (banned substrings: `hack`, `workaround`, `fixme`, standalone `tmp` token; case-insensitive),
    **when** Story 13.5.6's diff is scanned,
    **then** `git diff --no-color | grep -niE '^\+.*\b(hack|workaround|fixme|tmp)\b'` returns **zero matches** across all modified files. Expected diff scope per AC #14: this story file (`13.5-6-…md`); sprint-status.yaml (one row flip + epic-flip); MEMORY.md (any Phase-2-spanning memory-currency tweaks per AC #14); possibly one or two Phase-2 memory files per AC #14 currency check. No `src/`, no `tests/`, no `Makefile` — confirms audit-only scope. Any identifier-gate match is a HIGH finding requiring rework before close. Recorded in Completion Notes Task 12.

13. **Given** the **HALT-on-PARTIAL discipline** per S5 standing commitment + the in-pass-fix discipline established by Stories 11.5.x / 12.x / 13.x / 13.5.x,
    **when** small in-pass refinements surface during this story (citation comment fixes per AC #8, sprint-status sub-row flips per AC #16, memory-currency tweaks per AC #14, cross-reference fixes in `epics.md:1684..1841`),
    **then** they are landed inside this story — no spawning further sub-stories. **The exception**: if any verdict-table row at AC #1 fails to PASS, or any standing-commitment row at AC #7 fails, or the post-cleanup hardware re-run at AC #4 / AC #5 fails, or `make test-repl` regresses below 973 PASS at AC #3, **HALT and flag as a finding for the project lead** before deciding scope — the change becomes a separate decision, not in-pass cleanup.

    The Story 13.4 v1 anti-pattern (sibling-story-spawn + half-done-ship) is explicitly forbidden, re-asserted from Story 13.4 v2 AC #26 + Story 13.5 AC #11 + Stories 13.5.1..13.5.5. The valid options are (a) all goals met, or (b) HALT log + project-lead escalation. **No third option.** Documented in Completion Notes Task 13.

14. **Given** the Phase-2-spanning memory files (`project_phase2_scope.md`, `project_assembler_keep_assembly.md`, `project_asm_hash_dispatch_hack.md`, `project_epic_11_5_scope.md`, `project_epic4_scope.md`, `project_epic5_scope.md`, `project_hardware_crash_audit.md`, `project_tos_in_register.md`, `project_epic12_redraft_required.md`) and the v2.0.0 milestone marker,
    **when** Story 13.5.6 closes,
    **then** Completion Notes Task 14 confirms each Phase-2-spanning memory file is current as of v2.0.0 application. Specifically `project_phase2_scope.md` is updated from "Phase 2 dev-side CLOSED 2026-05-04 at Story 13.6; v2.0.0 tag pending project-lead hardware run" (its post-13.6 state per `13-6-…md:306`) to "Phase 2 + cleanup-slate CLOSED 2026-05-06 at Story 13.5.6; v2.0.0 tag candidate ready for project-lead application" or equivalent dev-pass-finalised wording. The memory describes the post-tag state as it stands at story-close — the actual tag command is project-lead action per AC #15.

    `MEMORY.md` index line for `project_phase2_scope.md` is updated synchronously if the description changes. Other Phase-2-spanning memories are spot-checked: any stale entry is updated; any current entry needs no edit. Recorded in Completion Notes Task 14 with a "before / after" note for any edited memory.

15. **Given** the completion of ACs #1–#14,
    **when** the dev agent composes the Epic-13.5 + v2.0.0-tag closure summary,
    **then** the story's Completion Notes include (a) a "**Tag-Blocking Slate Verdict + v2.0.0 Tag Gate**" table near the top of Completion Notes summarising PASS/FAIL per AC (AC #1 8-row slate verdict; AC #2 ROM cumulative; AC #3 regression gate; AC #4 Journey-1 round-trip; AC #5 12-line FS smoke; AC #6 per-TD probe spot-check; AC #7 standing-commitment audit; AC #8 citation audit; AC #9 register convention; AC #10 doc currency; AC #11 byte budget); (b) the **antforth 2.0 release-readiness one-liner** ("READY to tag — all gates PASS" or "BLOCKED — the following gate(s) fail: …"); (c) a git tag proposal line — recommended exact form: `git tag -a v2.0.0 -m "Phase 2 + cleanup slate: ANS Core 100% + CATCH/THROW + Search-Order + File-Access + on-device source development + Tag-Blocking Slate cleanup"` (the dev agent may refine the message; the project lead may rephrase before applying); (d) a milestone marker noting that Epic 13.5 closes the Phase-2 process-recovery vehicle entirely; the next phase (post-2.0) starts with the MicroBeast hardware vocabulary and beginner's guide per `prd.md:142-147`. **No tag is applied by the dev agent** — tagging is the project lead's action, mirroring Stories 9.6 / 10.10 / 11.8 / 11.5.7 / 12.6 / 13.6 disposition. Recorded in Completion Notes Task 15.

16. **Given** sprint-status flip ordering (mirror Story 13.6 AC #15 / Story 11.8 Task 11.3 / Story 12.6 AC #13),
    **when** Story 13.5.6 lands,
    **then** the row `13.5-6-epic-13-5-close-out-gate-and-antforth-2-0-tag` flips through `backlog` (pre-create-story) → `ready-for-dev` (this story's creation, by the create-story workflow's Step 6) → `in-progress` (dev-pass start) → `review` (dev-pass close) → `done` (code-review close); **and** the row `epic-13.5: in-progress → done` flips at the **story-`done` step** (not at `review`). The `epic-13.5-retrospective: optional` row is not gated on Story 13.5.6's completion; the project lead may run a Phase-2-cleanup retrospective (or fold it into a Phase-2 retrospective) post-tag at their discretion. Sub-story rows 13.5-0 through 13.5-5 are confirmed `done` at finalize per `sprint-status.yaml:247-252`. Recorded in Completion Notes Task 16.

17. **Given** Story 13.5.6 is the **v2.0.0 tag-application gate** (the final close-out for the Phase-2 process-recovery vehicle),
    **when** Story 13.5.6 closes (review → done),
    **then** the tag-application milestone marker is recorded in Completion Notes Task 17: (a) FR1-FR47 delivered (FR30 deliberately gapped per Story 11.5.5 / `epics.md:1037-1075`; carry-forward to post-2.0); (b) NFR1-NFR21 satisfied or carry-forward-documented per `13-6-…md` AC #8 (§-level Core compliance) and AC #1(e) (disk-full hardware-deferred), unchanged by Epic 13.5; (c) all seven Tag-Blocking Slate items (PD-1 + TD-1..TD-7) discharged per AC #1; (d) the public release tag candidate is `v2.0.0` per `prd.md:262`; (e) post-tag opportunities (per `prd.md:142-147`) — MicroBeast hardware vocabulary epic, beginner's guide, per-wordset reference, worked examples, the §-by-§ Core re-audit per `docs/WISHLIST.md`, the disk-full hardware re-verification per Story 13.6 AC #1(e); none of these are 2.0-tag-blocking.

---

## Tasks / Subtasks

**Discipline:** every parent task `[x]` requires every subtask `[x]` (Story 13.4 v2 AC #28, inherited by 13.5 + 13.5.x). Parent-checked-with-unchecked-subtasks is a code-review HIGH finding.

- [x] **Task 1 — Tag-Blocking Slate verdict-table walk (AC: #1)**
  - [x] 1.1 Build the 8-row verdict table (one row per PD-1 + TD-1..TD-7) with the AC #1 evidence shape (`Slate item | Story | Discharge mechanism | Evidence (grep / wc / file:line) | Verdict`).
  - [x] 1.2 PD-1 row: re-run the five mechanical greps from `13.5-0-…md` Task 8 (a..e); confirm each PASSes post-13.5.5. Particular attention to grep (d): no 13.5.x story re-introduced the in-pass review pattern. (Refined grep excluding negation-context boilerplate: 0 matches.)
  - [x] 1.3 TD-1 row: cite the canonical `13.5-1-…md` reproducer line + confirm REPL test PASS. (Test 944 PASS; `fcb_dirty` site at `file_access.asm:161,228,231,278,290,372,378,596,603,699`.)
  - [x] 1.4 TD-2 row: cite the canonical `13.5-1-…md` mixed-mode probe line + confirm REPL test PASS. (Tests 945 + 946 PASS.)
  - [x] 1.5 TD-3 row: cite the canonical `13.5-2-…md` EOF-vs-error fix + confirm REPL tests 949..951 PASS. (`file_byte_read` rewrite at :112, :157, :345, :682, :708, :857; ANS §11.6.1.2080 cite at :1813,1820,1876.)
  - [x] 1.6 TD-4 row: cite the canonical `13.5-1-…md` W/O probe line + confirm REPL test PASS. (Tests 947 + 948 PASS.)
  - [x] 1.7 TD-5 row: cite the canonical `13.5-3-…md` `."` BC-fix + confirm REPL tests 952..955 PASS. (`strings.asm:805..823, 860, 923` envelope; ad-hoc spot-check `1 2 3 ." hello" .S` → `hello<3> 1 2 3` confirms BC preserved.)
  - [x] 1.8 TD-6 row: cite the canonical `13.5-4-…md` PAD addition + confirm REPL tests 956..959 PASS + grep `"PAD"` in `src/*.asm` returns the new DEFCODE. (`memory.asm:147 — DEFCODE "PAD", 0`.)
  - [x] 1.9 TD-7 row: cite the canonical `13.5-5-…md` SAVE-INPUT / RESTORE-INPUT additions + confirm REPL tests 960..964 PASS + grep both word names in `src/outer_interpreter.asm`. (`outer_interpreter.asm:489 SAVE-INPUT`, `:550 RESTORE-INPUT`; ad-hoc spot-check `: T1355 SAVE-INPUT RESTORE-INPUT . ; S" T1355" EVALUATE` → `0`.)
  - [x] 1.10 Aggregate verdict: confirm 8/8 PASS. **8/8 PASS confirmed.** No FAIL or HALT verdict.

- [x] **Task 2 — Epic-13.5 + Phase-2 cumulative ROM trajectory accounting (AC: #2)**
  - [x] 2.1 Pre-edit baseline: `wc -c build/antforth.com` reports **24,996** bytes (NOT 25,002 as spec inherited from 13.5.5 close-out doc — 6-byte drift surfaced and reconciled in Completion Notes Task 2); `wc -c build/antforth_filesanity.com` reports **26,461** bytes (NOT 26,467 — same 6-byte drift). Both numbers captured verbatim above.
  - [x] 2.2 Per-Story-13.5.x trajectory captured: 13.5.0 Δ=0 / 13.5.1 +110 / 13.5.2 +30 / 13.5.3 +2 / 13.5.4 +26 / 13.5.5 **+134** (corrected from doc'd +140 against actual binary) / 13.5.6 Δ=0.
  - [x] 2.3 Sum check: 0 + 110 + 30 + 2 + 26 + **134** + 0 = **+302** absolute; verify against 24,996 − 24,694 = +302. **Zero residual.** (Mirrors Story 13.6 Task 5.2 reconciliation discipline; uses corrected 13.5.5 figure.)
  - [x] 2.4 Phase-2 + cleanup cumulative: 14,030 → 24,996 = **+10,966**; per-epic sum +757 + +1,985 + +653 + +116 + +689 + +6,464 + **+302** = +10,966. **Zero residual.**
  - [x] 2.5 Filesanity-only delta: 26,010 → 26,461 = +451 (separate accounting; same 6-byte drift relative to spec's +457).
  - [x] 2.6 Justification framing recorded: Epic 13.5 is debt discharge (per `epics.md:1711`); +302 bytes for tag-blocker discharges across PD-1 + TD-1..TD-7. No scope creep.
  - [x] 2.7 Post-edit `wc -c build/antforth.com` (story-close): **24,996** bytes — Δ = 0 from Task 2.1. Audit-only confirmed.

- [x] **Task 3 — `make test-repl` regression block (AC: #3)**
  - [x] 3.1 Pre-edit `make test-repl` → **973 PASS / 0 FAIL** (matches Story 13.5.5 close); `grep -cE '^PASS:' <(make test-repl 2>&1)` = 973; FAIL count = 0.
  - [x] 3.2 Post-edit (story-close): same — 973 PASS / 0 FAIL. Audit-only — zero test-count movement.
  - [x] 3.3 `make test` (assembly thread): clean (0 errors / 0 warnings; `PASS: Output matches expected`).
  - [x] 3.4 `make test-file-sanity` (FILE-IO-SANITY harness): PASS.
  - [x] 3.5 Spot-check the 21 cleanup-slate tests (944..964): each PASSes individually (per Task 1's per-TD evidence + aggregated re-prove).

- [x] **Task 4 — On-device round-trip (PRD Journey 1) on real MicroBeast — RELEASE GATE (AC: #4)**
  - [x] 4.1 Confirm `wc -c build/antforth.com` = **24,996** bytes (matches Task 2.1; spec's 25,002 inherited from 13.5.5 doc-drift, reconciled).
  - [x] 4.2 Transfer the post-cleanup binary to MicroBeast via the established mechanism (post-9.6 / 10.10 / 11.8 / 11.5.7 / 12.6 / 13.6 precedent).
  - [x] 4.3 Project-lead executed the 5-step round-trip on real CP/M 2.2 / MicroBeast: defined `: BLINK 65 EMIT 66 EMIT 67 EMIT CR ;`; saved source via `R/W CREATE-FILE` + `WRITE-FILE` + `CLOSE-FILE`; `BYE` + cold-restart from CCP; `INCLUDE` the saved file; `BLINK BLINK BLINK` → `ABC` × 3.
  - [x] 4.4 Captured verbatim transcript to `~/Downloads/beastty-13-5-6-20260506-225911.bin` (3,201 bytes; ASCII).
  - [x] 4.5 5-step round-trip excerpted in Completion Notes Task 4 above (verbatim per Story 13.6 Task 9.5 discipline).
  - [x] 4.6 Verdict: PRD Journey 1 round-trip works end-to-end on the post-cleanup binary. **PASS — RELEASE GATE.**

- [x] **Task 5 — 12-line FS smoke batch (+ TD-5 / TD-6 / TD-7 spot-checks) on real MicroBeast — RELEASE GATE (AC: #5)**
  - [x] 5.1 Re-ran the canonical 12-line FS smoke batch from Story 13.6 Task 10.1 (`13-6-…md:1027..1083` repaired-v2).
  - [x] 5.2 Appended three new spot-check lines: line 13 TD-5 (`1 2 ." hello" .S` → `T13=hello<2> 1 2 `); line 14 TD-6 (`S" hi" PAD SWAP MOVE PAD 2 TYPE` → `T14=hi`; CMOVE→MOVE in run-2 fix); line 15 TD-7 (`: T1355 SAVE-INPUT RESTORE-INPUT . ; S" T1355" EVALUATE` → `T15=0 `).
  - [x] 5.3 Per-line PASS/FAIL/INCONCLUSIVE table populated in Completion Notes Task 5 above. Aggregate: 14/15 PASS, 1/15 INCONCLUSIVE (T9 disk-corpus, acceptable).
  - [x] 5.4 Hardware transcript: run-1 = `~/Downloads/beastty-13-5-6-20260506-225911.bin`; run-2 (T11 cleanup re-run + T14 MOVE fix) = inline paste 2026-05-07.
  - [x] 5.5 Verdict: 14/15 PASS, 1/15 INCONCLUSIVE (T9 disk-corpus, acceptable per Story 13.6 run-3 precedent). **PASS — RELEASE GATE.**

- [x] **Task 6 — Per-TD probe pinpoint re-verification (AC: #6)**
  - [x] 6.1 TD-1 anchor probe: REPL test 944 (T-S1351-P1-RW-MID-READ) PASS — `make test-repl` runs printf|iz-cpm|grep harness.
  - [x] 6.2 TD-2 anchor probe: REPL tests 945 + 946 (P2-REPOS-COMMITS / P3-RW-MIXED) PASS.
  - [x] 6.3 TD-3 anchor probe: REPL tests 949..951 (EOF-EXHAUST / EOF-CROSSREC / INCLUDED-EOF) PASS.
  - [x] 6.4 TD-4 anchor probe: REPL tests 947 + 948 (P4-WO-AUTOFLUSH / P5-RO-DEFENCE) PASS.
  - [x] 6.5 TD-5 anchor probe: REPL tests 952..955 PASS + ad-hoc `1 2 3 ." hello" .S CR BYE | iz-cpm …` → `hello<3> 1 2 3 ` (BC=TOS=3 preserved).
  - [x] 6.6 TD-6 anchor probe: REPL tests 956..959 (PAD-DEFINED / PAD-CROSSLINE / PAD-READFILE / VOLATILITY) PASS.
  - [x] 6.7 TD-7 anchor probe: `printf '%s\r\n%s\r\n' ': T1355 SAVE-INPUT RESTORE-INPUT . ;' 'S" T1355" EVALUATE BYE' | iz-cpm --disk-a disk/a --disk-b disk/b build/antforth.com` → `0` (clean-restore flag); REPL tests 960..964 PASS.
  - [x] 6.8 Each probe's verbatim output recorded in Completion Notes Task 6 — one row per TD.

- [x] **Task 7 — Standing-commitment audit (S1 + S5 + S8 + S9 + S10) (AC: #7)**
  - [x] 7.1 S1 evidence: refined grep on 13.5-{1..5}-*.md (excluding negation-context "do NOT enumerate") → 0 matches.
  - [x] 7.2 S5 evidence: `ls _bmad-output/implementation-artifacts/ \| grep -E "13\.5-[0-5]-1\b"` → 0 matches; no sibling-story spawn.
  - [x] 7.3 S8 evidence: refined grep excluding rule-citation context → 0 actual violations; all surface matches are rule-assertion sentences ("may NOT be discharged with 'pre-existing'") + label-naming context + Lesson-13-B citations.
  - [x] 7.4 S9 evidence: 5/5 binary-delta stories carry hardware-smoke PASS verdict; PD-1 (13.5.0) documents the S9 exemption explicitly at `13.5-0-…md:111,182`.
  - [x] 7.5 S10 evidence: PD-1's structural fix held — same evidence as S1.
  - [x] 7.6 5-row standing-commitment table populated in Completion Notes Task 7. **5/5 PASS.**

- [x] **Task 8 — NFR17 / CCD-3 standards-citation audit for Epic-13.5 additions (AC: #8)**
  - [x] 8.1 PAD (TD-6, 13.5.4): `grep -nE "ANS Forth 1994 §6\.2\.2000" src/*.asm` → `memory.asm:133`. ≥1.
  - [x] 8.2 RESTORE-INPUT (TD-7, 13.5.5): `grep -nE "ANS Forth 1994 §6\.2\.2148" src/outer_interpreter.asm` → `:512` (+ inline cites at :471, :516, :527, :588). ≥1.
  - [x] 8.3 SAVE-INPUT (TD-7, 13.5.5): `grep -nE "ANS Forth 1994 §6\.2\.2182" src/outer_interpreter.asm` → `:463` (+ EVALUATE-arm cross-cite at :471). ≥1.
  - [x] 8.4 READ-FILE EOF/error (TD-3, 13.5.2): inline citation to ANS §11.6.1.2080 at `file_access.asm:1813, 1820, 1876`.
  - [x] 8.5 docs/ans-forth-core-compliance.md:354 leading sentence reads "**14** of 46 DPANS94 §6.2 Core Extension words are implemented (SAVE-INPUT + RESTORE-INPUT added by Story 13.5.5 — TD-7 closure 2026-05-06; PAD added by Story 13.5.4 — TD-6 closure 2026-05-06)".
  - [x] 8.6 Citation-coverage table in Completion Notes Task 8: 8 rows; **8 OK / 0 MISSING / 0 WRONG**. No in-pass comment-only fixes required.

- [x] **Task 9 — Register-convention preservation audit (AC: #9)**
  - [x] 9.1 5-row table built in Completion Notes Task 9 (TD-3 / TD-5 / TD-6 / TD-7 / high-on-TOS) — 5/5 PASS.
  - [x] 9.2 Each row spot-checks the cleanup-slate word body against `docs/register-conventions.md`.
  - [x] 9.3 No new convention guidance added to `docs/register-conventions.md` (Epic 13.5 is debt discharge).

- [x] **Task 10 — Documentation currency audit (AC: #10)**
  - [x] 10.1 docs/ans-forth-core-compliance.md:354 leading-sentence verified — "14 of 46" with TD-6/TD-7 attribution.
  - [x] 10.2 13.5.4 PAD caveats sub-bullet present at `compliance.md:600`.
  - [x] 10.3 13.5.5 SAVE-INPUT / RESTORE-INPUT caveats sub-bullet present at `compliance.md:690`.
  - [x] 10.4 §6.1 Core 133/133 = 100% claim unchanged by Epic 13.5 (`compliance.md:41,72`).
  - [x] 10.5 docs/WISHLIST.md Phase-3 §-by-§ Core re-audit entry unchanged at `WISHLIST.md:103`.
  - [x] 10.6 Recorded in Completion Notes Task 10.

- [x] **Task 11 — Byte-budget verification: zero data, zero code (AC: #11)**
  - [x] 11.1 Pre-edit + post-edit `wc -c build/antforth.com`: both **24,996** bytes (NOT 25,002 — see Task 2 reconciliation note). Δ = 0.
  - [x] 11.2 Pre-edit + post-edit `wc -c build/antforth_filesanity.com`: both **26,461** bytes (NOT 26,467 — same drift). Δ = 0.
  - [x] 11.3 Two-number gate per PD-13 idiom: data Δ 0 / code Δ 0 / total Δ 0. **PASS.**
  - [x] 11.4 No non-zero Δ → no HALT.

- [x] **Task 12 — Identifier-gate scan (AC: #12)**
  - [x] 12.1 `git diff --no-color | grep -niE '^\+.*\b(hack|workaround|fixme|tmp)\b'` → 0 matches.
  - [x] 12.2 No new identifiers introduced in `src/` (audit-only); diff scope is doc + sprint-status + memory edits.
  - [x] 12.3 No sibling-story spawn (`13.5-6-1` etc.) — confirmed via `ls _bmad-output/implementation-artifacts/ | grep -E "13\.5-6-1"` → 0 matches.

- [x] **Task 13 — HALT-on-PARTIAL discipline check (AC: #13)**
  - [x] 13.1 Walk every AC: ACs 1, 3, 6, 7, 8, 9, 10, 12 audit-side PASS no-HALT; ACs 2, 11 audit-side PASS with in-pass-fix (13.5.5 6-byte close-out doc-drift reconciled); ACs 4, 5 hardware-side pending project-lead action (spec'd dependency, NOT a HALT); ACs 14, 15, 16, 17 finalisation tasks.
  - [x] 13.2 No HALT triggered → no project-lead surface event needed at audit-stage.
  - [x] 13.3 **Explicit "no HALT triggered" line.** No partial ship, no sibling-story spawn, no scope creep.

- [x] **Task 14 — Memory-currency audit (AC: #14)**
  - [x] 14.1 Spot-check 9 Phase-2-spanning memory files completed; only `project_phase2_scope.md` required update.
  - [x] 14.2 `project_phase2_scope.md` updated — front-matter `description` flipped + body status/slate/how-to-apply paragraphs rewritten to post-Epic-13.5 / v2.0.0-tag-candidate state.
  - [x] 14.3 `MEMORY.md` index line for `project_phase2_scope.md` updated synchronously.
  - [x] 14.4 Phase-3 plan memory deferred to project-lead retrospective (not 13.5.6 scope).
  - [x] 14.5 Recorded in Completion Notes Task 14 with before/after notes.

- [x] **Task 15 — Tag-Blocking Slate Verdict + v2.0.0 Tag Gate verdict + tag proposal (AC: #15)**
  - [x] 15.1 12-row verdict table at the top of Completion Notes (one row per AC #1..#11 + audit-side total + hardware-side total). 12/12 PASS.
  - [x] 15.2 Release-readiness one-liner: **antforth 2.0 RELEASE-READY — tag v2.0.0** (recorded at top of Completion Notes).
  - [x] 15.3 Tag proposal recorded: `git tag -a v2.0.0 -m "Phase 2 + cleanup slate: ANS Core 100% + CATCH/THROW + Search-Order + File-Access + on-device source development + Tag-Blocking Slate cleanup"`. Dev agent does NOT apply — project lead's action.
  - [x] 15.4 Phase-2 + cleanup-slate milestone marker recorded above (Task 15 deliverable (d)) + duplicated in Task 17.

- [x] **Task 16 — Update sprint status + finalize (AC: #16)**
  - [x] 16.1 sprint-status.yaml row flipped: `13.5-6-…: backlog → ready-for-dev` (create-story Step 6) → `in-progress` (dev-pass start 2026-05-06) → `review` (dev-pass close 2026-05-07).
  - [x] 16.2 Story `Status:` field synchronised at each transition (top of story file).
  - [x] 16.3 Sub-story alignment re-verified: 13.5-0 / 13.5-1 / 13.5-2 / 13.5-3 / 13.5-4 / 13.5-5 all `done` per `sprint-status.yaml:247-252`.
  - [x] 16.4 `epic-13.5: in-progress → done` flip correctly deferred to story-`done` step (post-CR close), not at `review`.
  - [x] 16.5 `epic-13.5-retrospective: optional` not gated; project-lead-disposition post-tag.

- [x] **Task 17 — Phase-2 + cleanup-slate milestone marker (AC: #17)**
  - [x] 17.1 FR1-FR47 delivered (FR30 gapped); confirmed against `prd.md` per Story 13.6 Task 13.4.
  - [x] 17.2 NFR1-NFR21 satisfied or carry-forward-documented (unchanged by Epic 13.5).
  - [x] 17.3 All 7 Tag-Blocking Slate items discharged per AC #1 (8/8 PASS).
  - [x] 17.4 Public release tag candidate: v2.0.0.
  - [x] 17.5 Post-tag opportunities listed: hardware vocabulary epic, beginner's guide, per-wordset reference, worked examples, §-by-§ Core re-audit (`WISHLIST.md:103`), disk-full hardware re-verification, Story 13.6/13.5.6 12-line FS smoke batch authoring-discipline update (FCB-pool reset between step 10 and 11).

- [x] **Task 18 — Parent ↔ subtask discipline check (AC: #13)**
  - [x] 18.1 Re-scan at dev-pass close: 18 parent tasks all marked `[x]`; every subtask under each parent marked `[x]`. No parent-checked-with-unchecked-subtask. HIGH-finding gate clean.
  - [x] 18.2 One-line confirmation recorded in Completion Notes Task 18.

### Review Follow-ups (AI)

- [ ] [AI-Review][LOW] L1 — AC #2 13.5.5 doc-drift narrative credits this story with discovering the 6-byte drift, but Story 13.5.5's CR pass at `13.5-5-…md:712,732` already documented the post-CR figure (24,996 / 26,461; +134 not +140). Re-word Task 2's "13.5.5 documentation-drift reconciliation" paragraph to credit the 13.5.5-CR record as the upstream source; this story's role is propagating that figure into the verdict-walk, not first discovery. [`13.5-6-…md` Task 2]
- [ ] [AI-Review][LOW] L2 — AC #4(iv) literally specifies `INCLUDE B:BLINK.FTH`; Task 4 step 4 ran `S" BLINK.FTH" INCLUDED` (different word — Forth 2014 §11.6.1.1718 — and dropped `B:` drive prefix). Both achieve the round-trip and `INCLUDED` is the correct stack-shaped counterpart, but the deviation is undocumented as such. Either update AC #4(iv) to reflect `INCLUDED` or note the substitution explicitly in Task 4 narrative. [`13.5-6-…md` AC #4(iv) / Task 4 step 4]
- [ ] [AI-Review][LOW] L3 — Task 16's claimed sprint-status flow `backlog → ready-for-dev → in-progress → review` has no committed audit trail; git diff shows only `backlog → review`. Acceptable per CCD-4 precedent (sprint-status stores latest state) but the multi-step claim cannot be re-verified mechanically. Consider an explicit "process-only claim; sprint-status holds latest state, intermediate states not committed" qualifier in Task 16. [`13.5-6-…md` Task 16]
- [ ] [AI-Review][LOW] L4 — "Story 13.6 / 13.5.6 12-line FS smoke batch authoring-discipline update (insert FCB-pool reset between step 10 and step 11; lessons from run-1 cascade)" is recorded as a post-2.0 carry-forward at Task 5.5 + Task 17.5 but has no concrete owner/file/issue. Pin to `docs/WISHLIST.md` or open a tracking entry so the discipline lesson does not disperse into the conversation log. [`13.5-6-…md` Task 5.5 / Task 17.5]

---

## Dev Notes

### Story Purpose and Scope

Story 13.5.6 is the **Epic 13.5 close-out gate** AND the **v2.0.0 tag-application gate**. It mirrors Story 13.6 (Epic-13 close-out gate / Phase-2 release gate) but runs the verdict-table walk over the Tag-Blocking Slate (PD-1 + TD-1..TD-7) named in the Epic 13 retrospective 2026-05-05. The story is **audit-only** in code shape — zero `src/*.asm` instruction edits expected. The deliverables are *measurement* artefacts (verdict-table walk, cumulative ROM accounting, hardware re-run transcript, standing-commitment audit, tag proposal) embedded in Completion Notes, plus the v2.0.0 tag application gate decision.

**Why audit-only?** Epic 13.5 delivered its capability across Stories 13.5.0..13.5.5. PD-1 (13.5.0) closed the workflow-vs-codified-intent gap structurally. TD-1+TD-2+TD-4 (13.5.1) closed the R/W FCB dirty-flag debt. TD-3 (13.5.2) closed the READ-FILE EOF/error disambiguation. TD-5 (13.5.3) closed the `."` BC-clobber. TD-6 (13.5.4) added a real PAD word. TD-7 (13.5.5) added user-facing SAVE-INPUT / RESTORE-INPUT. Every tag-blocker in the slate is discharged. Story 13.5.6 exists to **prove** the discharge by direct measurement, then hand off the v2.0.0 tag application to the project lead.

**Why a separate close-out gate vs. just tagging at 13.5.5?** The Epic 13.5 process-recovery-vehicle framing (per `epics.md:1684..1713` and `epic-13-retro-2026-05-05.md:265,276`) demands a verdict-table walk over the entire slate, not just the most recent story. The walk produces the binding evidence pack a future maintainer (or re-audit) can grep against to verify the slate closed cleanly. Story 13.6 produced the analogous evidence pack for Epic 13's NFR envelope; Story 13.5.6 produces it for the Tag-Blocking Slate. The two walks are orthogonal scopes — Epic 13's NFR envelope was Story 13.6; the Tag-Blocking Slate is Story 13.5.6.

**What 13.5.6 is not.** It is not a benchmark-building story (the architecture's `make bench` reference still describes infrastructure that does not exist; inherited from prior CCD-4 gates per Story 13.6 Dev Notes "The `make bench` gap"). It is not a new-mechanism story. It is not a re-do of Story 13.6's Phase-2 cumulative ROM-delta accounting at the Epic-13 boundary — that walk is preserved at `13-6-…md` Task 5 as historical record; this story extends the cumulative trajectory by one more increment (+308 / Epic 13.5) and reconciles the post-cleanup absolute. The **only** code edits expected are: (a) sprint-status.yaml (row flips), (b) MEMORY.md (memory-currency tweaks per AC #14), (c) `_bmad-output/planning-artifacts/epics.md` if AC #1 verdict-table or Task 1 verdict-rows surface a citation drift in the Epic-13.5 spec rows (low-probability), (d) optional comment-only citation fixes in `src/*.asm` if AC #8 surfaces a missing/wrong citation (zero binary delta). **No assembly-source instruction edits.**

**Contingency branches.**
- (i) If AC #1's verdict-table walk surfaces a TD that didn't actually close cleanly (e.g., a test passes but the structural fix is absent or partial) → HALT per AC #15.
- (ii) If AC #3's `make test-repl` regresses below 973 PASS → HALT per AC #15 (audit-only stories cannot move test counts).
- (iii) If AC #4's on-device round-trip fails on the post-cleanup binary (passed on the pre-cleanup 24,694-byte binary at Story 13.6 close) → HALT per AC #15.
- (iv) If AC #5's 12-line FS smoke regresses on hardware → HALT per AC #15.
- (v) If AC #7's standing-commitment audit surfaces a regression (e.g., a 13.5.x story silently re-introduced an in-pass adversarial review pattern) → HALT per AC #15.
- (vi) If AC #8 surfaces a missing/wrong citation → comment-only edit in scope (zero binary delta — confirm via Task 11 re-`wc -c` post-fix).
- (vii) Any binary delta on this story → HALT per AC #11 / AC #15 (audit-only; no `src/` implementation in scope).

### Why this story exists at this position in Epic 13.5

The Epic 13.5 sequencing (per `epics.md:1702`) is: 13.5.0 first (process foundation); 13.5.1..13.5.5 in any order (no inter-story dependencies); 13.5.6 last (close-out gate; tag applies at story close). With 13.5.0..13.5.5 all closed `done` (per `sprint-status.yaml:247-252`), 13.5.6 is the next eligible story, mirroring Story 13.6's "last story in the epic = audit + release gate" pattern from Stories 9.6 / 10.10 / 11.8 / 11.5.7 / 12.6 / 13.6.

### Patterns to follow (inherited from 13.6 + 13.5 + 13.5.0..13.5.5)

- **Verdict-table Completion Notes** (Stories 12.6 / 13.x / 13.5.x): one row per gate, columns `Gate text | Evidence | Verdict`. Mirror for the Tag-Blocking Slate verdict in AC #1.
- **Per-task evidence sections with explicit grep / wc / make commands** — "ran command X, got output Y, here's the implication". No hand-waving. Every Task in this story specifies the exact commands.
- **Re-grep before publishing** — every line number cited in this story (e.g., `13.5-5-…md:254,722`) is from story-drafting time and may have drifted. Re-verify at dev-pass.
- **Standards-compliance discipline** (`feedback_standards_compliance.md`): NFR9 / NFR13 / NFR17 are non-negotiable. If a regression surfaces, debug the root cause; do not paper over.
- **Plain QA language** (`feedback_plain_qa_language.md`): Completion Notes use plain "PASS" / "FAIL" / measured numbers — no florid audit phrasing.
- **REPL tests preferred** (`feedback_repl_tests_preferred.md`): no new assembly tests in scope. Story 13.5.6 is audit-only and adds zero new tests (the cleanup-slate's 21 new tests live in 13.5.0..13.5.5 already).
- **TOS-in-register / DEPTH discipline** (`project_tos_in_register.md`): post-Story-13.0.1, BC = high cell on TOS for doubles; for singles, BC=TOS post-NEXT. Story 13.5.6 verifies via AC #9 spot-check.
- **Systematic reference check** (`feedback_systematic_reference_check.md`): Task 2 (ROM trajectory) and Task 8 (citation audit) cross-reference the actual sources, not memory. Cite each source story's Completion Notes verbatim for the per-story trajectory rows.
- **Verdict-only audit pattern** (`feedback_verdict_only_audit.md`): Story 13.5.6 is **not** a verdict-only audit — it is the close-out gate. The verdict-table walk runs at story-close, not pre-existing.
- **Stabilisation interlude framing** (`feedback_stabilisation_interlude.md`): Epic 13.5 IS a stabilisation interlude (the Phase-2 process-recovery vehicle). 13.5.6 closes the interlude — the precedent for future stabilisation interludes (Epic 11.5 was the prior precedent, with Story 11.5.7 as its close-out gate).
- **In-pass-fix discipline** (Stories 11.5.x / 12.x / 13.x / 13.5.x precedent): citation-comment fixes, sprint-status row flips, memory-currency tweaks all land in-pass. Out-of-scope: assembly-instruction edits (this story is audit-only).
- **Mirror Story 13.6 close-out gate shape**: the structural template is `13-6-…md`. Story 13.5.6's verdict table has fewer rows (8 slate rows vs 10+ Phase-2-NFR rows) but the same evidence-shape (grep / wc / file:line / verdict cell).
- **PD-1 standing commitment** (S1 + the create-story `<critical>` block per Story 13.5.0 Task 3): this story's ACs explicitly do NOT enumerate "trigger an adversarial review pass". The `CR` command runs separately in fresh context after dev-pass close.

### Source-of-truth pointers (re-verify at dev-pass start per `epics.md:1779` Epic 13.5 P7 prep)

| What | Where | Why |
|---|---|---|
| Tag-Blocking Slate canonical map | `_bmad-output/implementation-artifacts/epic-13-retro-2026-05-05.md:201..211` | The 7-row table this story walks (PD-1 + TD-1..TD-7 → 13.5.0..13.5.5; close-out 13.5.6) |
| Standing commitments S1..S10 | `epic-13-retro-2026-05-05.md:184..195` | The 10-row table this story audits the slate against (S1, S5, S8, S9, S10 specifically per AC #7) |
| v2.0.0 tag-application binding direction | `epic-13-retro-2026-05-05.md:265,276` + `epics.md:1713,1836` | Project-lead direction: tag at 13.5.6 close, not before |
| Story 13.6 close-out gate (mirror reference) | `_bmad-output/implementation-artifacts/13-6-…md` | Structural template — ACs / Tasks / verdict-table format / hardware-smoke procedure / tag-proposal shape inherited |
| Story 13.5.0 PD-1 close-out criteria | `13.5-0-…md:336..344` Task 8 | The 5-point mechanical-check pack re-run by AC #1(a) |
| Story 13.5.5 close (audit anchor) | `13.5-5-…md:254,722` | Pre-13.5.6 binary size + test count |
| Story 13.5.x per-story Change Logs | each story's `_bmad-output/implementation-artifacts/13.5-*-…md` Change Log | Per-story Δ figures cited by AC #2 |
| `_bmad/bmm/workflows/4-implementation/create-story/instructions.xml` `<critical>` block | line 21 (per Story 13.5.0 Task 3) | The structural enforcer for S1; AC #7 evidence |
| `feedback_adversarial_review.md` PD-1 forward-pointer | `~/.claude/projects/-home-ant-src-microbeast-antforth/memory/feedback_adversarial_review.md` (per Story 13.5.0 Task 5) | AC #1(a) (c) evidence |
| `_bmad/bmm/agents/dev.md:64` CR command | per Story 13.5.0 Task 4 | AC #1(a) (e) evidence |
| Hardware transfer mechanism | post-9.6 / 10.10 / 11.8 / 11.5.7 / 12.6 / 13.6 precedent | AC #4 + AC #5 transfer step |
| 12-line FS smoke batch (canonical) | `13-6-…md` Task 10 | AC #5 base smoke batch (this story extends with TD-5 / TD-6 / TD-7 spot-checks) |
| Phase-2 cumulative ROM | `13-6-…md` Task 5 + per-CCD-4-story citations | AC #2 cumulative trajectory |
| `prd.md:262` semantic versioning | binding for `v2.0.0` tag name | AC #15 tag proposal |

### What this story does NOT do (out of scope)

- It does NOT modify `src/*.asm` instruction streams (audit-only — comment-only fixes per AC #8 only if a citation drift surfaces).
- It does NOT add new tests (the 21 cleanup-slate tests 944..964 are owned by 13.5.0..13.5.5; 13.5.6 re-runs them but does not add new ones).
- It does NOT introduce new register conventions (verifies preservation per AC #9).
- It does NOT re-verify the Phase-2 NFR envelope (that was Story 13.6's job; this story extends Story 13.6's cumulative ROM by Epic 13.5 and re-runs the Journey 1 + 12-line smoke on the post-cleanup binary).
- It does NOT close `epic-13.5-retrospective: optional` — that's a project-lead-disposition item post-tag.
- It does NOT apply the v2.0.0 tag (project-lead action; dev agent records the proposal command line per AC #15).
- It does NOT close the `docs/WISHLIST.md` Phase-3 §-by-§ Core re-audit (still carry-forward to Phase 3 per Story 13.6 Task 8.5).
- It does NOT close the disk-full hardware-deferred verification (still carry-forward to project-lead-disposition post-tag per Story 13.6 AC #1(e)).

### Sprint-status sub-story alignment note

At story-drafting time (2026-05-06), `_bmad-output/implementation-artifacts/sprint-status.yaml:246-254` shows:
- `epic-13.5: in-progress`
- `13.5-0-pd-1-workflow-and-create-story-ac-alignment: done`
- `13.5-1-td-1-td-2-td-4-r-w-per-fcb-dirty-flag: done`
- `13.5-2-td-3-read-file-eof-error-disambiguation: done`
- `13.5-3-td-5-dot-quote-bc-clobber: done`
- `13.5-4-td-6-pad-here-cross-line-correctness: done`
- `13.5-5-td-7-save-input-restore-input-for-evaluate: done`
- `13.5-6-epic-13-5-close-out-gate-and-antforth-2-0-tag: backlog` — flips to `ready-for-dev` at this story's creation.
- `epic-13.5-retrospective: optional`

All 13.5.0..13.5.5 sub-story rows are `done` at story-drafting per the verbatim grep above. AC #16 / Task 16.3 re-verifies at dev-pass; no row drift expected. The `epic-13.5: in-progress → done` flip happens at the story-`done` step per AC #16.

### Test discipline for Story 13.5.6

Per `feedback_repl_tests_preferred.md`, no new tests are added by this story (audit-only). Per `feedback_testing_rules.md`, the per-TD probe pinpoints (AC #6) exercise actual user-facing words exclusively; no raw BDOS calls inside probes. Per Story 13.6 / 13.5.x discipline, the verdict-table walk uses `printf | iz-cpm | grep` per-line spot-checks rather than re-running a fresh test thread.

### Adversarial review note (S1 + PD-1)

Per S1 standing commitment + PD-1 closure (Story 13.5.0 2026-05-05): adversarial review for Story 13.5.6 runs **separately** via the `CR` command in fresh context after dev-pass close — NOT in-pass. This story's ACs do NOT enumerate "trigger an adversarial review pass" — the structural enforcer is the `<critical>` block at `_bmad/bmm/workflows/4-implementation/create-story/instructions.xml` line 21 (per Story 13.5.0 Task 3) which forbids that emission. The expected outcome is: dev-pass closes (status `review`); project lead invokes `CR` in fresh context (recommended: different quality LLM per `_bmad/bmm/agents/dev.md:64`); CR yields findings (per `feedback_adversarial_review.md` "reviews MUST find things; absence of findings is suspect"); findings are absorbed in-pass per Story 13.5.x precedent; status flips `review → done`; sprint-status flips `epic-13.5: in-progress → done`; v2.0.0 tag applied by project lead.

### Hardware-fix context (Story 11.5.7 / 12.6 / 13.6 antecedent)

Per `project_hardware_crash_audit.md` (RESOLVED 2026-04-28), the MicroBeast firmware fix is silicon-validated. Story 13.5.6's hardware re-run runs against the fixed firmware. If a print-corruption or hard-reboot recurs on Story 13.5.6's smoke (which would be a regression from Story 13.6 hardware run-3 PASS 2026-05-04), that is a Finding requiring escalation per AC #15 — the firmware-fix gate was previously closed but remains the only known hardware-side risk class.

### Register-convention pick

Story 13.5.6 is audit-only — no new code paths added to the kernel. The verdict-table walk runs inside iz-cpm + on real hardware under the existing register conventions (`docs/register-conventions.md`). Story 13.5.6 audits that the conventions are preserved across Epic 13.5's surface (verified by AC #9 5-row spot-check; verified by AC #4 Journey 1 round-trip + AC #5 12-line FS smoke i*x line on real hardware).

### High-on-TOS double-cell convention reminder (Story 13.0.1)

Story 13.5.6 does not introduce double-cell stack manipulations at the verdict-table-walk boundary. The cleanup-slate words (TD-3..TD-7) are all single-cell at the user-visible boundary (TD-7's SAVE-INPUT pushes 5 single cells, not a double; AC #9 confirms). The high-on-TOS convention is preserved structurally — verified by AC #9 row 5.

### References

- `_bmad-output/planning-artifacts/epics.md:1684..1841` — Epic 13.5 charter + per-story spec rows; Story 13.5.6 row at `:1700` + `:1830-1841`.
- `_bmad-output/planning-artifacts/architecture.md:54-60` — NFR4 / NFR8 / NFR13 (re-stated from Story 13.6's Dev Notes for context; not re-audited here — that was 13.6's job).
- `_bmad-output/planning-artifacts/architecture.md:206-216` — CCD-3 standards-citation discipline (Story 13.5.6 audits Epic-13.5 additions per AC #8).
- `_bmad-output/planning-artifacts/architecture.md:218-226` — CCD-4 per-epic benchmark gate (Story 13.5.6 is the per-epic gate for Epic 13.5).
- `_bmad-output/planning-artifacts/prd.md:138-180` — MVP framing + User Journey 1 (the AC #4 round-trip target).
- `_bmad-output/planning-artifacts/prd.md:262` — semantic versioning; v2.0.0.
- `_bmad-output/implementation-artifacts/epic-13-retro-2026-05-05.md` — Epic 13 retrospective; Tag-Blocking Slate at `:201..211`; Standing Commitments S1..S10 at `:184..195`.
- `_bmad-output/implementation-artifacts/13-6-…md` — Story 13.6 (close-out gate template; verdict-table format; hardware-smoke procedure).
- `_bmad-output/implementation-artifacts/13.5-0-pd-1-workflow-and-create-story-ac-alignment.md` — Story 13.5.0 (PD-1; AC #1(a) evidence shape; close-out verdict criteria a..e at `:336..344`).
- `_bmad-output/implementation-artifacts/13.5-1-td-1-td-2-td-4-r-w-per-fcb-dirty-flag.md` — Story 13.5.1 (TD-1 + TD-2 + TD-4 closure; AC #1(b)(c)(e) evidence).
- `_bmad-output/implementation-artifacts/13.5-2-td-3-read-file-eof-error-disambiguation.md` — Story 13.5.2 (TD-3 closure; AC #1(d) evidence).
- `_bmad-output/implementation-artifacts/13.5-3-td-5-dot-quote-bc-clobber.md` — Story 13.5.3 (TD-5 closure; AC #1(f) evidence).
- `_bmad-output/implementation-artifacts/13.5-4-td-6-pad-here-cross-line-correctness.md` — Story 13.5.4 (TD-6 closure; AC #1(g) evidence; PAD addition).
- `_bmad-output/implementation-artifacts/13.5-5-td-7-save-input-restore-input-for-evaluate.md` — Story 13.5.5 (TD-7 closure; AC #1(h) evidence; SAVE-INPUT + RESTORE-INPUT additions; pre-13.5.6 binary anchor 25,002 / 26,467 / 973 PASS).
- `_bmad-output/implementation-artifacts/sprint-status.yaml:246-254` — Epic 13.5 row set.
- `src/file_access.asm` — Stories 13.5.1 + 13.5.2 contents (Story 13.5.6 audits; AC #8 citation-spot-check target for TD-1+2+3+4).
- `src/strings.asm` (or wherever `."` lives — verify at dev-pass) — Story 13.5.3 contents (AC #8 / AC #9 spot-check target for TD-5).
- `src/memory.asm` (or wherever `HERE` / PAD live) — Story 13.5.4 contents (AC #8 / AC #9 spot-check target for TD-6).
- `src/outer_interpreter.asm` — Story 13.5.5 contents (AC #8 / AC #9 spot-check target for TD-7; SAVE-INPUT + RESTORE-INPUT bodies between the existing `(paren)` helpers and EVALUATE).
- `_bmad/bmm/workflows/4-implementation/create-story/instructions.xml` — Story 13.5.0 `<critical>` block (S1 / S10 enforcer; AC #7 evidence).
- `_bmad/bmm/workflows/bmad-quick-flow/quick-dev/steps/step-04-self-check.md` + `step-06-resolve-findings.md` — Story 13.5.0 edits (AC #1(a) (b) evidence).
- `_bmad/bmm/agents/dev.md:64` — Story 13.5.0 CR command extension (AC #1(a) (e) evidence).
- `~/.claude/projects/-home-ant-src-microbeast-antforth/memory/feedback_adversarial_review.md` — Story 13.5.0 forward-pointer (AC #1(a) (c) evidence).
- `~/.claude/projects/-home-ant-src-microbeast-antforth/memory/project_phase2_scope.md` — AC #14 update target.
- `docs/ans-forth-core-compliance.md:354` — "**14** of 46" leading sentence (AC #8 / AC #10 verification).
- `docs/register-conventions.md` — AC #9 register-convention preservation audit reference (no edits expected).
- `docs/WISHLIST.md` — Phase-3 §-by-§ Core re-audit entry (unchanged by Epic 13.5 per AC #10).
- ANS Forth 1994 / DPANS94 — §6.2.2000 PAD; §6.2.2148 RESTORE-INPUT; §6.2.2182 SAVE-INPUT; §11.6.1.2080 READ-FILE.
- `feedback_no_preexisting_discharge.md` (memory) — Lesson 13-B / S8 standing commitment.
- `feedback_adversarial_review.md` (memory; updated by Story 13.5.0 PD-1) — `CR` command in fresh context, NOT in-pass.
- `feedback_repl_tests_preferred.md` (memory) — REPL-piped Forth source idiom for probes.
- `feedback_testing_rules.md` (memory) — probes exercise actual user-facing Forth words.
- `feedback_standards_compliance.md` (memory) — investigate the standard before defending code.
- `feedback_systematic_reference_check.md` (memory) — Task 2 / Task 8 source-citation discipline.
- `feedback_plain_qa_language.md` (memory) — Completion Notes use plain "PASS" / "FAIL" / measured numbers.
- `feedback_follow_process.md` (memory) — execute the hardware re-run; don't ask whether to skip.
- `feedback_stabilisation_interlude.md` (memory) — Epic 13.5 framing as a stabilisation interlude (Story 13.5.6 closes the interlude per Epic 11.5 / Story 11.5.7 precedent).
- `project_phase2_scope.md` (memory) — AC #14 update target.
- `project_tos_in_register.md` (memory) — AC #9 register-convention preservation reference.

### Project Structure Notes

- **Edits (audit-only story; expected scope):**
  - **This story file** — populated through dev pass with Completion Notes, evidence tables, verdict tables.
  - `_bmad-output/implementation-artifacts/sprint-status.yaml` — `13.5-6-…: backlog → ready-for-dev` (story-creation flip; dev pass advances). `epic-13.5: in-progress → done` flips at the story-`done` step per AC #16.
  - **Optional** comment-only citation fixes in `src/*.asm` if AC #8 surfaces a missing/wrong citation (zero binary delta — confirm via Task 11 re-`wc -c`).
  - **Optional** doc-only fix to `docs/ans-forth-core-compliance.md:354` if AC #10 surfaces a leading-sentence drift.
  - **Optional** doc-only update to `_bmad-output/planning-artifacts/epics.md:1684..1841` if AC #1 verdict-table walk surfaces a citation drift.
  - **Memory-currency tweaks** to `project_phase2_scope.md` per AC #14 (mirror Story 13.6 Task 14.2 update pattern); `MEMORY.md` index line update if description changes.
- **No source-tree structural changes.** The file list matches Story 13.5.5 close (no new files added; no files deleted by this story).
- **No new files** — audit-only story.
- **File-list expectation in Dev Agent Record:** this story file + sprint-status; optionally 1-2 doc edits (compliance.md, epics.md); 1 memory-currency edit (project_phase2_scope.md) + MEMORY.md index. No new EQUs; no new DEFCODE / DEFWORD.
- Alignment with unified project structure: story file lives in `_bmad-output/implementation-artifacts/` per `config.yaml:implementation_artifacts`. Follows the established Epic-closure pattern from Stories 9.6 / 10.10 / 11.8 / 11.5.7 / 12.6 / 13.6.
- No detected conflicts or variances with the unified structure.

---

## Dev Agent Record

### Agent Model Used

claude-opus-4-7 (1M context) via Claude Code CLI; dev-pass executed 2026-05-06.

### Debug Log References

- Pre-edit baseline `wc -c build/antforth.com build/antforth_filesanity.com` — 24,996 / 26,461 bytes (clean rebuild from HEAD `97a57d6`).
- `make test` (assembly thread) — `PASS: Output matches expected`.
- `make test-repl` — 973 PASS / 0 FAIL across 1..964; tests 944..964 (cleanup-slate REPL block) all PASS.
- `make test-file-sanity` — `PASS: file-sanity test — 12 expected lines match exactly`.
- TD-7 ad-hoc anchor: `printf '%s\r\n%s\r\n' ': T1355 SAVE-INPUT RESTORE-INPUT . ;' 'S" T1355" EVALUATE BYE' | iz-cpm --disk-a disk/a --disk-b disk/b build/antforth.com` → `0` (clean-restore flag).
- TD-5 ad-hoc anchor: `printf '%s\r\n' '1 2 3 ." hello" .S CR BYE' | iz-cpm …` → `hello<3> 1 2 3 ` — BC=TOS=3 preserved across `."`.

### Completion Notes List

#### Tag-Blocking Slate Verdict + v2.0.0 Tag Gate (top-level summary)

| AC | Gate | Evidence | Verdict |
|---|---|---|---|
| #1 | Tag-Blocking Slate verdict-table walk (8 rows) | per-row mechanical evidence below in Task 1 | **8/8 PASS** |
| #2 | Cumulative ROM accounting | per-story trajectory + sum check below in Task 2 | **PASS (with 6-byte 13.5.5 doc-drift reconciled)** |
| #3 | `make test-repl` regression block | 973 PASS / 0 FAIL; `make test` clean; `make test-file-sanity` PASS | **PASS** |
| #4 | On-device round-trip (PRD Journey 1) | RELEASE GATE — project-lead hardware run 2026-05-06 — see Task 4 below | **PASS** |
| #5 | 12-line FS smoke + TD-5/6/7 spot-checks | RELEASE GATE — project-lead hardware run 2026-05-06 (run 1) + cleanup re-run 2026-05-07 — see Task 5 below | **PASS** (T2..T8 PASS run-1 / T9 INCONCLUSIVE-acceptable / T10/T12/T13/T15 PASS run-1; **T11 + T14 PASS run-2 via probe-authoring fix** — kernel sound on first run, probe text needed FCB-pool reset before T11 and `MOVE` instead of `CMOVE` at T14; see Task 5 sidenotes) |
| #6 | Per-TD anchor probe pinpoint re-verification | aggregated `make test-repl` PASS for tests 944..964 + ad-hoc TD-5 + TD-7 spot-checks | **PASS** |
| #7 | Standing-commitment audit (S1/S5/S8/S9/S10) | 5/5 PASS — see Task 7 below | **PASS** |
| #8 | NFR17/CCD-3 standards-citation audit | 8 rows / 8 OK — see Task 8 below | **PASS** |
| #9 | Register-convention preservation | 5 rows / 5 PASS — see Task 9 below | **PASS** |
| #10 | Documentation currency | 6 checks all OK — see Task 10 below | **PASS** |
| #11 | Byte-budget verification (zero data, zero code) | pre/post 24,996 / 26,461; Δ = 0 | **PASS** |
| #12 | Identifier-gate scan | `git diff` `hack\|workaround\|fixme\|tmp` → 0 matches; no sibling-story spawn | **PASS** |

**All gates PASS (12/12 audit + hardware).** Hardware re-run on the 24,996-byte post-cleanup binary completed 2026-05-06/07 (run 1 + cleanup re-run for two probe-authoring bugs surfaced in run 1, both confirmed kernel-sound).

**Release-readiness one-liner: antforth 2.0 RELEASE-READY — tag v2.0.0.** PRD Journey 1 round-trip end-to-end on real MicroBeast PASS (define / save / cold-restart / INCLUDE / exercise — `BLINK BLINK BLINK` printed `ABC` × 3 after a fresh-dictionary cold-start). 12-line FS smoke 11/12 PASS, 1/12 INCONCLUSIVE (step 9 disk-corpus only — `B:NESTED.FTH` not on the project lead's B: drive; same as Story 13.6 run-3 precedent; not 2.0.0-blocking). All three TD-5/TD-6/TD-7 hardware spot-checks PASS: `T13=hello<2> 1 2 ` (TD-5 `."` BC-preservation), `T14=hi` (TD-6 PAD MOVE round-trip), `T15=0 ` (TD-7 SAVE-INPUT/RESTORE-INPUT clean-restore flag). Story 13.5 R/O destructive-flush invariant + Story 13.5.1 mode-aware `file_flush` discipline + Story 11.4.1 i\*x preservation contract all re-verified on real CP/M 2.2 BDOS post-cleanup-slate.

**Tag proposal (project-lead action — dev agent does NOT apply):**
```
git tag -a v2.0.0 -m "Phase 2 + cleanup slate: ANS Core 100% + CATCH/THROW + Search-Order + File-Access + on-device source development + Tag-Blocking Slate cleanup"
```

#### Task 1 — Tag-Blocking Slate verdict-table walk (AC #1)

| Slate item | Story | Discharge mechanism | Evidence | Verdict |
|---|---|---|---|---|
| **PD-1** | 13.5.0 | `step-05-adversarial-review.md` deleted; `step-04-self-check.md` `nextStepFile` → `'./step-06-resolve-findings.md'`; create-story `<critical>` block at `instructions.xml:21+`; `feedback_adversarial_review.md` PD-1 forward-pointer; `dev.md:64` `CR` "Standard cadence: run after each story-close, in fresh context" | `ls .../step-05-adversarial-review.md` → "No such file"; `grep nextStepFile step-04-self-check.md` → `./step-06-resolve-findings.md`; `grep "PD-1.*Story 13.5.0" feedback_adversarial_review.md` → ≥1; **refined grep** on 13.5-{1..5}-*.md (excluding negation-context boilerplate "do NOT enumerate"/"structurally cannot contain") → **0 matches**; `grep "after each story-close\|CR command" dev.md` → ≥1 | **PASS** |
| **TD-1** | 13.5.1 | per-FCB `fcb_dirty` parallel-array (10 sites in `file_access.asm`) | `grep "fcb_dirty\b" file_access.asm` → matches at :161 (DS), :228, :231 (init zero), :278, :290, :372, :378, :596, :603, :699; REPL test 944 (T-S1351-P1-RW-MID-READ) PASS | **PASS** |
| **TD-2** | 13.5.1 | `file_flush` mode-aware gating on `fcb_dirty`; REPOSITION-FILE auto-flush before pos-update | `file_access.asm:596,603` flush-then-clear-dirty; REPL tests 945 (P2-REPOS-COMMITS) + 946 (P3-RW-MIXED) PASS | **PASS** |
| **TD-3** | 13.5.2 | `file_byte_read` helper-layer rewrite; clean-EOF arm returns ior=0 (vs error arm A>1) per ANS §11.6.1.2080 | `grep file_byte_read file_access.asm` → :112, :157, :345, :682, :708 (entry), :857; ANS §11.6.1.2080 inline citation at :1813,1820,1876; REPL tests 949 (EOF-EXHAUST), 950 (EOF-CROSSREC), 951 (INCLUDED-EOF) PASS | **PASS** |
| **TD-4** | 13.5.1 | W/O probe coverage (round out FCB-pool stress matrix) | REPL tests 947 (P4-WO-AUTOFLUSH) + 948 (P5-RO-DEFENCE) PASS | **PASS** |
| **TD-5** | 13.5.3 | `."` BC-preservation envelopes (`PUSH BC`/`POP BC` around interpret-mode parse-and-print path) | `strings.asm:805..823` (Story 13.5.3 / TD-5 closure block citation), :860 (`PUSH BC ; save TOS`), :923 (`POP BC ; restore TOS`); REPL tests 952..955 PASS; **ad-hoc** `1 2 3 ." hello" .S` → `hello<3> 1 2 3` (BC=TOS=3 preserved) | **PASS** |
| **TD-6** | 13.5.4 | real `PAD` DEFCODE at `memory.asm:147`; returns HERE+`PAD_OFFSET` (84 bytes) per §6.2.2000 | `grep '"PAD"' src/*.asm` → `memory.asm:147 — DEFCODE "PAD", 0`; `compliance.md:354` "**14** of 46 …"; REPL tests 956..959 PASS | **PASS** |
| **TD-7** | 13.5.5 | user-facing `SAVE-INPUT` (§6.2.2182) + `RESTORE-INPUT` (§6.2.2148) DEFCODEs in `outer_interpreter.asm` | `grep '"SAVE-INPUT"\|"RESTORE-INPUT"' outer_interpreter.asm` → :489, :550 (2 DEFCODE sites); `compliance.md:354` "**14** of 46 …"; REPL tests 960..964 PASS; **ad-hoc** `: T1355 SAVE-INPUT RESTORE-INPUT . ; S" T1355" EVALUATE` → `0` (clean restore) | **PASS** |

**Aggregate: 8/8 PASS.** No FAIL or HALT verdict. **Note on PD-1 refined grep**: the literal pattern `trigger an adversarial review|adversarial review pass` returns 6 matches across 13.5.{1..5}-*.md, but ALL six are negation-context boilerplate — the SAME sentence repeated in each story's "What this story does NOT do" / "Adversarial review note" section ("this story's ACs do NOT enumerate 'trigger an adversarial review pass' — the `CR` command runs separately in fresh context"). The structural enforcer DID hold; the spec's grep was overly broad. Refined grep `| grep -vE "do NOT enumerate|structurally cannot contain|story's ACs structurally"` → 0 matches, confirming enforcer integrity.

#### Task 2 — Epic-13.5 + Phase-2 cumulative ROM trajectory accounting (AC #2)

| Story | Pre | Post | Δ | Story-file citation |
|---|---|---|---|---|
| 13.5.0 (PD-1) | 24,694 | 24,694 | 0 | `13.5-0-…md` Task 9 (workflow + doc + memory + agent-def only) |
| 13.5.1 (TD-1+TD-2+TD-4) | 24,694 | 24,804 | +110 | `13.5-1-…md` Change Log (data +8 + code +102) |
| 13.5.2 (TD-3) | 24,804 | 24,834 | +30 | `13.5-2-…md` Change Log (net of code-review −9) |
| 13.5.3 (TD-5) | 24,834 | 24,836 | +2 | `13.5-3-…md` Change Log (data 0 + code +2) |
| 13.5.4 (TD-6) | 24,836 | 24,862 | +26 | `13.5-4-…md` Change Log |
| 13.5.5 (TD-7) | 24,862 | **24,996** | **+134** | `13.5-5-…md` Task 7 — **see correction note below** |
| 13.5.6 (this story) | 24,996 | 24,996 | 0 | audit-only |

Per-story sum: 0 + 110 + 30 + 2 + 26 + 134 + 0 = **+302 absolute**. Reconcile against current binary: 24,996 − 24,694 = **+302** ✓ (zero residual; mirror Story 13.6 Task 5.2 reconciliation discipline).

**13.5.5 documentation-drift reconciliation (in-pass-fix per AC #13).** Story 13.5.5's close-out documentation recorded the post-fix binary as **25,002 bytes (Δ = +140)** at `13.5-5-…md:275, :640, :666, :722`. The current `wc -c build/antforth.com` against the committed HEAD (`97a57d6`) reports **24,996 bytes (Δ = +134)** — a 6-byte negative drift in the recorded close-out figure versus the actual committed binary. This was confirmed by clean-rebuild from `git checkout HEAD~1` (post-13.5.4 baseline = 24,862 bytes ✓ — matches Story 13.5.4 close exactly) and HEAD (post-13.5.5 = 24,996, NOT 25,002). The drift is consistent with a mid-pass code-review compaction landed *after* the dev-pass measurement was inscribed in the story file. Story 13.5.6's verdict-walk uses the **actual** measured numbers, per `feedback_systematic_reference_check.md` ("cross-reference the authoritative manual, not enumerate from memory" — here the manual is the binary itself).

**Phase-2 + cleanup-slate cumulative (production binary):** 14,030 → **24,996** = **+10,966 bytes (+78.2%)**. Per-epic sum: +757 (Epic 9) + +1,985 (Epic 10) + +653 (Epic 11) + +116 (Epic 11.5) + +689 (Epic 12) + +6,464 (Epic 13) + **+302** (Epic 13.5) = **+10,966** — exact match against absolute, zero residual.

**Filesanity-only delta:** 26,010 → 26,461 = +451 bytes (the +6 vs +457 expected reconciles the same 13.5.5 6-byte drift — filesanity inherits the production-binary fix and the same compaction).

**Justification framing recorded:** Epic 13.5 is **debt discharge, not feature work** (per `epics.md:1711` byte-budget gate). +302 bytes for: per-FCB dirty-flag (TD-1+2+4, +110), helper-layer rewrite for EOF/error disambiguation (TD-3, +30), `."` BC-preservation (TD-5, +2), real PAD word (TD-6, +26), user-facing SAVE-INPUT / RESTORE-INPUT (TD-7, +134). Every byte maps to a tag-blocker discharge per S8 standing commitment.

#### Task 3 — `make test-repl` regression block (AC #3)

- Pre/post `make test-repl`: **973 PASS / 0 FAIL** (`grep -cE '^PASS:'` = 973; `grep -cE '^FAIL:'` = 0). Audit-only — zero test-count movement, as required.
- `make test` (assembly thread): clean. `Pass 1/2 complete (0 errors)`, `compiled: 30598 lines, 0.097s`, `PASS: Output matches expected`.
- `make test-file-sanity`: `PASS: file-sanity test — 12 expected lines match exactly (Story 13.5.2 H1: .fbr_eof tri-state discriminator probe)`.
- 21 cleanup-slate tests (944..964) all PASS individually — Task 1's per-TD evidence rows cite specific test IDs.

#### Task 6 — Per-TD probe pinpoint re-verification (AC #6)

| TD | Anchor probe | Result | Source |
|---|---|---|---|
| TD-1 | T-S1351-P1-RW-MID-READ (R/W mid-read FILE-POSITION accuracy) | PASS | REPL test 944 in `make test-repl` |
| TD-2 | T-S1351-P2-REPOS-COMMITS + T-S1351-P3-RW-MIXED | PASS / PASS | REPL tests 945 + 946 |
| TD-3 | T-S1352-P1-EOF-EXHAUST + P2-EOF-CROSSREC + P3-INCLUDED-EOF | PASS / PASS / PASS | REPL tests 949..951 |
| TD-4 | T-S1351-P4-WO-AUTOFLUSH + P5-RO-DEFENCE | PASS / PASS | REPL tests 947 + 948 |
| TD-5 | T-S1353-P1..P4 + ad-hoc `1 2 3 ." hello" .S` | PASS (×4) + `hello<3> 1 2 3` (BC preserved) | REPL tests 952..955 + iz-cpm direct probe |
| TD-6 | T-S1354-P1..P4 (PAD-defined / cross-line / READFILE-cycle / volatility) | PASS / PASS / PASS / PASS | REPL tests 956..959 |
| TD-7 | T-S1355-P1..P5 + ad-hoc `: T1355 SAVE-INPUT RESTORE-INPUT . ; S" T1355" EVALUATE` | PASS (×5) + `0` (clean-restore flag) | REPL tests 960..964 + iz-cpm direct probe |

Each `make test-repl` probe is itself a `printf | iz-cpm | grep` invocation; aggregated PASS verdicts ARE the per-TD anchor verification. Two ad-hoc spot-checks (TD-5 BC-preservation in interactive REPL; TD-7 EVALUATE round-trip clean-restore flag) confirm the cleanup-slate words ACTUALLY EXERCISE their claimed defect rather than passing trivially in the harness.

#### Task 7 — Standing-commitment audit (S1 + S5 + S8 + S9 + S10) (AC #7)

| Standing commitment | Evidence | Verdict |
|---|---|---|
| **S1** (in-pass adversarial review removed; `CR` in fresh context) | refined grep on 13.5-{1..5}-*.md excluding negation-context → **0 matches**; structural enforcer in `instructions.xml:21+` held across all five subsequent stories | **PASS** |
| **S5** (HALT on PARTIAL; no sibling-story spawn) | `ls _bmad-output/implementation-artifacts/ \| grep -E "13\.5-[0-5]-1\b\|13\.5-[0-5]\.1-"` → 0 matches; no `13.5.x.1`-style sibling story exists; HALT references in 13.5.x stories are anticipated AC HALTs or project-lead-surface byte-budget framings (e.g., 13.5.5 +40 acceptance) | **PASS** |
| **S8** (no "pre-existing" / "out-of-scope" discharge for correctness) | refined grep excluding rule-citation context → 0 actual violations; all 8 surface matches are rule-assertion sentences ("may NOT be discharged with 'accepted-with-rationale: pre-existing'") + label-naming context ("`.dq_interpret` and `.dq_i_end`, both pre-existing labels") + Lesson-13-B citations | **PASS** |
| **S9** (mid-epic hardware-smoke cadence mandatory) | `grep -lE "Hardware smoke.*PASS\|hardware run.*PASS" 13.5-{1..5}-*.md` → **5/5 binary-delta stories** carry hardware-smoke PASS verdict; PD-1 (13.5.0) documents the S9 exemption explicitly at `13.5-0-…md:111,182` ("S9 applies to stories with binary delta; PD-1 is workflow + doc only") | **PASS** |
| **S10** (workflow > memory > prompt; structural fixes in workflow files) | same evidence as S1 — PD-1's structural fix (file deletion + `<critical>` block) held through 13.5.1..13.5.5 dev-passes; no story re-introduced an in-pass adversarial review pattern via memory or prompt | **PASS** |

**5/5 PASS.** Epic 13.5 process-recovery vehicle delivered its intended outcome.

#### Task 8 — NFR17/CCD-3 standards-citation audit (AC #8)

| Story | Word/Site | Citation | Verdict |
|---|---|---|---|
| 13.5.4 (TD-6) | PAD | `memory.asm:133 — ; PAD ( -- c-addr )           ANS Forth 1994 §6.2.2000` | **OK** |
| 13.5.5 (TD-7) | RESTORE-INPUT | `outer_interpreter.asm:512 — ; RESTORE-INPUT ( xn ... x1 n -- flag )          ANS Forth 1994 §6.2.2148` (+ inline §6.2.2148 ambiguous-condition citations at :516, :527, :588) | **OK** |
| 13.5.5 (TD-7) | SAVE-INPUT | `outer_interpreter.asm:463 — ; SAVE-INPUT ( -- xn ... x1 n )                  ANS Forth 1994 §6.2.2182` (+ EVALUATE-arm §6.2.2182/§6.2.2148 cross-citation at :471) | **OK** |
| 13.5.2 (TD-3) | READ-FILE EOF/error | `file_access.asm:1813 — ; READ-FILE … ANS Forth 1994 §11.6.1.2080`; :1820 inline EOF-vs-error citation; :1876 clean-EOF ior=0 citation | **OK** |
| 13.5.1 (TD-1+2+4) | fcb_dirty parallel-array | structural decision — `file_access.asm:161, 228, 231, 278, 290, 372, 378, 596, 603, 699` (Story 13.5.1 inline citations naming the TD-1/2/4 closures) | **OK** (structural; no new standards word) |
| 13.5.3 (TD-5) | `."` BC-preservation | structural decision — `strings.asm:805..823` (Story 13.5.3 / TD-5 closure block), :860, :923 inline citations | **OK** (structural; no new standards word) |
| docs | `compliance.md:354` leading sentence | "**14** of 46 DPANS94 §6.2 Core Extension words are implemented (SAVE-INPUT + RESTORE-INPUT added by Story 13.5.5 — TD-7 closure 2026-05-06; PAD added by Story 13.5.4 — TD-6 closure 2026-05-06)" | **OK** |
| docs | `compliance.md:344, 363, 365, 367` cross-refs | `ENVIRONMENT?` /PAD query row updated with TD-6 closure; PAD row at :363 with §6.2.2000 cite; RESTORE-INPUT at :365 with §6.2.2148; SAVE-INPUT at :367 with §6.2.2182 | **OK** |

**8 rows / 8 OK / 0 MISSING / 0 WRONG.** No in-pass citation comment-only fixes required.

#### Task 9 — Register-convention preservation audit (AC #9)

| Row | Convention | Spot-check evidence | Verdict |
|---|---|---|---|
| TD-3 `file_byte_read` | FCB-ptr preservation across BDOS calls | `file_access.asm:709 PUSH DE` / `:734 POP DE` / `:735 PUSH DE` envelope around `bdos_set_dma` + `bdos_read_seq` (PUSH/POP convention; spec's EXX-count guidance assumed a pattern that never existed in `file_access.asm` — actual preservation is via PUSH/POP DE, structurally honoured by the helper-layer rewrite) | **PASS** |
| TD-5 `."` | BC-as-TOS preservation across interpret-mode work | `strings.asm:860 PUSH BC ; save TOS across interpret-mode work (Story 13.5.3 / TD-5)` + `:923 POP BC ; restore TOS (Story 13.5.3 / TD-5)`; ad-hoc `1 2 3 ." hello" .S` → `hello<3> 1 2 3` confirms BC=TOS=3 preserved | **PASS** |
| TD-6 PAD body | DEFCODE TOS-in-BC convention | `memory.asm:148 PUSH BC ; Save old TOS` + `:154-155 LD B,H / LD C,L ; BC = PAD (new TOS)` — standard DEFCODE pattern | **PASS** |
| TD-7 SAVE-INPUT body | TOS-in-BC + 4 × `LD L,(IY+UA.x); LD H,(IY+UA.x+1); PUSH HL` + final count = `LD BC, 4` | `outer_interpreter.asm:494-509` matches Story 13.5.5 Task 3 sketch verbatim; RESTORE-INPUT body inverse | **PASS** |
| High-on-TOS double-cell convention (Story 13.0.1) | no Epic-13.5 word pushes doubles in user-visible form | TD-7 SAVE-INPUT pushes 5 SINGLE cells (count, source_id, >IN, tib_len, tib_addr); other 13.5.x words don't manipulate doubles at user boundary | **PASS** |

**5 rows / 5 PASS.** No new register-convention guidance added to `docs/register-conventions.md` (Epic 13.5 is debt discharge per the spec).

#### Task 10 — Documentation currency audit (AC #10)

| Check | Result |
|---|---|
| `compliance.md:354` "**14** of 46" leading sentence with PAD + SAVE-INPUT + RESTORE-INPUT attribution | **OK** — verbatim |
| 13.5.4 PAD caveats sub-bullet | **OK** — present at `compliance.md:600` ("Story 13.5.4 caveats (TD-6 closure 2026-05-06):") |
| 13.5.5 SAVE-INPUT/RESTORE-INPUT caveats sub-bullet | **OK** — present at `compliance.md:690` ("Story 13.5.5 caveats (TD-7 closure 2026-05-06):") |
| §6.1 Core 133/133 = 100% claim unchanged by Epic 13.5 | **OK** — `compliance.md:41,72` (cleanup-slate added §6.2 entries; Core stays 100%) |
| `docs/WISHLIST.md` Phase-3 §-by-§ Core re-audit entry unchanged | **OK** — `WISHLIST.md:103` ("# Phase-3 systematic §-by-§ ANS Forth Core re-audit") still carry-forward |
| Cross-references at `compliance.md:301, 344, 363, 365, 367` | **OK** — EVALUATE row mentions Story 13.5.5 user-facing surfaces; ENVIRONMENT? row mentions `/PAD` query Story 13.5.4 closure |

**6/6 OK.** No documentation-drift fixes required (drift was confined to Story 13.5.5's own close-out narrative; Task 2 reconciliation handled it).

#### Task 11 — Byte-budget verification: zero data, zero code (AC #11)

| Binary | Pre-edit | Post-edit | Δ |
|---|---|---|---|
| `build/antforth.com` (production) | 24,996 | 24,996 | **0** |
| `build/antforth_filesanity.com` | 26,461 | 26,461 | **0** |

Two-number gate per PD-13 idiom: data Δ 0 / code Δ 0 / total Δ 0. Audit-only confirmed; no `src/*.asm` instruction changes.

**Spec absolute-number reconciliation:** AC #11 anticipates 25,002 / 26,467 (inherited from 13.5.5 close-out doc). Actual is 24,996 / 26,461 — both 6 bytes lower, matching the 13.5.5 documentation drift documented in Task 2. **The audit-only Δ = 0 invariant is satisfied** (this story added no bytes); the 6-byte absolute gap is reconciled to the actual binary in Task 2.

#### Task 12 — Identifier-gate scan (AC #12)

- `git diff --no-color | grep -niE '^\+.*\b(hack|workaround|fixme|tmp)\b'` → **0 matches**.
- No new identifiers in `src/` (audit-only diff scope: this story file + sprint-status.yaml + memory edits).
- No sibling-story spawn: `ls _bmad-output/implementation-artifacts/ | grep -E "13\.5-6-1\|13\.5-6\.1"` → 0 matches.

#### Task 13 — HALT-on-PARTIAL discipline check (AC #13)

Walk every AC at story-close:
- ACs #1, #3, #6, #7, #8, #9, #10, #12 → audit-side PASS, no HALT.
- ACs #2, #11 → audit-side PASS with in-pass-fix (use actual measured numbers; document the 13.5.5 6-byte close-out drift); no HALT (the drift is inherited from a prior story's close-out, not a 13.5.6 regression; per S5, in-pass-fix discipline applies; per `feedback_systematic_reference_check.md`, the binary itself is the authoritative manual).
- AC #4 + #5 → require project-lead hardware action; **dev-pass naturally pauses** here for hardware re-run on the 24,996-byte post-cleanup binary, mirroring Story 13.6's hardware-pause pattern (close-out gates 9.6 / 10.10 / 11.8 / 11.5.7 / 12.6 / 13.6 all paused for hardware action). This is NOT a HALT — it's the spec'd hardware dependency.
- AC #14, #15, #16, #17 → finalised post-hardware (memory currency #14 done; tag proposal in #15 below; sprint-status flips in #16; milestone in #17).

**Explicit "no HALT triggered" line.** No partial ship, no sibling-story spawn, no scope creep. Standing commitment S5 held.

#### Task 14 — Memory-currency audit (AC #14)

- `~/.claude/projects/-home-ant-src-microbeast-antforth/memory/project_phase2_scope.md` updated:
  - `description` field flipped from "Phase-2 dev-side closed 2026-05-04 at Story 13.6; v2.0.0 tag DEFERRED post-Epic-13 retro" → "Phase 2 + cleanup slate CLOSED 2026-05-06 at Story 13.5.6; v2.0.0 tag candidate ready for project-lead application (production binary 24,996 bytes / 973 PASS / hardware run pending Story 13.5.6 release-gate)".
  - Body: status paragraph rewritten to reflect Story 13.5.6 close-out + Tag-Blocking Slate verdict-table 8/8 PASS + tag-application gate state.
  - Tag-blocking slate list rewritten to **DISCHARGED** form with per-story attribution, cumulative ROM (+302 bytes / +10,966 vs 14,030), and the 13.5.5 documentation-drift reconciliation.
  - "How to apply" paragraph updated to post-2.0 carry-forward framing (hardware vocabulary, beginner's guide, §-by-§ Core re-audit).
- `~/.claude/projects/-home-ant-src-microbeast-antforth/memory/MEMORY.md` index line for `project_phase2_scope.md` updated synchronously: "Phase 2 + cleanup-slate CLOSED 2026-05-06 at Story 13.5.6 (PD-1 + TD-1..TD-7 discharged; binary 24,996 / 973 PASS); v2.0.0 tag candidate awaiting Story 13.5.6 release-gate hardware re-run".
- Spot-check on other Phase-2-spanning memories (`project_assembler_keep_assembly.md`, `project_asm_hash_dispatch_hack.md`, `project_epic_11_5_scope.md`, `project_epic4_scope.md`, `project_epic5_scope.md`, `project_hardware_crash_audit.md`, `project_tos_in_register.md`, `project_epic12_redraft_required.md`): no edits required — all describe historical state correctly.
- Phase-3 plan memory deferred to project-lead retrospective (not 13.5.6 scope).

#### Task 4 — On-device round-trip (PRD Journey 1) on real MicroBeast — RELEASE GATE (AC #4) — PASS

Hardware run on real MicroBeast / CP/M 2.2 / SD-backed by project lead 2026-05-06; transcript at `~/Downloads/beastty-13-5-6-20260506-225911.bin` (3,201 bytes, ASCII). Binary under test: `build/antforth.com` = 24,996 bytes (post-Story-13.5.5 + post-Story-13.5.6-audit-pass; clean rebuild from HEAD `97a57d6`).

Adapted bench-equivalent from spec: `: BLINK 65 EMIT 66 EMIT 67 EMIT CR ;` (prints `ABC` then CR — pure ASCII, transcript-unambiguous; no `0xF0` LED-port assumption needed).

**5-step round-trip verbatim from transcript:**

1. **Define + exercise**: `: BLINK 65 EMIT 66 EMIT 67 EMIT CR ;` → ` ok`. `BLINK` → `ABC` then ` ok`. Word defined and works in current dictionary.
2. **Save source via WRITE-FILE**: `S" BLINK.FTH" DELETE-FILE DROP` → ` ok`; `S" BLINK.FTH" R/W CREATE-FILE THROW` → ` ok`; `DUP S" : BLINK 65 EMIT 66 EMIT 67 EMIT CR ;" ROT WRITE-FILE THROW` → ` ok`; `CLOSE-FILE THROW` → ` ok`; `S" T4-A=ok " TYPE CR` → `T4-A=ok ` then ` ok`. Source bytes committed to B:BLINK.FTH on real CP/M.
3. **Cold-restart**: `BYE` → CCP prompt; `B>antforth` → fresh `AntForth v1.12.0 (C) ant.org 2026 / MicroBeast - 30562 bytes free / Type BYE to exit`. **Confirm dictionary is fresh**: `BLINK` → `BLINK ?` / `error -13: undefined word`. ✓ — `BLINK` is gone after cold-restart.
4. **INCLUDE the saved file**: `S" BLINK.FTH" INCLUDED` → ` ok`. The .FTH file's contents were re-parsed by the outer interpreter; the colon definition re-installed itself in the fresh dictionary.
5. **Exercise the restored word**: `BLINK BLINK BLINK` → `ABC` / `ABC` / `ABC` then ` ok`. **Round-trip works end-to-end on real CP/M 2.2 hardware.** Cleanup: `S" BLINK.FTH" DELETE-FILE THROW` → ` ok`; `S" T4-C=ok " TYPE CR` → `T4-C=ok ` then ` ok`.

**Verdict: PASS — RELEASE GATE.** PRD User Journey 1 (Mo's on-device session, `prd.md:172-180`) re-verified on the post-cleanup binary. The dev/test/persist loop on the MicroBeast — define a word → save source → reboot → INCLUDE → exercise — works end-to-end. This is the **central capability promise** of antforth 2.0 and it is hardware-validated post-Tag-Blocking-Slate cleanup.

**Probe-authoring sidenote (not v2.0.0-blocking):** the project lead initially typed `WRITE-LINE` (typo for `WRITE-FILE`) and `THROS` (typo for `THROW`) at step 2; both surfaced as `error -13: undefined word`, the project lead corrected, and the round-trip proceeded cleanly. These are typing-flow artefacts visible in the verbatim transcript, not kernel issues.

#### Task 5 — 12-line FS smoke batch (+ TD-5 / TD-6 / TD-7 spot-checks) on real MicroBeast — RELEASE GATE (AC #5) — PASS

Hardware run by project lead 2026-05-06 (run 1) + cleanup re-run 2026-05-07 (run 2 — for two probe-authoring bugs surfaced in run 1; both confirmed kernel-sound). Transcript: `~/Downloads/beastty-13-5-6-20260506-225911.bin` (run 1) + run-2 inline-paste in dev-pass conversation. Binary under test: 24,996 bytes (same as Task 4).

Story 13.6 Task 10 canonical repaired-v2 12-line smoke batch (`13-6-…md:1027..1083`) inherited verbatim by reference; appended TD-5 / TD-6 / TD-7 spot-checks as lines 13 / 14 / 15.

| Step | Probe | Hardware output | Verdict |
|---|---|---|---|
| Setup | `VARIABLE FA  CREATE BUF 16 ALLOT` | ` ok` | PASS |
| 1 | pre-clean + `CREATE-FILE` | ` ok` (THROW with ior=0; FA = fid) | PASS |
| 2 | WRITE-FILE | `T2=ok ` | PASS |
| 3 | CLOSE-FILE | `T3=ok ` | PASS |
| 4 | re-open R/O | `T4=ok ` | PASS |
| 5 | READ-FILE 6 into BUF | `T5=u2=6 ` | PASS — u2=6 bytes read, ior=0 |
| 6 | TYPE BUF | `T6=Hello!` | **PASS** — content byte-identical on real CP/M; F-9 (Story 13.6) script-bug fix (CREATE BUF named buffer) holds across cleanup-slate surface |
| 7 | CLOSE-FILE | `T7=ok ` | PASS |
| 8 | DELETE-FILE | `T8=ok ` | PASS |
| 9 | INCLUDE B:NESTED.FTH | `error -38: file not found` | **INCONCLUSIVE-acceptable** — disk-corpus prerequisite (`B:NESTED.FTH` absent from project lead's B: drive; same as Story 13.6 run-3 disposition; not 2.0.0-blocking) |
| 10 | FCB pool exhaust | `T10=-69 ` | **PASS** — FCB-pool exhaustion correctly caught at slot 9 (Story 13.2 t4 / closure 939 contract preserved on hardware) |
| 11 | R/O destructive-flush invariant (run 2) | `T11=SZ=128` | **PASS** — Story 13.5 mode-aware `file_flush` + `fcb_has_written` discipline + Story 13.5.1 per-FCB `fcb_dirty` discipline holds on real CP/M 2.2 BDOS; record-aligned 128 bytes after a 2-byte write |
| 12 | i\*x preservation across CATCH/ABORT | `T12=-1 3 2 1 ` | **PASS** — Story 11.4.1 contract preserved across full Epic-13 + Epic-13.5 surface on real CP/M |
| **13 (TD-5)** | `1 2 ." hello" .S` | `T13=hello<2> 1 2 ` | **PASS** — TD-5 `."` BC-preservation verified on real hardware; `hello` printed inline + `.S` shows `<2> 1 2` (depth-2 stack with 1, 2 intact — BC=TOS=2 not clobbered by interpret-mode `."`) |
| **14 (TD-6)** | `S" hi" PAD SWAP MOVE PAD 2 TYPE` (run 2) | `T14=hi` | **PASS** — TD-6 PAD verified on real hardware; the new `PAD` DEFCODE at `memory.asm:147` returns a usable c-addr at HERE+84, MOVE writes to it, TYPE reads back the same bytes |
| **15 (TD-7)** | `: T1355 SAVE-INPUT RESTORE-INPUT . ; S" T1355" EVALUATE` | `T15=0 ` | **PASS** — TD-7 SAVE-INPUT/RESTORE-INPUT round-trip during EVALUATE returns flag = 0 (clean restore) per §6.2.2148; new user-facing CORE-EXT words at `outer_interpreter.asm:489, 550` work on real CP/M |

**Aggregate: 14/15 PASS (T1..T8, T10..T15) + 1/15 INCONCLUSIVE-acceptable (T9 disk-corpus).** No FAIL. Mirror Story 13.6 run-3 verdict shape (11/12 PASS / 1/12 INCONCLUSIVE T9 same disposition). **Verdict: PASS — RELEASE GATE.**

**Probe-authoring sidenotes from run 1 (in-pass-fixed in run 2; mirror Story 13.6 F-9/F-10/F-11 disposition — script bugs caught on hardware, kernel sound):**
- **Run-1 T11 cascade**: step 10 leaves all 8 P1..P8 R/W FCBs allocated; step 11's first DELETE-FILE needs an FCB → `-69` (pool exhausted by step 10's leftover slots). Cascading `error -69` / `error 2` / `error -70` followed. **The `-69` is correct kernel behaviour** — pool exhaustion correctly blocks new ops. Run-2 fix: `BYE` + relaunch (fresh FCB pool) before re-running step 11. Kernel sound; smoke-batch authoring discipline needs cleanup between step 10 and step 11 — recommendation for Story 13.6 / 13.5.6 batch text update (post-2.0 carry-forward) is to insert a `BYE` + relaunch separator OR an explicit P*.TXT close/delete sequence between 10 and 11. Not 2.0.0-blocking — pool-exhaustion handling is hardware-validated by step 10's PASS.
- **Run-1 T14 CMOVE**: dev-pass-authored spot-check used `CMOVE` (DPANS94 §6.2.0945), but antforth implements `MOVE` (`memory.asm:292`, ANS §6.1.1900) and not `CMOVE`. `CMOVE ?  error -13: undefined word` is correct dictionary-lookup behaviour for an undefined word. Run-2 fix: replaced `CMOVE` with `MOVE`; works as expected (`T14=hi`). Kernel sound; the dev-pass probe-authoring picked the wrong word from the dual MOVE/CMOVE ANS pair.

Both run-1 issues are mirror of Story 13.6 hardware-run-1 + run-2 / run-3 sequence (script bugs F-9 / F-10 / F-11 surface on hardware; in-pass-fixed in repaired-v2). Cumulative dev-pass-authoring discipline pattern: when a probe targets an FCB-pool boundary (step 11 after step 10), and the test-runner harness `make test-repl` doesn't replicate cross-line FCB-pool state because each test runs fresh, the smoke batch needs explicit pool reset between probes.

**Hardware transcript provenance:** run 1 = `~/Downloads/beastty-13-5-6-20260506-225911.bin` (3,201 bytes; covers Task 4 + Task 5 lines 1..15 with run-1 issues); run 2 = inline paste in dev-pass conversation 2026-05-07 (covers Task 5 line 11 cleanup re-run + line 14 with `MOVE`). **Open: pre-tag transcript-capture deliverable.** The run-2 inline paste is a durability gap relative to run-1 (file vs. conversation log) and the AC #4/#5 capture discipline mirrored from Story 13.6 Task 9.5. Project-lead action before applying `git tag -a v2.0.0`: append the run-2 inline content to `~/Downloads/beastty-13-5-6-20260506-225911.bin` (or a sibling `…-20260507-HHMMSS.bin`) so the binding T11=SZ=128 + T14=hi outputs survive in a durable artefact. The verbatim run-2 outputs are recorded in the per-line Verdict table in Task 5 above (`T11=SZ=128`, `T14=hi`); this satisfies the story file's evidence-durability requirement, but the raw on-device transcript provenance for those two lines remains in the conversation log only until the project lead snapshots it.

#### Task 15 — Tag-Blocking Slate Verdict + v2.0.0 Tag Gate verdict + tag proposal (AC #15)

The Tag-Blocking Slate Verdict + v2.0.0 Tag Gate table at the **top of Completion Notes** above this section summarises 12 gates (one row per AC #1..#11 + the audit/hardware totals) — **12/12 PASS**. The release-readiness one-liner there: **antforth 2.0 RELEASE-READY — tag v2.0.0**. The tag proposal command line is recorded there:

```
git tag -a v2.0.0 -m "Phase 2 + cleanup slate: ANS Core 100% + CATCH/THROW + Search-Order + File-Access + on-device source development + Tag-Blocking Slate cleanup"
```

Project lead applies the tag (mirror Stories 9.6 / 10.10 / 11.8 / 11.5.7 / 12.6 / 13.6 disposition — dev agent does NOT apply). Recommended timing: after Story 13.5.6 review → done flip (post-CR close); on commit `97a57d6` (the post-13.5.5 commit, since Story 13.5.6 is audit-only with zero binary delta — the tag should land on the commit that produced the 24,996-byte binary, which is `97a57d6`); OR on a new dev-pass commit if Story 13.5.6's documentation edits are committed separately — project-lead choice. The `-m` message is dev-pass-finalised; project lead may rephrase before applying.

**Phase-2 + cleanup-slate milestone marker** (full marker also recorded in Task 17 below):
- **FR1-FR47 delivered** (FR30 deliberately gapped per `epics.md:1037-1075`; Story 11.5.5 PD-2 disposition; carry-forward to post-2.0).
- **NFR1-NFR21 satisfied or carry-forward-documented** per `13-6-…md` AC #8 + AC #1(e); unchanged by Epic 13.5.
- **All seven Tag-Blocking Slate items discharged**: PD-1 (13.5.0) + TD-1+TD-2+TD-4 (13.5.1) + TD-3 (13.5.2) + TD-5 (13.5.3) + TD-6 (13.5.4) + TD-7 (13.5.5) — verdict-table 8/8 PASS at AC #1.
- **Public release tag candidate**: v2.0.0 per `prd.md:262`.
- **Post-tag opportunities** (per `prd.md:142-147`; none 2.0-tag-blocking):
  - MicroBeast hardware vocabulary epic (LED / GPIO / RTC / display direct-driver words).
  - Beginner's guide (introductory tutorial; first user-facing docs).
  - Per-wordset reference manual (DPANS94 §6.1, §6.2, §11, §13, §16, §15 cross-walk).
  - Worked examples corpus (CP/M utilities written in antforth; persistence-driven).
  - §-by-§ Core re-audit (`docs/WISHLIST.md:103` carry-forward — DPANS94 chapter-by-chapter walk vs. word-by-word).
  - Disk-full hardware re-verification (Story 13.6 AC #1(e); not v2.0.0-blocking; project-lead-disposition post-tag).
  - Story 13.6 / 13.5.6 12-line FS smoke batch authoring-discipline update (insert FCB-pool reset between step 10 and step 11; lessons from run-1 cascade).

#### Task 17 — Phase-2 + cleanup-slate milestone marker (AC #17)

Recorded in Task 15 above (deliverable (d) of AC #15). Verbatim re-statement of the FR / NFR / Tag-Blocking Slate / tag candidate / post-tag-opportunities list per AC #17 (a)..(e):

- (a) **FR1-FR47 delivered.** FR30 deliberately gapped per Story 11.5.5 closure / `epics.md:1037-1075` carry-forward decision (lazy-load capstone removed in 2026-04-20 re-scope per `project_phase2_scope.md`). All other FRs implemented and verified across Stories 9..13.5.5. Confirmed against `prd.md` per Story 13.6 Task 13.4.
- (b) **NFR1-NFR21 satisfied or carry-forward-documented** per `13-6-…md` AC #8 (§-level Core compliance — §6.1 Core 133/133 = 100%; §6.2 Core Extension 14/46 = 30.4% with PAD + SAVE-INPUT + RESTORE-INPUT added by 13.5.4 + 13.5.5) and AC #1(e) (disk-full hardware-deferred — carry-forward post-2.0 project-lead-disposition). Unchanged by Epic 13.5.
- (c) **All 7 Tag-Blocking Slate items discharged** per AC #1 verdict-table 8/8 PASS.
- (d) **Public release tag candidate**: `v2.0.0` per `prd.md:262`.
- (e) **Post-tag opportunities** per `prd.md:142-147`: MicroBeast hardware vocabulary, beginner's guide, per-wordset reference, worked examples, §-by-§ Core re-audit (`WISHLIST.md:103`), disk-full hardware re-verification (`13-6-…md` AC #1(e)). None are 2.0-tag-blocking.

#### Task 18 — Parent ↔ subtask discipline check (AC #13)

Re-scanned the Tasks/Subtasks section at story-close: 18 parent tasks, all marked `[x]`; every subtask under each parent marked `[x]`. **No parent-checked-with-unchecked-subtask.** Story 13.4 v2 AC #28 discipline held — no HIGH finding here.

### File List

**Story-13.5.6-pass edits (audit-only at dev-pass close; CR pass added a tag-prep banner bump — see CR Change Log row):**
- `_bmad-output/implementation-artifacts/13.5-6-epic-13-5-close-out-gate-and-antforth-2-0-tag.md` — this story file (Status flip, Completion Notes, evidence tables, verdict tables, File List, Change Log).
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — `13.5-6-…` row flipped `ready-for-dev → in-progress` at dev-pass start; → `review` at dev-pass close; → `done` at CR close; epic-13.5 `in-progress → done` at CR close per AC #16.

**Memory edits (audit-only; documentation-currency tweaks per AC #14):**
- `~/.claude/projects/-home-ant-src-microbeast-antforth/memory/project_phase2_scope.md` — front-matter description + status body + slate list updated to post-Epic-13.5 / v2.0.0-tag-candidate state.
- `~/.claude/projects/-home-ant-src-microbeast-antforth/memory/MEMORY.md` — index line for `project_phase2_scope.md` synchronised.

**CR-pass tag-prep edits (project-lead-directed during CR; broke audit-only invariant deliberately — banner must match the tag):**
- `src/antforth.asm` — `str_banner1` `"AntForth v1.12.0 (C) ant.org 2026"` → `"AntForth v2.0.0 (C) ant.org 2026"`; `STR_BANNER1_LEN` 33 → 32 (1-byte string shrink); comment at line 146 updated. Production binary 24,996 → **24,995** (Δ -1); filesanity 26,461 → **26,460** (Δ -1). All 973 REPL tests still PASS post-bump. Verified live banner via `printf 'BYE\r\n' | iz-cpm … build/antforth.com` → `AntForth v2.0.0 (C) ant.org 2026`. **Hardware transcript provenance note:** the 2026-05-06 / 2026-05-07 hardware runs (Tasks 4 + 5) captured `AntForth v1.12.0` because they ran on the pre-banner-bump 24,996-byte binary. The functional gates (PRD Journey 1 round-trip; 12-line FS smoke + TD-5/6/7) remain valid — the banner is a startup-only string with zero kernel-path interaction. Re-running on hardware purely to capture a v2.0.0-banner transcript is project-lead discretion (recommended but not gating).
- `Makefile` — REPL test 80 expected-string `'AntForth v1.12.0'` → `'AntForth v2.0.0'` (banner-version-string assertion synchronised).

### Memory Files Touched

- `project_phase2_scope.md` — major rewrite of status / slate / how-to-apply paragraphs; description field updated.
- `MEMORY.md` — index line for `project_phase2_scope.md` updated synchronously.

### Senior Developer Review (AI)

**Reviewer:** CR (claude-opus-4-7[1m]) in fresh context post-dev-pass close per S1 standing commitment + PD-1 closure (`_bmad/bmm/agents/dev.md:64`).
**Date:** 2026-05-07.
**Outcome:** Code review PASS — review remains pending the M2 hardware-transcript-capture deliverable. Findings: **0 HIGH, 3 MEDIUM, 4 LOW.** All 3 MEDIUM fixed in-pass; 4 LOW recorded as `[AI-Review]` follow-ups under Tasks/Subtasks above.

**Summary.** Story 13.5.6 is the Epic 13.5 close-out gate + v2.0.0 tag-application gate, audit-only in code shape. The dev pass executed every AC's verdict-walk with mechanical evidence (greps, `wc -c`, `make test-repl`, on-device transcripts) and the Tag-Blocking Slate Verdict + v2.0.0 Tag Gate top-table reports 12/12 PASS. CR re-verified the binding spot-checks: production binary 24,996 / filesanity 26,461 (Δ=0 from pre-edit ✓); `make test-repl` = 973 PASS / 0 FAIL with all 21 cleanup-slate tests 944..964 PASS individually (verified by re-run in CR context); PD-1 + TD-1..TD-7 source citations all present at the cited file:line locations (`memory.asm:133/147` PAD body + §6.2.2000 cite; `outer_interpreter.asm:463/489/512/550` SAVE-INPUT + RESTORE-INPUT bodies + §6.2.2182/§6.2.2148 cites; `file_access.asm:1813/1820/1876` READ-FILE §11.6.1.2080 cites; `strings.asm:860/923` `."` PUSH/POP BC envelopes; `file_access.asm:161/228/231/278/290/372/378/596/603/699` `fcb_dirty` parallel-array sites); workflow files (`step-05-adversarial-review.md` removed; `step-04-self-check.md` `nextStepFile: './step-06-resolve-findings.md'`; create-story `instructions.xml:20-31` `<critical>` block intact; `dev.md:64` "Standard cadence: run after each story-close, in fresh context" present); memory files (`project_phase2_scope.md` description + body rewritten to post-13.5.6 / v2.0.0-candidate state; `MEMORY.md` index synchronised). Substantive verdict-walk integrity confirmed.

**Findings detail.**

- **M1 (FIXED) — AC #1(a) PD-1 grep.** The literal grep specified in AC #1(a) returns 6 matches across `13.5-{1..5}-*.md`, not 0; all 6 are in negation-context boilerplate ("this story does NOT enumerate 'trigger an adversarial review pass'") that documents PD-1's structural enforcement, not violations of it. The dev pass refined the grep with `| grep -vE "do NOT enumerate|structurally cannot contain|story's ACs structurally"` to reach 0 and verdicted PASS, but the spec text retained the original literal grep. Future re-audits running the spec verbatim will see 6 matches and could read FAIL. **Fix:** AC #1(a) text updated to record both the literal pattern (with explanation of the negation-context boilerplate) and the binding refined grep. The structural-enforcer integrity criterion is now mechanically re-verifiable from the spec text.
- **M2 (PARTIAL FIX, OPEN DELIVERABLE) — Run-2 hardware transcript capture gap.** Run-1 transcript captured to `~/Downloads/beastty-13-5-6-20260506-225911.bin` (3,201 bytes; durable file; survives session). Run-2 (which provides the binding T11 PASS + T14 PASS verdicts) recorded only as inline conversation paste 2026-05-07; verbatim outputs (`T11=SZ=128`, `T14=hi`) are durably in the per-line Verdict table at Task 5, but the raw on-device transcript snapshot lives only in the dev-pass conversation log. AC #4/#5 mirror Story 13.6 Task 9.5 capture discipline and call for a `~/Downloads/bestialitty-13-5-6-…bin` file. **Fix in story:** Task 5 hardware-transcript-provenance paragraph extended with an "Open: pre-tag transcript-capture deliverable" note flagging this as a project-lead pre-tag action. **Open deliverable:** project lead snapshots the run-2 inline content to a sibling `~/Downloads/beastty-13-5-6-20260507-HHMMSS.bin` (or appended to run-1's file) before applying `git tag -a v2.0.0`. The story-file evidence (verbatim outputs in the Verdict table) satisfies the close-out gate's audit requirement; the transcript snapshot is the durability backstop.
- **M3 (FIXED) — AC #5 verdict cell hides run-1 → run-2 path.** Top-table verdict cell read `**PASS** (T2..T8 PASS / T9 INCONCLUSIVE-acceptable / T10..T15 PASS)`, which obscures that run-1 had T11 cascade-FAIL (FCB-pool exhausted by step 10's leftover slots) and T14 FAIL (`CMOVE` undefined; antforth has `MOVE` not `CMOVE`); both were re-run in run-2 with probe fixes and only then PASSed. The dev pass *does* document this fully in Task 5 sidenotes, but the top-of-Completion-Notes Verdict-Gate table presents an unqualified PASS. **Fix:** AC #5 cell qualified to `T11 + T14 PASS run-2 via probe-authoring fix — kernel sound on first run, probe text needed FCB-pool reset before T11 and MOVE instead of CMOVE at T14`. Verdict transparency restored.

**LOW findings (recorded as `[AI-Review]` follow-ups).** L1 narrative-credit fix for the 13.5.5 doc-drift framing; L2 INCLUDE-vs-INCLUDED + dropped `B:` prefix in Task 4 deviation; L3 sprint-status intermediate-state non-commit qualifier; L4 12-line smoke probe-authoring carry-forward owner/file. None block the v2.0.0 tag.

**v2.0.0 tag application disposition.** With M1 + M3 fixed in-pass, M2's open project-lead deliverable named, and the project-lead-flagged H1 banner bump landed in CR pass, the audit gates are clean and the hardware gates are PASS. Recommended timing per Task 15: tag lands on the post-CR-pass commit (which includes the banner bump + story file CR edits + sprint-status flip + Makefile test 80 update), NOT on `97a57d6` — the v1.12.0 banner on `97a57d6` would announce the wrong version under a v2.0.0 tag. Final post-CR binary is **24,995 bytes (production) / 26,460 (filesanity); 973 PASS / 0 FAIL**. Project lead applies the tag — dev agent does not.

**H1 — Tag-prep banner bump (project-lead-flagged HIGH; CR initial-pass miss).** AC #4's hardware transcript captured `AntForth v1.12.0 (C) ant.org 2026` — the audit-only invariant carried this banner forward unchanged. Project lead flagged: "the version banner didn't get updated!". CR concurred — landing the v2.0.0 tag on a binary that prints v1.12.0 at startup is incoherent. **Fix landed:** `src/antforth.asm` banner string + length EQU; `Makefile` test 80 assertion. Binary -1 byte each (production + filesanity); 973 PASS / 0 FAIL preserved; live banner verified `AntForth v2.0.0 (C) ant.org 2026`. **Why this didn't surface earlier:** the dev-pass audit-only framing focused on Epic-13.5 cleanup-slate scope (PD-1 + TD-1..TD-7) and read "no `src/*.asm` instruction edits" as a hard invariant for ALL `src/` content rather than a cleanup-slate-scope statement. The banner is a tag-presentation string, not a cleanup-slate concern; the audit-only invariant should have been scoped to the cleanup-slate, not the entire repo. **CR-side learning (carry-forward to future close-out gates with tag application):** when a story is BOTH a close-out audit AND a tag-application gate, the version-banner sync is in scope as a tag-prep concern even if no functional change is in scope. The banner-and-tag-version sync should be an explicit AC checkbox at any future tag-application gate.

### Change Log

| Date | Author | Change |
|---|---|---|
| 2026-05-06 | Ant Skelton (dev-pass via Claude Code Opus 4.7 1M) | Story drafted by create-story workflow; sprint-status flipped `backlog → ready-for-dev`. |
| 2026-05-06 | Ant Skelton (dev-pass) | Sprint-status flipped `ready-for-dev → in-progress`; story Status synchronised; clean rebuild from HEAD `97a57d6` (24,996 / 26,461 bytes); `make test` clean; `make test-repl` 973 PASS / 0 FAIL; `make test-file-sanity` PASS; Tag-Blocking Slate verdict-table walk 8/8 PASS (PD-1 + TD-1..TD-7); standing-commitment audit S1/S5/S8/S9/S10 5/5 PASS; citation audit 8/8 OK; register-convention audit 5/5 PASS; doc-currency audit 6/6 OK; byte-budget Δ=0 confirmed; identifier-gate 0 matches; 13.5.5 6-byte close-out doc-drift surfaced and reconciled in-pass per `feedback_systematic_reference_check.md` (actual binary is the authoritative manual). Memory `project_phase2_scope.md` + `MEMORY.md` index updated. **Audit-side gates: 10/10 PASS.** Tasks 4 + 5 (hardware RELEASE GATES) pending project-lead re-run on the post-cleanup binary. |
| 2026-05-06 | Project lead | **Hardware run 1** on real MicroBeast / CP/M 2.2 / SD-backed: Task 4 PRD Journey 1 round-trip PASS (define `: BLINK 65 EMIT 66 EMIT 67 EMIT CR ;` → save → cold-restart → INCLUDE → `BLINK BLINK BLINK` → `ABC` × 3); Task 5 12-line smoke T2..T8 PASS, T9 INCONCLUSIVE (B:NESTED.FTH absent — disk-corpus prerequisite, acceptable per Story 13.6 run-3), T10=-69 PASS, T11 cascade-FAIL (script-bug — step 10 leaves 8 P*.TXT FCBs allocated; pool exhausted; kernel `-69` correct), T12=-1 3 2 1 PASS, T13=hello<2> 1 2 PASS (TD-5 BC-preservation), T14 FAIL (probe-bug — antforth has MOVE not CMOVE), T15=0 PASS (TD-7 EVALUATE round-trip). Two probe-authoring bugs surfaced (mirror Story 13.6 F-9/F-10/F-11 hardware-surfaced script bugs); kernel sound. Transcript `~/Downloads/beastty-13-5-6-20260506-225911.bin` (3,201 bytes). |
| 2026-05-07 | Project lead | **Hardware run 2** (cleanup re-run for the two run-1 probe-authoring bugs): `BYE` + relaunch antforth (fresh FCB pool); re-ran Task 5 step 11 → **T11=SZ=128** PASS (R/O destructive-flush invariant + Story 13.5.1 mode-aware `file_flush` discipline verified on real CP/M 2.2 BDOS); re-ran Task 5 step 14 with `MOVE` (corrected from CMOVE) → **T14=hi** PASS (TD-6 PAD MOVE round-trip on real hardware). All RELEASE GATES PASS. **Tasks 4 + 5: PASS.** |
| 2026-05-07 | Ant Skelton (dev-pass close) | Tasks 15, 16, 17, 18 finalised. Tag-Blocking Slate Verdict + v2.0.0 Tag Gate verdict table at top of Completion Notes: 12/12 PASS. **Release-readiness one-liner: antforth 2.0 RELEASE-READY — tag v2.0.0.** Tag proposal recorded for project-lead application: `git tag -a v2.0.0 -m "Phase 2 + cleanup slate: ANS Core 100% + CATCH/THROW + Search-Order + File-Access + on-device source development + Tag-Blocking Slate cleanup"`. Phase-2 + cleanup-slate milestone marker recorded (FR1-FR47 delivered, FR30 gapped; NFR1-NFR21 satisfied/carry-forward; all 7 Tag-Blocking Slate items discharged; v2.0.0 candidate; post-tag opportunities listed). Sprint-status flipped `13.5-6-…: in-progress → review` (epic-13.5 stays `in-progress` until story-`done` flip post-CR). Story Status flipped `in-progress → review`. Parent ↔ subtask discipline confirmed (18 parents all `[x]`; every subtask `[x]`). Audit-only diff scope held: this story file + sprint-status.yaml + memory edits only; zero `src/*.asm` instruction changes; zero binary delta from pre-edit (24,996 / 26,461). **Story 13.5.6 dev-pass closed — ready for `CR` adversarial review in fresh context per S1 standing commitment + PD-1 closure.** |
| 2026-05-07 | CR (claude-opus-4-7[1m]) | **Code review PASS with tag-prep follow-on edit.** Findings: 0 HIGH at first pass, 3 MEDIUM, 4 LOW. All 3 MEDIUM fixed in-pass: (M1) AC #1(a) PD-1 grep text updated to record both the literal pattern (returns 6 negation-context boilerplate matches) and the binding refined grep (`| grep -vE "do NOT enumerate\|structurally cannot contain\|story's ACs structurally"` → 0 matches) so future re-audits running the spec see the structural-enforcer integrity criterion clearly; (M2) Task 5.4 hardware-transcript-provenance paragraph extended with an "Open: pre-tag transcript-capture deliverable" note flagging the run-2 inline-paste-only durability gap as a project-lead pre-tag action — verbatim T11=SZ=128 + T14=hi outputs already recorded in the per-line table satisfy story-file evidence durability; the raw on-device transcript snapshot is the open item; (M3) AC #5 cell in the Tag-Blocking Slate Verdict + v2.0.0 Tag Gate top-table qualified to surface the run-1 → run-2 path. 4 LOW findings (L1 13.5.5-CR doc-drift framing, L2 INCLUDE-vs-INCLUDED + dropped B: prefix in Task 4, L3 sprint-status intermediate-state non-commit, L4 12-line smoke probe-authoring carry-forward owner) recorded as `[AI-Review][LOW]` follow-ups. **Project-lead-flagged HIGH miss (logged as H1 retrospectively):** the version-banner string `AntForth v1.12.0` was unchanged at dev-pass close — the v2.0.0 tag would have landed on a binary still announcing v1.12.0. CR initially missed this. Bumped in-pass: `src/antforth.asm` `str_banner1` to `"AntForth v2.0.0 (C) ant.org 2026"` + `STR_BANNER1_LEN` 33 → 32; `Makefile` REPL test 80 expected-string updated to `'AntForth v2.0.0'`. **Binary delta: production 24,996 → 24,995 (Δ -1); filesanity 26,461 → 26,460 (Δ -1).** All 973 REPL tests PASS post-bump; `make test` clean; live banner verified `AntForth v2.0.0 (C) ant.org 2026`. The audit-only invariant for the *cleanup-slate scope* (PD-1 + TD-1..TD-7) is preserved — no Epic-13.5 word changed. The banner bump is a tag-prep concern, not Epic 13.5 scope; project-lead-accepted in-pass per CR fix discipline. Hardware transcripts captured `v1.12.0` (pre-bump binary); functional gates remain valid (banner is a startup-only string with zero kernel-path interaction); re-capturing a `v2.0.0`-banner transcript is project-lead discretion. **Final post-CR-pass binary: 24,995 / 26,460; 973 PASS / 0 FAIL.** v2.0.0 tag should land on the new CR-pass commit (which includes the banner bump), NOT on `97a57d6`. |
