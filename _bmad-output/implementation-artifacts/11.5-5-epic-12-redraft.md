# Story 11.5.5: Epic 12 redraft — drop ASSEMBLER wordlist entirely

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As an antforth maintainer,
I want Epic 12 sections in `_bmad-output/planning-artifacts/epics.md` and the `epic-12` rows in `_bmad-output/implementation-artifacts/sprint-status.yaml` updated so that no `ASSEMBLER` wordlist, no `ASSEMBLER.FTH`, and no `CODE`/`END-CODE` auto-activation appear anywhere in the Epic 12 plan,
so that Epic 12 reflects the project lead's 2026-04-27 direction (the existing hard-coded assembler implementation in `src/assembler.asm` is what we keep going forwards) and no Epic 12 story is started against a stale spec — closes the `project_epic12_redraft_required.md` flag and clears the gate that Story 11.5.7 (Epic 11.5 close-out) requires before Epic 12 starts.

## Acceptance Criteria

1. **Given** the Epic 12 stale-banner at `_bmad-output/planning-artifacts/epics.md:1135` (the "⚠️ STALE — pending Story 11.5.5 redraft (2026-04-27)" paragraph) and the epic title at `:1133` (`## Epic 12: Multi-Vocabulary Search-Order` — note: the title was already partially updated 2026-04-27 to drop "& ASSEMBLER Wordlist" from the heading, but the stale-banner remains in place pending this redraft),
   **when** the dev agent reads the Epic 12 section start in current HEAD before edits,
   **then** the byte-for-byte content of `:1133-1137` is recorded in Completion Notes Task 1 as the documented baseline. Any deviation from the recorded baseline (e.g., an intervening edit changed the heading further) is investigated before proceeding — the story may need scope adjustment. The dev agent also records the line count of the current Epic 12 section (from `## Epic 12:` at `:1133` to the line before `## Epic 13:` at `:1351`) — expected ≈ 218 lines pre-redraft.

2. **Given** the project lead's 2026-04-27 direction (no `ASSEMBLER.FTH`, no ASSEMBLER wordlist, no auto-activation, hard-coded assembler stays as-is — per `project_assembler_keep_assembly.md` and `sprint-change-proposal-2026-04-27.md`),
   **when** the Epic 12 title and stale-banner are redrafted,
   **then** the title at `epics.md:1133` is confirmed as `## Epic 12: Multi-Vocabulary Search-Order` (no change required if already in this form; otherwise corrected); the stale-banner at `:1135` is **removed in full** and replaced with a one-line closure cross-reference to this story (e.g., `Redrafted 2026-04-27..2026-04-28 by Story 11.5.5 to match the 2026-04-20 + 2026-04-27 ASSEMBLER rollback decisions; see _bmad-output/implementation-artifacts/11.5-5-epic-12-redraft.md.`); the Epic 12 intro paragraph at `:1137` removes every mention of ASSEMBLER wordlist activation, opcode-word registration, and `CODE`/`END-CODE` auto-activation. Forward-looking phrasing only — historical narrative belongs in the sprint-change proposals, not the live epic spec. Recorded in Completion Notes Task 2.

3. **Given** Story 12.1 (`epics.md:1139-1169` — `### Story 12.1: Wordlist struct + hash parameterisation + FORTH-WORDLIST bootstrap`),
   **when** redrafted,
   **then** any ACs that pre-allocate an `ASSEMBLER` bucket slot or assume an ASSEMBLER wordlist will follow are stripped; the bucket-array layout is sized solely for the user-facing search-order wordsets. Story 12.1's user-story statement, the FR28 cross-reference, and the per-wordlist-130-byte-struct numbers stay intact (the struct layout is not affected by the rollback). Each AC is re-read in context to confirm zero forward-looking ASSEMBLER references survive. Recorded in Completion Notes Task 3 with the specific lines edited (or "no edits required" with justification).

4. **Given** Story 12.6 at `epics.md:1287-1317` (`### Story 12.6: ASSEMBLER wordlist + auto-activation in CODE/END-CODE`),
   **when** the redraft lands,
   **then** the entire story is **deleted** from `epics.md` (lines `:1287-1317` plus the trailing blank line). The Story-10.7 asm-`#` dispatch hack is **permanent** (no retirement vehicle planned); `project_asm_hash_dispatch_hack.md` already reflects this 2026-04-27 update — no further memory edit required for that hack. Recorded in Completion Notes Task 4 with the deleted line range and the post-deletion line count for cross-check against AC #1's pre-redraft count.

5. **Given** Story 12.7 at `epics.md:1319-1349` (`### Story 12.7: Epic 12 benchmark, CODE backward-compat suite + regression gate (CCD-4)`),
   **when** the redraft lands,
   **then** the heading is renumbered to `### Story 12.6: Epic 12 benchmark, CODE backward-compat suite + regression gate (CCD-4)` (filling the gap left by the Story 12.6 deletion); ACs are pruned to drop the CODE-source-file backward-compat suite that referenced ASSEMBLER opcode-relocation (FR31 / NFR14 backward-compat is **still verified** — but framed against the unchanged hard-coded assembler subsystem, not against any opcode migration); the AC at `:1339-1341` (kernel ROM size delta accounting) is recalibrated to drop the bullet-point reference to "`ASSEMBLER` wordlist registration, `CODE`/`END-CODE` auto-activation hooks" while keeping the Search-Order machinery + per-wordlist hash layout bullets. Recorded in Completion Notes Task 5 with the specific AC clauses edited or struck.

6. **Given** sprint-status.yaml Epic 12 rows at `_bmad-output/implementation-artifacts/sprint-status.yaml:185-193`,
   **when** the redraft lands,
   **then** the row `12-6-assembler-wordlist-and-auto-activation-in-code-end-code: backlog` (`:191`) is **removed**; the row `12-7-epic-12-benchmark-code-backward-compat-suite-and-regression-gate-ccd-4: backlog` (`:192`) is renamed to `12-6-epic-12-benchmark-code-backward-compat-suite-and-regression-gate-ccd-4: backlog` (matching the renumbered story slug from AC #5); the `epic-12: backlog` header at `:185` and other Stories 12.1–12.5 rows are unchanged. Resulting row order: `epic-12 / 12-1 / 12-2 / 12-3 / 12-4 / 12-5 / 12-6 (new — was 12-7) / epic-12-retrospective`. The placeholder comment block above `epic-12: backlog` (`:149-156`) referencing Story 11.5.5 is updated to a closure cross-reference (e.g., `# Epic 12 redrafted 2026-04-27..2026-04-28 by Story 11.5.5; rows below are post-redraft.`). Recorded in Completion Notes Task 6.

7. **Given** the affected memories — `project_phase2_scope.md`, `project_epic12_redraft_required.md`, `project_assembler_keep_assembly.md`, `project_asm_hash_dispatch_hack.md`, `project_epic_11_5_scope.md` (all under `/home/ant/.claude/projects/-home-ant-src-microbeast-antforth/memory/`),
   **when** the redraft lands,
   **then**:
   - `project_phase2_scope.md` Epic 12 entry is verified current (already updated 2026-04-27 to "Search-Order Wordset only (Stories 12.1–12.5 + close-out gate, post-Story-11.5.5 redraft)"); no edit required unless drift is found. Recorded in Completion Notes Task 7a.
   - `project_epic12_redraft_required.md` is **replaced** with a brief closure note (or **deleted** entirely; either is acceptable per the epic-spec's literal phrasing). Recommendation: replace with a 3-5 line closure pointer to this story's commit and the post-redraft Epic 12 spec, so future agents searching for "epic12_redraft" find a non-stub answer; if deleted, remove the corresponding line from `MEMORY.md`. Recorded in Completion Notes Task 7b.
   - `project_assembler_keep_assembly.md` is verified current (already updated 2026-04-27 to capture both rollback waves); no edit required unless drift is found. Recorded in Completion Notes Task 7c.
   - `project_asm_hash_dispatch_hack.md` is verified current (already marked permanent 2026-04-27); no edit required unless drift is found. Recorded in Completion Notes Task 7d.
   - `project_epic_11_5_scope.md` is updated to show Story 11.5.5 closed: replace the AC-3 line for 11.5.5 ("Document-surgery; the path is settled…") with a "**Closed (date)** — Epic 12 redrafted; Story 12.6 deleted; Story 12.7 renumbered to 12.6; PRD/architecture verified clean." line, mirroring the convention used for `project_hardware_crash_audit.md`'s RESOLVED 2026-04-28 update. Recorded in Completion Notes Task 7e.
   - `MEMORY.md` index is left intact unless `project_epic12_redraft_required.md` was deleted — in that case, remove its index line.

8. **Given** the PRD (`_bmad-output/planning-artifacts/prd.md`) and architecture spec (`_bmad-output/planning-artifacts/architecture.md`),
   **when** the redraft lands,
   **then** every residual `ASSEMBLER` wordlist / auto-activation reference is scrubbed to match the post-2026-04-27 reality — but most of this is already done (see Dev Notes "Pre-redraft state of PRD/architecture" below). The dev agent verifies the in-flight high-impact edits already made on 2026-04-27 are intact: **(a)** PRD FR30 deletion (`prd.md:418` — `*(FR30 — ASSEMBLER wordlist auto-activation — withdrawn 2026-04-27 …)*`); **(b)** PRD Executive Summary phrasing (`prd.md:63` — "no `ASSEMBLER.FTH`, no ASSEMBLER wordlist, no auto-activation"); **(c)** PRD MVP rule (`prd.md:113` — "No ASSEMBLER wordlist is created"); **(d)** PRD vocabulary-model table (`prd.md:129` — "Unchanged — baked into kernel… no ASSEMBLER wordlist"); **(e)** PRD Mo's journey / runtime-model boot-flow (`prd.md:266` — "no ASSEMBLER wordlist or auto-activation is created in Phase 2"); **(f)** PRD post-MVP first-consumer wording (`prd.md:86` — "opcode words remain kernel-resident… (no ASSEMBLER wordlist or auto-activation; the existing implementation is what we keep going forwards)"); **(g)** PRD requirements-summary table (`prd.md:314-315` — Epic 11.5 row + Epic 12 row); **(h)** architecture.md §FR23–FR29, FR31 description (`:47` — "Built-in opcode words remain kernel-resident in the global dictionary as today (no ASSEMBLER wordlist; FR30 withdrawn 2026-04-27)"); **(i)** architecture.md E12-D4 withdrawn block (`:344-348`); **(j)** architecture.md Decision Impact Analysis (`:405` — "Epic 12 fourth — multi-vocabulary Search-Order wordset only…"); **(k)** architecture.md project-structure tree (`:677` — "assembler.asm — unchanged in Phase 2"); **(l)** architecture.md tests-tree comment (`:723` — "(assembler_wordlist_tests.fth — withdrawn 2026-04-27…)"); **(m)** architecture.md Epic-to-file mapping (`:789` — Epic 12 row). Any remaining narrative drift is reconciled in-pass. Final verification: `grep -nE 'ASSEMBLER wordlist|ASSEMBLER\.FTH|auto-activat' _bmad-output/planning-artifacts/{prd,architecture,epics}.md` returns **only historical / superseded-tagged hits** (e.g., the strikethrough FR30 row, the WITHDRAWN E12-D4 block, the Epic 11.5 redraft cross-references) — **zero forward-looking promises**. Recorded in Completion Notes Task 8 with the full grep output classified line-by-line as "historical / superseded / current-statement-of-rollback / NEEDS-EDIT".

9. **Given** the redraft is **document-only** (no code changes; no test changes; no Makefile changes),
   **when** the changes land,
   **then** zero binary delta — `wc -c build/antforth.com` pre-redraft and post-redraft are byte-identical; zero test-count delta — `make test-repl` PASS count pre-redraft and post-redraft are identical (expected 805 PASS / 0 FAIL post-Story-11.5.4 baseline); `make test-repl` regression baseline preserved. The dev agent runs the build + test pair before and after the redraft and records both readings in Completion Notes Task 9 — a non-zero delta in either dimension means a code or test file was inadvertently touched and the redraft scope is breached.

10. **Given** the `feedback_systematic_reference_check.md` discipline ("'Complete X' story specs must cross-reference the authoritative manual, not enumerate from memory"),
    **when** the dev agent surveys the residual ASSEMBLER-wordlist references for AC #8,
    **then** the survey command is run literally — `grep -nE 'ASSEMBLER wordlist|ASSEMBLER\.FTH|auto-activat' _bmad-output/planning-artifacts/{prd,architecture,epics}.md` — and the full output is classified line-by-line, **not** enumerated from memory. The dev agent does NOT skim the AC #8 list above and assume it is exhaustive — the grep is the source of truth. Any line found in the grep that is not in the AC #8 list above is investigated and either added to the AC #8 list (in-pass amendment) or scrubbed in-pass. Documented in Completion Notes Task 10.

11. **Given** the `feedback_adversarial_review.md` discipline ("reviews MUST find things; absence of findings is suspect"),
    **when** Story 11.5.5's review runs,
    **then** **at least 1 LOW finding is expected** despite this being a document-surgery story. Likely candidates the review must probe:
    - **(a) Cross-reference completeness** — Did the redraft introduce any dangling references? After Story 12.6 deletion + Story 12.7 → 12.6 renumber, every `Story 12.6` mention in epics.md / PRD / architecture / sprint-status / memories must point at the **new** Story 12.6 (the close-out gate), not the **deleted** Story 12.6 (ASSEMBLER wordlist). The review verifies by `grep -n 'Story 12\.6' _bmad-output/planning-artifacts/*.md _bmad-output/implementation-artifacts/sprint-status.yaml /home/ant/.claude/projects/-home-ant-src-microbeast-antforth/memory/*.md`.
    - **(b) Story 12.7 phantom references** — After the renumber, are there any residual `Story 12.7` references that should now read `Story 12.6`? E.g., the architecture's Story-12.6/12.7 cross-references at `architecture.md` (CCD-4 close-out gate), or the PRD's Epic-Sequence narrative. Review verifies by `grep -n 'Story 12\.7\|12\.7' _bmad-output/planning-artifacts/*.md _bmad-output/implementation-artifacts/sprint-status.yaml`.
    - **(c) Sprint-status placeholder comment honesty** — The pre-redraft comment block at `sprint-status.yaml:149-156` says "Story 11.5.5 redrafts Epic 12. Epic 12 backlog rows below are kept as placeholders; expect them to be edited or replaced by Story 11.5.5." After this story lands, that comment is stale. Review verifies the comment was updated (per AC #6) to a closure cross-reference, not left dangling.
    - **(d) AC #8 grep-classification honesty** — Per AC #10, the grep output for AC #8 is the source of truth, not the in-AC-#8 enumeration. Review re-runs the grep independently and cross-checks against the dev agent's classification — at least one mismatch (a hit not in the dev's classified list, or vice versa) is expected if the grep is being used honestly.
    - **(e) Stale-banner removal vs. rewrite** — Per AC #2, the stale-banner is "removed in full and replaced with a one-line closure cross-reference." Did the dev agent literally remove the banner (replacing it with a one-liner) or did they leave the banner-shaped paragraph and just edit its words? The closure note should be a single-line `> ` blockquote or comment, not a banner-shaped warning paragraph.
    - **(f) Memory closure-note consistency** — Per AC #7, multiple memories are touched. Did the closure-note phrasing differ across them (e.g., one says "Closed 2026-04-28", another says "Resolved 2026-04-28")? Internal consistency across memories matters for grep-driven future searches. Review verifies a single canonical closure phrasing.

    Triage all findings; HIGH/MEDIUM block the gate; LOW may be accepted with rationale (mirror Stories 11.5.2 / 11.5.3 / 11.5.4 review-log discipline). Recorded in Completion Notes Task 11.

12. **Given** the Story 11.5.1 verdict-only discipline does **not** propagate to this story (Story 11.5.5 IS a document-surgery story spawned from a documented epic-update signal, not from the audit),
    **when** small in-pass refinements are warranted (additional grep-driven scrubs surfaced during AC #10, polished closure-note phrasing, a one-line cross-reference adjustment),
    **then** they are landed inside this story — no spawning further sub-stories. Match the Story 11.5.2 / 11.5.3 / 11.5.4 in-pass-fix policy. The exception: if the AC #8 grep surfaces a forward-looking ASSEMBLER-wordlist promise that is **structurally load-bearing** in PRD/architecture (i.e., its scrub would change the meaning of an FR or NFR, not just narrative), HALT and flag it as a finding for the project lead before scrubbing — the change becomes a separate decision, not in-pass cleanup. Documented in Completion Notes Task 12.

13. **Given** the Epic 11.5 sprint-status row `epic-11.5: in-progress` at `sprint-status.yaml:158` and the Story 11.5.5 row `11.5-5-epic-12-redraft: backlog` at `:180`,
    **when** this story closes (`done`),
    **then** the row at `:180` flips to `done`. Story 11.5.5 closure does **not** flip the `epic-11.5` row to `done` — that flip is owned by Story 11.5.7 (the close-out gate) per the epics.md Epic 11.5 close-out spec. The dev agent does not edit `:158`. Recorded in Completion Notes Task 13.

14. **Given** the `feedback_repl_tests_preferred.md` convention does NOT apply here (no new tests added; this story is document-only per AC #9),
    **when** the dev agent considers adding a "regression test" for the redraft,
    **then** **no test is added.** Rationale: the regression net for a document-surgery story is the byte-identity check from AC #9 (build/binary unchanged) plus the grep-based residual-reference check from AC #8 + AC #10. No runtime test can exercise the document-surgery property; runtime tests would reflect the unchanged binary, not the doc redraft.

15. **Given** the `feedback_follow_process.md` discipline ("Don't ask permission for obvious next steps; just execute the workflow"),
    **when** the dev agent encounters edge cases during the redraft (e.g., closure-note phrasing choice, deletion-vs-replacement of `project_epic12_redraft_required.md`),
    **then** the dev agent picks the documented recommendation in the relevant AC and proceeds — escalation to the project lead is reserved for the AC #12 structural-load-bearing case only. All in-pass picks are recorded in Completion Notes per the Tasks below.

## Tasks / Subtasks

- [x] **Task 1 — Capture pre-redraft baseline (AC: #1, #9)**
  - [x] 1.1 Build current HEAD: `make clean && make`. Verify clean build (0 errors / 0 warnings); capture `wc -c build/antforth.com` (expected 17518 bytes per Story 11.5.4 Task 8).
  - [x] 1.2 Read `_bmad-output/planning-artifacts/epics.md:1133-1137` and record the byte-for-byte content in Completion Notes Task 1 as the documented baseline.
  - [x] 1.3 Compute the line count of the current Epic 12 section: `awk '/^## Epic 12:/,/^## Epic 13:/' _bmad-output/planning-artifacts/epics.md | wc -l`. Expected ≈ 218 lines pre-redraft. Record in Completion Notes Task 1.
  - [x] 1.4 Capture pre-redraft `make test-repl` output: `make test-repl 2>&1 | tail -5 > /tmp/pre-115-5.log`. Verify 805 PASS / 0 FAIL (post-Story-11.5.4 baseline). Record PASS count in Completion Notes Task 1.

- [x] **Task 2 — Redraft Epic 12 title + intro + stale-banner (AC: #2)**
  - [x] 2.1 Confirm `epics.md:1133` heading is `## Epic 12: Multi-Vocabulary Search-Order` (already updated 2026-04-27 — no further edit if so).
  - [x] 2.2 Remove the stale-banner paragraph at `:1135` in full; replace with a single-line closure cross-reference (one-line blockquote or comment pointing at this story file).
  - [x] 2.3 Re-read `:1137` Epic 12 intro paragraph; remove every mention of ASSEMBLER wordlist activation, opcode-word registration, and `CODE`/`END-CODE` auto-activation. Forward-looking phrasing only.
  - [x] 2.4 Record the specific lines edited and their before/after content in Completion Notes Task 2.

- [x] **Task 3 — Strip ASSEMBLER bucket-slot pre-allocation from Story 12.1 ACs (AC: #3)**
  - [x] 3.1 Read `epics.md:1139-1169` (Story 12.1 in full).
  - [x] 3.2 Identify any AC clause that pre-allocates an `ASSEMBLER` bucket slot or assumes an ASSEMBLER wordlist will follow.
  - [x] 3.3 No hits found; "no edits required" recorded with scan output as evidence.

- [x] **Task 4 — Delete Story 12.6 entirely (AC: #4)**
  - [x] 4.1 Read `epics.md:1287-1317` (Story 12.6 in full) plus the trailing blank line.
  - [x] 4.2 Delete the entire range. Confirmed deletion does not orphan any cross-reference.
  - [x] 4.3 Record the deleted line range and the post-deletion total line count of `epics.md` in Completion Notes Task 4.

- [x] **Task 5 — Renumber Story 12.7 → 12.6 + prune ACs (AC: #5)**
  - [x] 5.1 Read `epics.md:1319-1349` (Story 12.7 in full).
  - [x] 5.2 Edit the heading from `### Story 12.7: …` to `### Story 12.6: …` (executed atomically with Task 4 deletion).
  - [x] 5.3 Prune CODE-source-file backward-compat AC framing + recalibrate kernel-ROM-size-delta bullet.
  - [x] 5.4 Record edited AC clauses in Completion Notes Task 5 with before/after content.

- [x] **Task 6 — Update sprint-status.yaml Epic 12 rows (AC: #6)**
  - [x] 6.1 Read `sprint-status.yaml:149-156` (the Epic 11.5 / Epic 12 transition comment block).
  - [x] 6.2 Read `sprint-status.yaml:185-193` (the Epic 12 rows).
  - [x] 6.3 Edit `:149-156` placeholder comment to a closure cross-reference.
  - [x] 6.4 Delete row `:191` (`12-6-assembler-wordlist-and-auto-activation-in-code-end-code: backlog`).
  - [x] 6.5 Rename row `:192` to `12-6-epic-12-benchmark-code-backward-compat-suite-and-regression-gate-ccd-4: backlog`.
  - [x] 6.6 Verified resulting row order: `epic-12 / 12-1 / 12-2 / 12-3 / 12-4 / 12-5 / 12-6 (new — was 12-7) / epic-12-retrospective`.

- [x] **Task 7 — Update memories per AC #7 (AC: #7)**
  - [x] 7.1 Verified `project_phase2_scope.md` Epic 12 entry — current; no edit required.
  - [x] 7.2 Replaced `project_epic12_redraft_required.md` with a closure-pointer body (kept file; updated frontmatter description + body).
  - [x] 7.3 Verified `project_assembler_keep_assembly.md` is current — no edit required.
  - [x] 7.4 Verified `project_asm_hash_dispatch_hack.md` is current — no edit required.
  - [x] 7.5 Edited `project_epic_11_5_scope.md` to mark Story 11.5.5 **Closed 2026-04-28** with cross-reference to this story file.
  - [x] 7.6 `MEMORY.md` left intact (file kept, not deleted) per AC #7.

- [x] **Task 8 — Verify PRD + architecture residual-references (AC: #8)**
  - [x] 8.1 Ran the survey grep verbatim: `grep -nE 'ASSEMBLER wordlist|ASSEMBLER\.FTH|auto-activat' _bmad-output/planning-artifacts/{prd,architecture,epics}.md` — 28 hits total (8 prd / 12 epics / 8 architecture).
  - [x] 8.2 Classified each hit line-by-line — zero NEEDS-EDIT hits.
  - [x] 8.3 Cross-checked AC #8 enumeration (a)..(m) against grep output — all 13 entries match.
  - [x] 8.4 No NEEDS-EDIT scrubs required (zero NEEDS-EDIT hits surfaced).
  - [x] 8.5 Re-ran the grep post-edit; zero NEEDS-EDIT hits confirmed.
  - [x] 8.6 Recorded the grep output classification in Completion Notes Task 8.

- [x] **Task 9 — Verify zero binary delta + zero test delta (AC: #9)**
  - [x] 9.1 Build post-redraft. `wc -c build/antforth.com` = 17518 bytes (byte-identical to pre-redraft).
  - [x] 9.2 Run post-redraft `make test-repl` — 805 PASS / 0 FAIL; `diff /tmp/pre-115-5.log /tmp/post-115-5*.log` is empty (FILES IDENTICAL).
  - [x] 9.3 Both deltas zero — no HALT.
  - [x] 9.4 Recorded both readings in Completion Notes Task 9.

- [x] **Task 10 — Systematic reference check honesty (AC: #10)**
  - [x] 10.1 Confirmed Task 8.1's grep was run literally (verbatim AC #8 command).
  - [x] 10.2 No AC #8 enumeration entry was discovered to be wrong (every (a)-(m) appeared in the grep output). The "≥1 expected mismatch" surfaces from the **grep-vs-AC #8 scope** rather than the enumeration accuracy — see Task 11.

- [x] **Task 11 — Adversarial self-review (AC: #11)**
  - [x] 11.1 Self-reviewed against all six probe categories (a)-(f).
  - [x] 11.2 No HIGH/MEDIUM findings surfaced.
  - [x] 11.3 LOW finding L1 surfaced from probe (a)/scope tense; one additional scope-clarity note (probe d) recorded.
  - [x] 11.4 Recorded findings in Completion Notes Task 11.

- [x] **Task 12 — In-pass-fix discipline + structural-load-bearing escalation gate (AC: #12)**
  - [x] 12.1 In-pass-fix log: 1 narrative scrub (`architecture.md:348` "deletes" → "deleted"); zero structural-load-bearing findings.
  - [x] 12.2 No HALT triggered — no FR/NFR-meaning changes required.
  - [x] 12.3 Recorded in Completion Notes Task 12.

- [x] **Task 13 — Sprint-status row flip (AC: #13)**
  - [x] 13.1 Edit `_bmad-output/implementation-artifacts/sprint-status.yaml` `11.5-5-epic-12-redraft: review` → `done` (executed at code-review close 2026-04-28 after review-fix pass; see Completion Notes Task 14).
  - [x] 13.2 Confirmed `epic-11.5: in-progress` row is **not** edited — that flip is owned by Story 11.5.7.
  - [x] 13.3 Recorded in Completion Notes Task 13.

- [x] **Task 14 — Code-review fix pass (post-review 2026-04-28)**
  - [x] 14.1 Adversarial code review surfaced 3 HIGH + 2 MEDIUM + 1 LOW findings missed by AC #8 grep (broken regex — didn't match backticked form `` `ASSEMBLER` wordlist ``).
  - [x] 14.2 Scrubbed `epics.md:228-230` (Epic 12 epic-overview: title + description + FRs-covered).
  - [x] 14.3 Withdrew `epics.md:68` (FR30 strikethrough in Phase-2 FR list).
  - [x] 14.4 Scrubbed `epics.md:142` (source-file-layout bullet promising ASSEMBLER wordlist registration).
  - [x] 14.5 Corrected `epics.md:1143` Story 12.1 So-that line FR range `FR23–FR30` → `FR23–FR29, FR31`.
  - [x] 14.6 Disambiguated `sprint-status.yaml:150` closure comment ("formerly Story 12.7" framing).
  - [x] 14.7 Re-ran broader grep `grep -nE 'ASSEMBLER.{0,3}[Ww]ordlist|ASSEMBLER\.FTH|auto-activat'` — 31 hits total (8 prd / 8 architecture / 15 epics), all classified historical / superseded / current-statement-of-rollback. Zero NEEDS-EDIT.
  - [x] 14.8 Verified byte-identical: `wc -c build/antforth.com` = 17518 bytes; `make test-repl` = 805 PASS / 0 FAIL.

## Dev Notes

### Story summary

This is a **document-surgery story**. No code, no tests, no Makefile changes. Five files touched:
- `_bmad-output/planning-artifacts/epics.md` — Epic 12 section redraft (title, banner, intro, Story 12.1 ACs, Story 12.6 deletion, Story 12.7 → 12.6 renumber + AC pruning)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — Epic 12 rows (delete 12-6, rename 12-7 → 12-6, update transition comment)
- `/home/ant/.claude/projects/-home-ant-src-microbeast-antforth/memory/project_epic12_redraft_required.md` — replace with closure note (or delete + remove `MEMORY.md` index line)
- `/home/ant/.claude/projects/-home-ant-src-microbeast-antforth/memory/project_epic_11_5_scope.md` — append closure note to Story 11.5.5 entry
- `_bmad-output/planning-artifacts/{prd,architecture}.md` — only if the AC #10 grep surfaces residual NEEDS-EDIT hits (pre-redraft expectation: zero such hits)

### Path discipline (one settled path, no project-lead pick)

The earlier sprint-change-proposal-2026-04-27.md draft offered three paths to the project lead (passive `ASSEMBLER` wordlist; drop Story 12.6 entirely; reinstate auto-activation under a different mechanism). **The pick is settled** — per `project_epic12_redraft_required.md` and `project_epic_11_5_scope.md`, the path is **drop Story 12.6 entirely**. The asm-`#` dispatch hack stays permanent (no retirement vehicle planned). This story does not re-open the choice; it executes the picked path.

### Pre-redraft state of PRD/architecture

The high-impact PRD/architecture edits were already made on 2026-04-27 by the sprint-change-proposal author (verbatim from `epics.md:1071`):

> *"the in-flight high-impact edits already made on 2026-04-27 (FR30 deletion, Executive Summary, MVP rule, vocabulary-model table, Mo's journey, runtime-model boot-flow, post-MVP first-consumer wording, requirements-summary table) are verified intact"*

Those edits are now load-bearing for AC #8 — the dev agent's job is **verification**, not re-doing the scrub. The scrub work concentrates in `epics.md` (the live spec) and `sprint-status.yaml` (the tracking file). PRD and architecture should require zero or near-zero edits.

### Story 12.6 deletion blast radius

After Story 12.6 is deleted from `epics.md`:
- **Cross-references in `epics.md`**: Story 12.7 (renumbered to 12.6) AC at `:1339-1341` references "ASSEMBLER wordlist registration, CODE/END-CODE auto-activation hooks" — pruned per AC #5.
- **Cross-references in PRD**: `prd.md:315` (Epic 12 row in requirements-summary table) explicitly says "Story 11.5.5 scrubs the original Story 12.6 from `epics.md`" — this is a forward-pointer that becomes a back-pointer after this story lands; phrasing already correct ("scrubs", past-action-shape).
- **Cross-references in architecture**: `architecture.md:344-348` (the WITHDRAWN E12-D4 block) says "Story 11.5.5 (Epic 12 redraft) deletes the original Story 12.6 from `epics.md`" — same forward-pointer pattern; phrasing already correct.
- **Cross-references in memory**: `project_phase2_scope.md` says "Story 12.6 (was 'ASSEMBLER wordlist + auto-activation') is dropped entirely" — already past-tense; correct. `project_asm_hash_dispatch_hack.md` says "The retirement vehicle was Story 12.6 (`ASSEMBLER` wordlist + …)" — past-tense; correct.

The deletion does not orphan any cross-reference — every `Story 12.6` mention outside `epics.md` is already past-tense referring to the deletion, not forward-looking.

### Story 12.7 → 12.6 renumber blast radius

After the renumber:
- **Forward-looking references**: post-redraft, every `Story 12.6` in PRD/architecture must point at the new close-out gate, not the deleted ASSEMBLER story. Per AC #11(a), the dev agent greps to verify.
- **Past-tense references** to the original Story 12.7 (the close-out gate, by its old number): the architecture's CCD-4 close-out cross-references; the implementation-readiness-report's Epic 12 capstone references. Most of these are in `sprint-change-proposal-2026-04-20.md` and historical artifacts — leave alone.
- **No live reference** to "Story 12.7" exists in `prd.md` or `architecture.md` post-2026-04-27 (per the existing pass) — the dev agent verifies via `grep -n 'Story 12\.7' _bmad-output/planning-artifacts/{prd,architecture}.md` (expected: zero hits or only historical/strikethrough hits).

### Asm-`#` dispatch hack permanence

`src/assembler.asm:1024-1027` (`w_HASH_cf` with the `LD A,(asm_mode); JP Z, w_PIC_HASH_cf` dispatch) stays as-is forever per `project_asm_hash_dispatch_hack.md` (marked permanent 2026-04-27). This story does not touch any source file — including `src/assembler.asm` — so the hack is not affected. The Story 11.5.5 closure does NOT spawn a follow-up "retire the dispatch hack" story; the +4-byte cost is accepted permanently.

### Why this story matters now (not deferred to Phase-2 mid-Epic-12)

1. **Stale spec is load-bearing for create-story.** Per `project_epic12_redraft_required.md` ("do not run `bmad-bmm-create-story` on any 12.x story — Epic 12 spec is load-bearing-stale until Story 11.5.5 lands"), no Epic 12 story can be created against the current `epics.md` Epic 12 section without inheriting the stale ASSEMBLER assumptions. This story unblocks Epic 12 dev start.
2. **Epic 11.5 close-out gate dependency.** Story 11.5.7 (Epic 11.5 close-out) AC at `epics.md:1121-1123` says: *"Given the Epic 12 redraft from Story 11.5.5 / When verified / Then the post-redraft Epic 12 sprint-status rows match the redrafted epic-spec stories; the asm-`#` hack memory is consistent with the picked retirement path; no orphan memory references the pre-redraft plan."* — Story 11.5.5 must land before Story 11.5.7 can verify its AC.
3. **Cheap to do.** Pure document surgery; no code; no tests; ~30 minutes of careful edits + grep verification + memory updates. Deferring it to Phase-2 mid-Epic-12 would mean re-loading the same context (the 2026-04-20 + 2026-04-27 rollback narrative) months later, with the additional risk that an Epic 12 story has already been started against the stale spec.

### Key file references

| File | Lines | Relevance |
|---|---|---|
| `_bmad-output/planning-artifacts/epics.md` | 1133-1351 | Epic 12 section — primary edit target |
| `_bmad-output/planning-artifacts/epics.md` | 1135 | Stale-banner — remove per AC #2 |
| `_bmad-output/planning-artifacts/epics.md` | 1137 | Epic 12 intro paragraph — re-scan + scrub per AC #2 |
| `_bmad-output/planning-artifacts/epics.md` | 1139-1169 | Story 12.1 — re-scan for ASSEMBLER bucket-slot pre-allocation per AC #3 |
| `_bmad-output/planning-artifacts/epics.md` | 1287-1317 | Story 12.6 — delete per AC #4 |
| `_bmad-output/planning-artifacts/epics.md` | 1319-1349 | Story 12.7 — renumber to 12.6 + prune ACs per AC #5 |
| `_bmad-output/planning-artifacts/epics.md` | 1037-1075 | Story 11.5.5 spec (this story's epic-spec source-of-truth) |
| `_bmad-output/implementation-artifacts/sprint-status.yaml` | 149-156 | Epic 11.5 / Epic 12 transition comment — closure cross-reference per AC #6 |
| `_bmad-output/implementation-artifacts/sprint-status.yaml` | 180 | This story's row — flip to `done` post-review per AC #13 |
| `_bmad-output/implementation-artifacts/sprint-status.yaml` | 185-193 | Epic 12 rows — delete 12-6, rename 12-7 → 12-6 per AC #6 |
| `_bmad-output/planning-artifacts/prd.md` | 63, 86, 113, 129, 266, 314-315, 416, 418 | PRD ASSEMBLER references — verify intact per AC #8 |
| `_bmad-output/planning-artifacts/architecture.md` | 47, 344-348, 405, 677, 723, 789 | Architecture ASSEMBLER references — verify intact per AC #8 |
| `~/.claude/projects/-home-ant-src-microbeast-antforth/memory/project_epic12_redraft_required.md` | full file | Replace with closure note or delete per AC #7 |
| `~/.claude/projects/-home-ant-src-microbeast-antforth/memory/project_epic_11_5_scope.md` | Story 11.5.5 entry | Append closure note per AC #7 |
| `~/.claude/projects/-home-ant-src-microbeast-antforth/memory/MEMORY.md` | full file | Update only if `project_epic12_redraft_required.md` deleted (per Task 7.6) |
| `_bmad-output/planning-artifacts/sprint-change-proposal-2026-04-27.md` | full file | Historical — DO NOT EDIT (records the decision; future agents may re-read it) |
| `_bmad-output/planning-artifacts/sprint-change-proposal-2026-04-20.md` | full file | Historical — DO NOT EDIT (records the earlier wave of the rollback) |

### Architecture context

- **No FR/NFR additions or deletions in this story.** PRD's FR30 deletion (line `:418`) is already done; this story only verifies and scrubs residuals.
- **NFR4 ROM-delta envelope** (`architecture.md:57`): zero contribution from this story — the redraft is document-only and the binary is byte-identical pre/post (AC #9).
- **CCD-3 citation discipline** (`architecture.md:206-216`): the closure cross-references added to the stale-banner replacement (AC #2) and to the sprint-status comment block (AC #6) follow the existing one-line cross-reference style. No new EQU citations or source comments — this is a planning-artifact pass, not a kernel-source pass.
- **Phase-2 architecture** (`project_phase2_scope.md`): Epic 12 is now "Search-Order Wordset only (Stories 12.1–12.5 + close-out gate, post-Story-11.5.5 redraft)". This story finalises that scope by editing the `epics.md` and `sprint-status.yaml` to match the memory.
- **Decision Impact Analysis** (`architecture.md:398-411`): Epic 12 still sequences fourth (after Epic 11) and fifth-from-last (before Epic 13). The redraft does not change sequencing; it only narrows scope.

### Previous-story intelligence

- **Story 11.5.4** (`11.5-4-…md`, just landed): print_throw_description hardening; near-zero binary delta (−2 bytes); pure code-side fix. Established the post-Story-11.5.4 baseline of 17518 bytes / 805 PASS that AC #9 byte-identity check uses.
- **Story 11.5.3** (`11.5-3-…md`): `(`/EVALUATE source-frame fix; established the 805 PASS high-water mark.
- **Story 11.5.2** (`11.5-2-…md`): stack-overflow `-3 THROW` guard; established the in-pass-fix policy that AC #12 inherits.
- **Story 11.5.1** (`11.5-1-…md`, status: review): hardware crash audit; verdict-only deliverable. Story 11.5.5 does NOT inherit the verdict-only discipline (AC #12 — Story 11.5.5 IS a fix-story spawned from a documented epic-update signal, not from the audit).
- **Story 11.5.1.2** (`11.5-1-2-…md`, done): firmware BDOS register-preservation reproducer; led to firmware fix landing 2026-04-28; Story 11.5.1.1 dropped permanently. Orthogonal to this story.
- **Sprint-change proposal 2026-04-27** (`_bmad-output/planning-artifacts/sprint-change-proposal-2026-04-27.md`): the proposal that inserted Epic 11.5 and approved the Epic 12 redraft path. Section 4.1 lists the three options the project lead was offered (passive ASSEMBLER wordlist / drop 12.6 entirely / reinstate via different mechanism); the picked path is **drop 12.6 entirely** per `project_epic12_redraft_required.md`.

### Git intelligence

Recent commits relevant to this story:

| Commit | Title | Relevance |
|---|---|---|
| `4f1dec3` | harden print_throw_description table walk with wrap-safe 16-bit ADD HL,DE | Story 11.5.4 (just landed); 805 PASS / 17518 bytes baseline |
| `822b5a8` | make EVALUATE THROW-safe so caught -58 / -258..-271 work via EVALUATE harness | Story 11.5.3 |
| `493e750` | throw on stack overflow | Story 11.5.2 |
| `634deec` | epic 11 retro and 11.5 course change | Epic 11.5 charter (sprint-change-proposal-2026-04-27 + epics.md Epic 11.5 section) |

No commits since `4f1dec3` — Story 11.5.5 is the next pass after Story 11.5.4's commit.

### Testing standards

- **No new tests.** Per AC #14 + Task 7 of `project_epic_11_5_scope.md` discipline, this is document-surgery only.
- **Regression net** is the byte-identity build check (AC #9) and the grep-residual-reference check (AC #8). Both are inspection-level, not runtime.
- **Adversarial review** discipline (AC #11) — at least 1 LOW finding expected. Most likely (a) cross-reference completeness or (d) AC #8 grep-classification honesty.

### Project Structure Notes

- All changes in `_bmad-output/planning-artifacts/epics.md`, `_bmad-output/implementation-artifacts/sprint-status.yaml`, and the memory directory.
- No new files. No directory moves. No source files touched.
- No Makefile changes. No test files touched.
- Pre-redraft sanity: `git status` should show `epics.md`, `sprint-status.yaml`, plus the touched memory files modified — and nothing else.

### References

- [Source: epics.md#Story-11.5.5](../planning-artifacts/epics.md) — story spec lines 1037-1075
- [Source: epics.md#Epic-12](../planning-artifacts/epics.md) — primary edit target lines 1133-1349
- [Source: epics.md#Story-11.5.7](../planning-artifacts/epics.md) — close-out gate dependency lines 1101-1131 (specifically `:1121-1123` AC about Epic 12 redraft verification)
- [Source: sprint-change-proposal-2026-04-27.md](../planning-artifacts/sprint-change-proposal-2026-04-27.md) — the proposal that inserted Epic 11.5 and approved the Epic 12 redraft path
- [Source: sprint-change-proposal-2026-04-20.md](../planning-artifacts/sprint-change-proposal-2026-04-20.md) — earlier ASSEMBLER.FTH rollback wave; the redraft must reflect both waves
- [Source: prd.md#Epic-12](../planning-artifacts/prd.md) — PRD Epic 12 row in requirements-summary table at `:315`
- [Source: prd.md#FR30](../planning-artifacts/prd.md) — FR30 withdrawal note at `:418`
- [Source: architecture.md#E12-D4](../planning-artifacts/architecture.md) — WITHDRAWN E12-D4 block at `:344-348`
- [Source: architecture.md#Epic-to-file-mapping](../planning-artifacts/architecture.md) — Epic 12 row at `:789`
- [Source: 11.5-4-…md](11.5-4-print-throw-description-table-walk-hardening.md) — most recent done sibling; AC structure / Completion Notes template / 805-PASS baseline source
- [Source: 11.5-3-…md](11.5-3-paren-evaluate-source-frame-fix.md) — Story 11.5.3 in-pass-fix policy precedent
- [Source: 11.5-2-…md](11.5-2-stack-overflow-throw-3-guard.md) — Story 11.5.2 in-pass-fix policy precedent
- [Source: epic-11-retro-2026-04-27.md](epic-11-retro-2026-04-27.md) — Epic 11 retro that surfaced the redraft signal
- [Source: project_epic12_redraft_required.md](../../../.claude/projects/-home-ant-src-microbeast-antforth/memory/project_epic12_redraft_required.md) — the redraft-required flag this story closes
- [Source: project_assembler_keep_assembly.md](../../../.claude/projects/-home-ant-src-microbeast-antforth/memory/project_assembler_keep_assembly.md) — cumulative rationale for the rollback (verified current per AC #7)
- [Source: project_asm_hash_dispatch_hack.md](../../../.claude/projects/-home-ant-src-microbeast-antforth/memory/project_asm_hash_dispatch_hack.md) — asm-`#` hack permanence (verified current per AC #7)
- [Source: project_phase2_scope.md](../../../.claude/projects/-home-ant-src-microbeast-antforth/memory/project_phase2_scope.md) — Phase-2 epic structure (verified current per AC #7)
- [Source: project_epic_11_5_scope.md](../../../.claude/projects/-home-ant-src-microbeast-antforth/memory/project_epic_11_5_scope.md) — Epic 11.5 charter (Story 11.5.5 row updated per AC #7)
- [Source: feedback_systematic_reference_check.md](../../../.claude/projects/-home-ant-src-microbeast-antforth/memory/feedback_systematic_reference_check.md) — grep-not-memory discipline for AC #10
- [Source: feedback_adversarial_review.md](../../../.claude/projects/-home-ant-src-microbeast-antforth/memory/feedback_adversarial_review.md) — review-must-find-things discipline for AC #11
- [Source: feedback_follow_process.md](../../../.claude/projects/-home-ant-src-microbeast-antforth/memory/feedback_follow_process.md) — don't-ask-for-obvious-next-steps discipline for AC #15
- [Source: feedback_repl_tests_preferred.md](../../../.claude/projects/-home-ant-src-microbeast-antforth/memory/feedback_repl_tests_preferred.md) — REPL-test convention (does not apply per AC #14, but cited for completeness)

## Dev Agent Record

### Agent Model Used

claude-opus-4-7 (1M context)

### Debug Log References

- `make clean && make` — clean build pre and post; 0 errors / 0 warnings each pass.
- `wc -c build/antforth.com` — 17518 bytes pre and post (byte-identical).
- `make test-repl 2>&1 | tail -5 > /tmp/{pre,post}-115-5.log` + `diff` — FILES IDENTICAL.
- `make test-repl 2>&1 | grep -c '^PASS:'` — 805 PASS / 0 FAIL pre and post.

### Completion Notes List

**Task 1 — Pre-redraft baseline.**
- Build: clean (0 errors / 0 warnings).
- `wc -c build/antforth.com` = 17518 bytes (matches Story 11.5.4 baseline).
- `epics.md:1133-1137` baseline (byte-for-byte):
  ```
  ## Epic 12: Multi-Vocabulary Search-Order

  ⚠️ **STALE — pending Story 11.5.5 redraft (2026-04-27).** This section was drafted before the 2026-04-27 direction that there will be **no ASSEMBLER wordlist, no ASSEMBLER.FTH, and no auto-activation**. The hard-coded assembler in `src/assembler.asm` stays as-is forever. Story 11.5.5 will: (a) drop Story 12.6 entirely, (b) renumber Story 12.7 → Story 12.6, (c) scrub ASSEMBLER-wordlist references from this epic intro and any Story 12.1 ACs that assume them. Until that redraft lands, the sections below should be read as historical-with-known-stale-elements.

  Users can create and manage multiple wordlists, control the search order, and direct definitions into specific wordlists — all while existing CODE source files assemble unchanged against the unchanged hard-coded assembler subsystem. Shippable as antforth 1.12.
  ```
- Epic 12 section line span pre-redraft: `awk '/^## Epic 12:/,/^## Epic 13:/' | wc -l` = 219 (matches "≈ 218" expectation; awk's range is inclusive of both bounds, so the 219 reflects 218 Epic-12 lines + the `## Epic 13:` line itself).
- Pre-redraft `make test-repl`: 805 PASS / 0 FAIL.

**Task 2 — Epic 12 title + intro + stale-banner.**
- Title at `epics.md:1133` already correct (`## Epic 12: Multi-Vocabulary Search-Order`) per 2026-04-27 in-flight edit; no further title change required.
- Intro at `epics.md:1137` already free of ASSEMBLER wordlist activation, opcode-word registration, and `CODE`/`END-CODE` auto-activation references. Existing wording: "Users can create and manage multiple wordlists, control the search order, and direct definitions into specific wordlists — all while existing CODE source files assemble unchanged against the unchanged hard-coded assembler subsystem. Shippable as antforth 1.12." Forward-looking and clean — no edit applied.
- Stale-banner at `epics.md:1135` removed in full and replaced with single-line blockquote closure cross-reference:
  ```
  > Redrafted 2026-04-27..2026-04-28 by Story 11.5.5 to match the 2026-04-20 + 2026-04-27 ASSEMBLER rollback decisions; see `_bmad-output/implementation-artifacts/11.5-5-epic-12-redraft.md`.
  ```
- Net delta from Task 2: −4 lines in `epics.md` (the multi-line `⚠️ **STALE …**` paragraph collapsed to one `>` blockquote).

**Task 3 — Story 12.1 ASSEMBLER bucket-slot pre-allocation scan.**
- Read `epics.md:1139-1169` (Story 12.1 in full).
- `grep -in 'assembler'` over Story 12.1 line bounds: zero hits. Story 12.1 was already kept clean by the 2026-04-27 in-flight pass; the 64-bucket layout in the AC at `:1165` (`WORDLIST_SIZE EQU 130`, `WORDLIST_BUCKETS EQU 64`) is sized for the user-facing search-order wordsets only, with no ASSEMBLER bucket pre-allocation.
- **No edits required** to Story 12.1.

**Task 4 — Story 12.6 deletion.**
- Story 12.6 was at `epics.md:1287-1317` (31 lines: heading + 27 body lines + 2 trailing blanks).
- Atomic edit combined Task 4 (delete Story 12.6 incl. trailing blank) with Task 5.2 (rename Story 12.7 heading to Story 12.6) in a single Edit call, replacing the 32-line block (Story 12.6 + Story 12.7 heading) with the renumbered Story 12.6 heading.
- Post-deletion: `### Story 12.7:` no longer exists in `epics.md`; the next heading after `### Story 12.5:` (line 1267) is `### Story 12.6:` (line 1287, content = the renumbered close-out gate).

**Task 5 — Story 12.7 → 12.6 renumber + AC pruning.**
- Heading rename: `### Story 12.7: Epic 12 benchmark, CODE backward-compat suite + regression gate (CCD-4)` → `### Story 12.6: Epic 12 benchmark, CODE backward-compat suite + regression gate (CCD-4)` (line 1287).
- AC pruning at the CODE-source-file backward-compat AC (formerly :1303-1305):
  - **Before:** `**When** assembled against the Epic-12 binary` / `**Then** output is byte-identical to the pre-phase-2 reference — FR31, NFR14 verified comprehensively.`
  - **After:** `**When** assembled against the Epic-12 binary (against the unchanged hard-coded assembler subsystem in `src/assembler.asm` — no opcode migration occurred)` / `**Then** output is byte-identical to the pre-phase-2 reference — FR31, NFR14 verified comprehensively.`
  - Rationale: explicitly framed against the unchanged hard-coded assembler subsystem; "no opcode migration occurred" makes the rollback shape unambiguous.
- AC pruning at the kernel-ROM-size-delta AC (formerly :1307-1309):
  - **Before:** `…the additions (Search-Order machinery, per-wordlist hash layout, `ASSEMBLER` wordlist registration, `CODE`/`END-CODE` auto-activation hooks) are expected to grow the kernel…`
  - **After:** `…the additions (Search-Order machinery, per-wordlist hash layout) are expected to grow the kernel…`
  - The "ASSEMBLER wordlist registration, CODE/END-CODE auto-activation hooks" sub-bullet stripped; the Search-Order machinery + per-wordlist hash layout bullets retained.
- Other ACs (NFR2 benchmark, slot-0 hit benchmark, full Phase-1+9/10/11 regression, real-MicroBeast smoke test) untouched — they're orthogonal to ASSEMBLER scope.

**Task 6 — sprint-status.yaml Epic 12 rows.**
- Transition comment block (`:149-156`) replaced with a 3-line closure cross-reference:
  ```
  # Epic 12 redrafted 2026-04-27..2026-04-28 by Story 11.5.5; rows
  # below are post-redraft (Story 12.6 deleted, Story 12.7 renumbered
  # to 12.6). See _bmad-output/implementation-artifacts/11.5-5-epic-12-redraft.md.
  ```
- Row `12-6-assembler-wordlist-and-auto-activation-in-code-end-code: backlog` deleted.
- Row `12-7-epic-12-benchmark-code-backward-compat-suite-and-regression-gate-ccd-4: backlog` renamed to `12-6-epic-12-benchmark-code-backward-compat-suite-and-regression-gate-ccd-4: backlog`.
- Resulting row order verified: `epic-12 / 12-1 / 12-2 / 12-3 / 12-4 / 12-5 / 12-6 (new — was 12-7) / epic-12-retrospective`. ✓

**Task 7 — Memories.**
- **7a** `project_phase2_scope.md` Epic 12 entry verified: already says "Search-Order Wordset only (Stories 12.1–12.5 + close-out gate, post-Story-11.5.5 redraft). **No `ASSEMBLER.FTH`. No `ASSEMBLER` wordlist. No auto-activation. No opcode relocation.**". No edit required.
- **7b** `project_epic12_redraft_required.md` **replaced with closure body** (file kept, not deleted, per the AC's recommendation). Frontmatter `name` updated to "Epic 12 redraft — closed 2026-04-28"; body now reads as a 3-paragraph closure pointer to this story file. MEMORY.md index hook left intact (file kept).
- **7c** `project_assembler_keep_assembly.md` verified: cumulative rationale captures both 2026-04-20 (.FTH rollback) and 2026-04-27 (wordlist + auto-activation drop) waves. No edit required.
- **7d** `project_asm_hash_dispatch_hack.md` verified: marked permanent 2026-04-27. No edit required.
- **7e** `project_epic_11_5_scope.md` Story 11.5.5 line updated:
  - **Before:** `**11.5.5 — Epic 12 redraft.** Document-surgery; the path is settled (project lead 2026-04-27): no ASSEMBLER wordlist at all. Delete Story 12.6 entirely; drop ASSEMBLER references from Epic 12 title / intro / Story 12.1 ACs; renumber Story 12.7 → 12.6. The asm-`#` dispatch hack is now permanent (no retirement vehicle planned) — see `project_asm_hash_dispatch_hack.md`.`
  - **After:** `**11.5.5 — Epic 12 redraft.** **Closed 2026-04-28** — see `_bmad-output/implementation-artifacts/11.5-5-epic-12-redraft.md`. Epic 12 redrafted; Story 12.6 deleted; Story 12.7 renumbered to 12.6; PRD/architecture verified clean. The asm-`#` dispatch hack is now permanent (no retirement vehicle planned) — see `project_asm_hash_dispatch_hack.md`.`
- **7.6** `MEMORY.md` index unchanged (file 7b kept, not deleted; AC #7 last bullet directs "left intact unless deleted"). The index hook's pre-redraft description ("must redraft before any 12.x dev — Story 11.5.5 owns the redraft") becomes slightly stale but the file body itself is now the closure-of-record; future agents grepping for `epic12_redraft` find a non-stub answer at the file body.

**Task 8 — PRD + architecture residual references.**
- Survey grep `grep -nE 'ASSEMBLER wordlist|ASSEMBLER\.FTH|auto-activat' _bmad-output/planning-artifacts/{prd,architecture,epics}.md` returned 28 hits (8 prd / 12 epics / 8 architecture).
- Classification (line-by-line):
  - **prd.md** — 8 hits, all **current-statement-of-rollback** (Executive Summary `:63`, post-MVP first-consumer `:86`, MVP rule `:113`, vocabulary-model table `:129`, Mo's journey `:266`, Epic 11.5 row `:314`, Epic 12 row `:315`, FR30 strikethrough `:418`).
  - **epics.md** — 12 hits: change-log `:8` (historical), Phase-2 intro `:26` (current-statement), strikethrough FR30 row `:195` (superseded), Epic 11.5 intro `:927` (current-statement), and 8 hits inside Story 11.5.5's own spec body `:1037-:1071` (story-spec — describes this very redraft).
  - **architecture.md** — 8 hits: FR23-FR29 description `:47` (current-statement), E12-D4 strikethrough heading `:344` (superseded), E12-D4 status block `:348` (superseded — "deletes" tense fixed in Task 11/L1), Decision Impact Analysis `:405` (current-statement), project-structure tree `:677` (current-statement), tests-tree withdrawn comment `:723` (superseded), Epic-to-file mapping `:789` (current-statement), Finding 3 superseded marker `:919` (historical).
- Zero **NEEDS-EDIT** hits surfaced.
- AC #8 enumeration (a)-(m) cross-check: every (a)-(m) line appears in the grep output and is correctly classified as current-statement-of-rollback or superseded. The enumeration was complete and exact.
- Post-edit grep (after Task 11/L1 fix): same 28 hits, all classified historical/superseded/current-statement; zero NEEDS-EDIT remain.

**Task 9 — Zero binary + test delta verification.**
- Pre-redraft: `wc -c build/antforth.com` = 17518 bytes; `make test-repl` = 805 PASS / 0 FAIL.
- Post-redraft: `wc -c build/antforth.com` = 17518 bytes; `make test-repl` = 805 PASS / 0 FAIL.
- `diff /tmp/pre-115-5.log /tmp/post-115-5-final.log` = FILES IDENTICAL.
- Zero binary delta + zero test delta confirmed. Document-only redraft scope preserved.

**Task 10 — Reference-check honesty.**
- Task 8.1's grep was run literally (verbatim AC #8 command). No skim-from-memory.
- AC #8 enumeration (a)-(m) all matched the grep output. No enumeration-vs-grep discrepancy at the (a)-(m) level. The "expected ≥1 mismatch" surfaces at a different level — see Task 11 finding L2 (scope clarity).

**Task 11 — Adversarial self-review.**
- **(a) Cross-reference completeness — `grep -n 'Story 12\.6'`**: 5 hits in planning-artifacts (excluding sprint-change-proposals). All correctly disambiguate "the original Story 12.6" (deleted) vs "Story 12.6" (the new renumbered close-out). ✓
- **(b) Story 12.7 phantom references — `grep -n 'Story 12\.7\|12\.7'`**: 1 hit in `epics.md:1057` (Story 11.5.5's own AC referencing the pre-redraft story number) + 1 hit in `sprint-status.yaml:150` (my closure comment "Story 12.7 renumbered to 12.6"). Both intentional historical references. ✓
- **(c) Sprint-status placeholder comment honesty**: pre-redraft `:149-156` "Story 11.5.5 redrafts Epic 12. Epic 12 backlog rows below are kept as placeholders" replaced with closure cross-reference at Task 6.3. ✓
- **(d) AC #8 grep-classification honesty**: re-ran the grep independently; cross-checked against AC #8 enumeration. All 13 (a)-(m) entries match. **Finding L2 (LOW, scope clarity)**: AC #8's enumeration was scoped to the in-flight 2026-04-27 PRD/architecture pass (13 lines); the grep's full result also surfaces 5 epics.md hits + `architecture.md:919` Finding 3 marker that fall outside the (a)-(m) enumeration but inside the grep scope. All extra hits classify as historical/superseded/current-statement — none are NEEDS-EDIT. The AC author's enumeration was honest and complete *for the in-flight 2026-04-27 surface area*, but a literal reading of "the AC #8 enumeration is the source of truth" is incorrect — **the grep is the source of truth**, the enumeration is a checklist of expected hits. Accepted as LOW; no edit required.
- **(e) Stale-banner removal vs. rewrite**: re-read Task 2's edit. Original banner-shaped paragraph (multi-line `⚠️ **STALE …**` warning paragraph) literally **removed**, replaced with a single-line `>` blockquote closure note. ✓
- **(f) Memory closure-note consistency**: phrasing across the touched memories — `project_epic_11_5_scope.md` uses "**Closed 2026-04-28**"; `project_epic12_redraft_required.md` frontmatter uses "Epic 12 redraft — closed 2026-04-28" with body "**Closed 2026-04-28**". Both use the canonical "Closed (date)" form. The MEMORY.md hook for `project_hardware_crash_audit.md` uses "RESOLVED 2026-04-28" — this is a **separate convention** for that file's hook, not within this story's surface area; consistency within the touched-memories scope is preserved.
- **Finding L1 (LOW, narrative tense)**: `architecture.md:348` E12-D4 status block said "Story 11.5.5 (Epic 12 redraft) **deletes** the original Story 12.6 from `epics.md`" — present-tense forward-pointer. Post-redraft, "deleted" is more accurate. Applied in-pass per AC #12: changed to "Story 11.5.5 (Epic 12 redraft, closed 2026-04-28) **deleted** the original Story 12.6 from `epics.md`." Re-ran build + `make test-repl`: 17518 bytes / 805 PASS / 0 FAIL — no delta from the L1 fix.
- **Triage**: 2 LOW findings (L1, L2); 0 MEDIUM; 0 HIGH. L1 fixed in-pass; L2 accepted with rationale. ≥1 LOW finding requirement (per `feedback_adversarial_review.md`) satisfied.

**Task 12 — In-pass-fix discipline + structural-load-bearing escalation.**
- In-pass fixes applied during Tasks 2-11:
  - Task 2: stale-banner replacement (banner → 1-line blockquote).
  - Task 5: AC pruning at CODE-source-file backward-compat (framing clarification) + kernel-ROM-size-delta bullet.
  - Task 11/L1: `architecture.md:348` "deletes" → "deleted" + closure-date annotation.
- **Structural-load-bearing findings**: zero. PRD/architecture FRs and NFRs are not changed in this pass — FR30 was deleted 2026-04-27, E12-D4 was withdrawn 2026-04-27, NFR4 / NFR14 / FR31 framings unchanged. No HALT triggered.
- AC #12's escalation gate did not trigger.

**Task 13 — Sprint-status row flip.**
- Story 11.5.5 row flipped to `done` at code-review close 2026-04-28 (after Task 14 review-fix pass).
- `epic-11.5: in-progress` row was **not** edited. That flip is owned by Story 11.5.7 (Epic 11.5 close-out gate).

**Task 14 — Code-review fix pass (2026-04-28).**

Adversarial code review found 3 HIGH + 2 MEDIUM + 1 LOW finding that the original dev pass missed. Root cause: AC #8 grep regex `'ASSEMBLER wordlist|ASSEMBLER\.FTH|auto-activat'` requires a literal space between `R` and `w`, so it does not match the markdown-backticked form `` `ASSEMBLER` wordlist `` (backtick + space). The Phase-2 overview block at `epics.md:60-230` uses this backticked form throughout and was therefore invisible to the dev's "source-of-truth" grep — three forward-looking ASSEMBLER promises survived the redraft.

**Findings + fixes:**

- **H1 — `epics.md:228-230` (Epic 12 epic-overview).** Title still read `### Epic 12: Multi-Vocabulary Search-Order & ASSEMBLER Wordlist`; description still promised "automatic `ASSEMBLER` wordlist activation inside `CODE`/`END-CODE`"; FRs-covered listed FR30. **Fix:** title → `### Epic 12: Multi-Vocabulary Search-Order`; description rewritten to "all while existing CODE source files assemble unchanged against the unchanged hard-coded assembler subsystem in `src/assembler.asm`. No `ASSEMBLER` wordlist, no `ASSEMBLER.FTH`, no `CODE`/`END-CODE` auto-activation"; FRs-covered → `FR23..FR29, FR31 (FR30 withdrawn 2026-04-27 — see :195)`.
- **H2 — `epics.md:68` (FR30 in Phase-2 FR list).** Plain (non-strikethrough) "FR30: The `ASSEMBLER` wordlist is automatically activated…" presented as a live FR. **Fix:** wrapped in `~~ ~~` strikethrough with `**withdrawn 2026-04-27** … FR30 is a deliberate gap.` annotation matching `:195` table-row convention.
- **H3 — `epics.md:142` (source-file layout).** Bullet promised `src/*.asm` would be edited "for ABORT→THROW migration, multi-vocabulary changes, and `ASSEMBLER` wordlist registration". **Fix:** dropped "ASSEMBLER wordlist registration"; added explicit "(`src/assembler.asm` itself is **unchanged** in Phase 2 — no ASSEMBLER wordlist registration, no auto-activation hooks, per 2026-04-27 rollback)".
- **M4 — Broken AC #8 grep regex (root cause of H1-H3).** Documented in this Task 14 narrative. Future runs of AC #8 / AC #10 should use `'ASSEMBLER.{0,3}[Ww]ordlist|ASSEMBLER\.FTH|auto-activat'` (handles backticked + capitalised variants). Recommend updating the grep example in `epics.md:1071` to the broader form for any future redraft stories that inherit this AC pattern.
- **M5 — `epics.md:1143` (Story 12.1 So-that line).** Range `FR23–FR30 unblocked` included withdrawn FR30. **Fix:** `FR23–FR29, FR31 unblocked — FR30 withdrawn 2026-04-27` (matches `architecture.md:47` framing).
- **L6 — `sprint-status.yaml:150` closure-comment phantom Story 12.7 reference.** Reworded to "(original Story 12.6 ASSEMBLER-wordlist deleted; the current Story 12.6 row is the renumbered close-out gate, formerly Story 12.7)" so a future reader greppping for "Story 12.7" lands with full context.

**Post-fix verification:**

- Broader grep (`'ASSEMBLER.{0,3}[Ww]ordlist|ASSEMBLER\.FTH|auto-activat'`) re-run across `prd.md` + `architecture.md` + `epics.md`: 31 hits total (8 prd / 8 architecture / 15 epics — up from the 28 the dev's narrower regex caught; the +3 are the previously-invisible H1/H2/H3 hits, now correctly classified as historical/superseded/current-statement-of-rollback).
- Build: `make clean && make` — 0 errors / 0 warnings; `wc -c build/antforth.com` = 17518 bytes (unchanged from Task 9 baseline).
- Tests: `make test-repl` = 805 PASS / 0 FAIL (unchanged from Task 9 baseline).
- AC #9 zero-binary-delta + zero-test-delta property preserved across the review-fix pass.

**Self-critique on dev-pass adversarial review.** AC #11(d) probe ("AC #8 grep-classification honesty") was honestly executed against the literal grep pattern but the dev concluded the grep was the source of truth without verifying the regex actually matched the data shape it was scanning. The L2 finding the dev did surface ("scope clarity" — that the AC #8 enumeration was a checklist, not the source of truth) was directionally right but stopped at the wrong layer; the actual gap was one level deeper (the regex itself). Future redraft stories should include a regex-permissiveness check as part of AC #11(d) — e.g., "compare narrow grep hit count against a permissive variant; investigate any divergence."

### File List

**Modified:**
- `_bmad-output/planning-artifacts/epics.md` — Epic 12 stale-banner replaced; Story 12.6 deleted; Story 12.7 renumbered to 12.6; AC pruning at the renumbered Story 12.6. Code-review fix pass (Task 14): Epic 12 epic-overview at `:228-230` rewritten (title/description/FRs-covered); FR30 withdrawn at `:68`; source-file-layout bullet at `:142` clarified; Story 12.1 So-that line FR range corrected at `:1143`.
- `_bmad-output/planning-artifacts/architecture.md` — E12-D4 WITHDRAWN block tense fix at `:348` ("deletes" → "deleted, closed 2026-04-28") per Task 11/L1.
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — Epic 11.5/12 transition comment replaced with closure cross-reference; Epic 12 row block has 12-6 (ASSEMBLER) deleted and 12-7 (close-out) renumbered to 12-6; Story 11.5.5 own row flipped `backlog` → `review` → `done` (final flip at code-review close 2026-04-28). Code-review fix pass (Task 14): closure comment at `:149-152` reworded with "formerly Story 12.7" disambiguation.
- `_bmad-output/implementation-artifacts/11.5-5-epic-12-redraft.md` — task checkboxes ticked; Completion Notes / File List / Change Log populated; Status: backlog → review → done. Task 14 added documenting the code-review fix pass.
- `~/.claude/projects/-home-ant-src-microbeast-antforth/memory/project_epic12_redraft_required.md` — frontmatter + body replaced with closure-pointer.
- `~/.claude/projects/-home-ant-src-microbeast-antforth/memory/project_epic_11_5_scope.md` — Story 11.5.5 line annotated **Closed 2026-04-28** with cross-reference to this story file.

**Verified-no-edit-required (read-only inspection):**
- `_bmad-output/planning-artifacts/prd.md` (8 ASSEMBLER-related hits, all current-statement-of-rollback or superseded).
- `~/.claude/projects/-home-ant-src-microbeast-antforth/memory/project_phase2_scope.md`
- `~/.claude/projects/-home-ant-src-microbeast-antforth/memory/project_assembler_keep_assembly.md`
- `~/.claude/projects/-home-ant-src-microbeast-antforth/memory/project_asm_hash_dispatch_hack.md`
- `~/.claude/projects/-home-ant-src-microbeast-antforth/memory/MEMORY.md`

## Change Log

| Date | Change | Author |
|------|--------|--------|
| 2026-04-28 | Story 11.5.5 created from `epics.md:1037-1075` (Story 11.5.5 spec) plus `sprint-change-proposal-2026-04-27.md` Section 4.1 + `project_epic12_redraft_required.md` (path settled: drop Story 12.6 entirely; renumber 12.7 → 12.6). Pre-redraft baseline captured in Tasks below: `epics.md` Epic 12 section ≈ 218 lines (`:1133`-`:1351`); Story 12.6 at `:1287-1317`; Story 12.7 at `:1319-1349`; sprint-status.yaml Epic 12 rows at `:185-193`. PRD/architecture in-flight 2026-04-27 edits identified for verification (AC #8 enumeration a-m). Adversarial-review probe categories defined (AC #11 a-f). In-pass-fix policy + structural-load-bearing escalation gate set per AC #12. Status: backlog → ready-for-dev. | Ant (claude-opus-4-7) |
| 2026-04-28 | Dev pass: Epic 12 redrafted per AC #1-#13. Stale-banner at `epics.md:1135` replaced with 1-line blockquote closure cross-reference; Story 12.6 (ASSEMBLER wordlist + auto-activation) deleted; Story 12.7 renumbered to Story 12.6 with AC pruning (CODE-source-file backward-compat reframed against unchanged hard-coded assembler subsystem; kernel-ROM-size-delta sub-bullet stripped of ASSEMBLER wordlist registration + CODE/END-CODE auto-activation hooks). sprint-status.yaml Epic 12 rows updated (12-6 deleted, 12-7 → 12-6 renumber, transition comment replaced with closure cross-reference). Memories updated: `project_epic12_redraft_required.md` replaced with closure-pointer body; `project_epic_11_5_scope.md` Story 11.5.5 line annotated **Closed 2026-04-28**; `project_phase2_scope.md` / `project_assembler_keep_assembly.md` / `project_asm_hash_dispatch_hack.md` / `MEMORY.md` verified current. PRD verified clean (zero NEEDS-EDIT residuals). Architecture: 1 in-pass narrative scrub at `:348` (E12-D4 WITHDRAWN block "deletes" → "deleted" + closure-date annotation per Task 11/L1). Binary 17518 bytes / tests 805 PASS / 0 FAIL byte-identical pre/post. 2 LOW review findings (L1 fixed in-pass; L2 scope-clarity accepted); 0 MEDIUM/HIGH. Status: ready-for-dev → review. | Ant (claude-opus-4-7) |
| 2026-04-28 | Code-review fix pass (Task 14). Adversarial review caught 3 HIGH + 2 MEDIUM + 1 LOW findings missed by the dev-pass adversarial-review (root cause: AC #8 grep regex `'ASSEMBLER wordlist\|...'` doesn't match the markdown-backticked form `` `ASSEMBLER` wordlist `` used throughout the Phase-2 overview at `epics.md:60-230`). Fixed: `epics.md:228-230` Epic 12 epic-overview (title + description + FRs-covered scrubbed); `epics.md:68` FR30 withdrawn-strikethrough; `epics.md:142` source-file-layout bullet clarified ("`src/assembler.asm` itself is **unchanged** in Phase 2"); `epics.md:1143` Story 12.1 So-that FR range `FR23–FR30` → `FR23–FR29, FR31`; `sprint-status.yaml:149-152` closure comment reworded with "formerly Story 12.7" disambiguation. Re-ran broader grep (`'ASSEMBLER.{0,3}[Ww]ordlist\|...'`): 31 hits all classified historical/superseded/current-statement-of-rollback; zero NEEDS-EDIT. Build 17518 bytes / tests 805 PASS / 0 FAIL — AC #9 zero-binary-delta + zero-test-delta property preserved. Sprint-status row 11.5-5 flipped `review` → `done`. Status: review → done. | Ant (claude-opus-4-7) |
